// Code generated by ucpgen. DO NOT EDIT.
// Source: common/loyalty.json (spec release/2026-08-25@cd78fb3)

package common

import (
	"encoding/json"
	"errors"
	ucp "github.com/chaz8081/ucp-go"
	"github.com/chaz8081/ucp-go/common/types"
	shoppingtypes "github.com/chaz8081/ucp-go/shopping/types"
)

// LoyaltyCart Cart extended with Loyalty capability.
type LoyaltyCart struct {
	// Outstanding extension-defined Actions for this cart.
	Actions     *types.Actions             `json:"actions,omitzero"`
	Attribution *shoppingtypes.Attribution `json:"attribution,omitzero"`
	// Optional buyer information for personalized estimates.
	Buyer *shoppingtypes.Buyer `json:"buyer,omitzero"`
	// Buyer signals for localization (country, region, postal_code). Merchant uses for pricing, availability, currency. Falls back to geo-IP if omitted.
	Context *types.Context `json:"context,omitzero"`
	// URL for cart handoff and session recovery. Enables sharing and human-in-the-loop flows.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ContinueURL *string `json:"continue_url,omitzero"`
	// ISO 4217 currency code. Determined by merchant based on context or geo-IP.
	Currency string `json:"currency"`
	// Cart expiry timestamp (RFC 3339). Optional.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ExpiresAt *string `json:"expires_at,omitzero"`
	// Unique cart identifier.
	ID string `json:"id"`
	// Cart line items. Same structure as checkout. Full replacement on update.
	LineItems []shoppingtypes.LineItem `json:"line_items"`
	// Optional merchant links (policies, FAQs).
	Links   []types.Link    `json:"links,omitzero"`
	Loyalty *LoyaltyLoyalty `json:"loyalty,omitzero"`
	// Validation messages, warnings, or informational notices.
	Messages []types.Message `json:"messages,omitzero"`
	// Policies (e.g., return/refund terms) that apply to the items in this cart. `applies_to` targets are relative to the response root; when absent or empty, refer to the URLs in `links[]`.
	Policies []types.Policy `json:"policies,omitzero"`
	Signals  *types.Signals `json:"signals,omitzero"`
	// Estimated cost breakdown. May be partial if shipping/tax not yet calculable.
	Totals types.Totals    `json:"totals"`
	UCP    ucp.UCPMetadata `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LoyaltyCart) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LoyaltyCart: null is not a valid object")
	}
	type LoyaltyCartAlias LoyaltyCart
	var named LoyaltyCartAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LoyaltyCart(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 5)
	for _, name := range []string{"currency", "id", "line_items", "totals", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "actions")
	delete(all, "attribution")
	delete(all, "buyer")
	delete(all, "context")
	delete(all, "continue_url")
	delete(all, "currency")
	delete(all, "expires_at")
	delete(all, "id")
	delete(all, "line_items")
	delete(all, "links")
	delete(all, "loyalty")
	delete(all, "messages")
	delete(all, "policies")
	delete(all, "signals")
	delete(all, "totals")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LoyaltyCart) MarshalJSON() ([]byte, error) {
	type LoyaltyCartAlias LoyaltyCart
	named, err := json.Marshal(LoyaltyCartAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LoyaltyCart) Validate() error {
	if v.present != nil {
		if !v.present["currency"] {
			return errors.New("currency: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["line_items"] {
			return errors.New("line_items: required property is missing")
		}
		if !v.present["totals"] {
			return errors.New("totals: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	if v.Actions != nil {
		if err := v.Actions.Validate(); err != nil {
			return err
		}
	}
	if v.Attribution != nil {
		if err := v.Attribution.Validate(); err != nil {
			return err
		}
	}
	if v.Buyer != nil {
		if err := v.Buyer.Validate(); err != nil {
			return err
		}
	}
	if v.Context != nil {
		if err := v.Context.Validate(); err != nil {
			return err
		}
	}
	for i := range v.LineItems {
		if err := v.LineItems[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Links {
		if err := v.Links[i].Validate(); err != nil {
			return err
		}
	}
	if v.Loyalty != nil {
		if err := v.Loyalty.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	if v.Signals != nil {
		if err := v.Signals.Validate(); err != nil {
			return err
		}
	}
	if err := v.Totals.Validate(); err != nil {
		return err
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// LoyaltyCheckout Checkout extended with Loyalty capability.
type LoyaltyCheckout struct {
	// Outstanding extension-defined Actions for this checkout.
	Actions     *types.Actions             `json:"actions,omitzero"`
	Attribution *shoppingtypes.Attribution `json:"attribution,omitzero"`
	// Representation of the buyer.
	Buyer   *shoppingtypes.Buyer `json:"buyer,omitzero"`
	Context *types.Context       `json:"context,omitzero"`
	// URL for checkout handoff and session recovery. MUST be provided when status is requires_escalation. See specification for format and availability requirements.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ContinueURL *string `json:"continue_url,omitzero"`
	// ISO 4217 currency code reflecting the merchant's market determination. Derived from address, context, and geo IP—buyers provide signals, merchants determine currency.
	Currency string `json:"currency"`
	// RFC 3339 expiry timestamp. Default TTL is 6 hours from creation if not sent.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ExpiresAt *string `json:"expires_at,omitzero"`
	// Unique identifier of the checkout session.
	ID string `json:"id"`
	// List of line items being checked out.
	LineItems []shoppingtypes.LineItem `json:"line_items"`
	// Links to be displayed by the platform (Privacy Policy, TOS). Mandatory for legal compliance.
	Links   []types.Link    `json:"links"`
	Loyalty *LoyaltyLoyalty `json:"loyalty,omitzero"`
	// List of messages with error and info about the checkout session state.
	Messages []types.Message `json:"messages,omitzero"`
	// Details about an order created for this checkout session.
	Order   *shoppingtypes.OrderConfirmation `json:"order,omitzero"`
	Payment *types.Payment                   `json:"payment,omitzero"`
	// Policies (e.g., return/refund terms) that apply to the items in this checkout. `applies_to` targets are relative to the response root; when absent or empty, refer to the URLs in `links[]`.
	Policies []types.Policy `json:"policies,omitzero"`
	Signals  *types.Signals `json:"signals,omitzero"`
	// Checkout state indicating the current phase and required processing. See Checkout Status lifecycle documentation for state transition details.
	Status string `json:"status"`
	// Different cart totals.
	Totals types.Totals    `json:"totals"`
	UCP    ucp.UCPMetadata `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LoyaltyCheckout) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LoyaltyCheckout: null is not a valid object")
	}
	type LoyaltyCheckoutAlias LoyaltyCheckout
	var named LoyaltyCheckoutAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LoyaltyCheckout(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 7)
	for _, name := range []string{"currency", "id", "line_items", "links", "status", "totals", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "actions")
	delete(all, "attribution")
	delete(all, "buyer")
	delete(all, "context")
	delete(all, "continue_url")
	delete(all, "currency")
	delete(all, "expires_at")
	delete(all, "id")
	delete(all, "line_items")
	delete(all, "links")
	delete(all, "loyalty")
	delete(all, "messages")
	delete(all, "order")
	delete(all, "payment")
	delete(all, "policies")
	delete(all, "signals")
	delete(all, "status")
	delete(all, "totals")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LoyaltyCheckout) MarshalJSON() ([]byte, error) {
	type LoyaltyCheckoutAlias LoyaltyCheckout
	named, err := json.Marshal(LoyaltyCheckoutAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LoyaltyCheckout) Validate() error {
	if v.present != nil {
		if !v.present["currency"] {
			return errors.New("currency: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["line_items"] {
			return errors.New("line_items: required property is missing")
		}
		if !v.present["links"] {
			return errors.New("links: required property is missing")
		}
		if !v.present["status"] {
			return errors.New("status: required property is missing")
		}
		if !v.present["totals"] {
			return errors.New("totals: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	if v.Status != "incomplete" && v.Status != "requires_escalation" && v.Status != "ready_for_complete" && v.Status != "complete_in_progress" && v.Status != "completed" && v.Status != "canceled" {
		return errors.New("status: not one of the permitted values")
	}
	if v.Actions != nil {
		if err := v.Actions.Validate(); err != nil {
			return err
		}
	}
	if v.Attribution != nil {
		if err := v.Attribution.Validate(); err != nil {
			return err
		}
	}
	if v.Buyer != nil {
		if err := v.Buyer.Validate(); err != nil {
			return err
		}
	}
	if v.Context != nil {
		if err := v.Context.Validate(); err != nil {
			return err
		}
	}
	for i := range v.LineItems {
		if err := v.LineItems[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Links {
		if err := v.Links[i].Validate(); err != nil {
			return err
		}
	}
	if v.Loyalty != nil {
		if err := v.Loyalty.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	if v.Order != nil {
		if err := v.Order.Validate(); err != nil {
			return err
		}
	}
	if v.Payment != nil {
		if err := v.Payment.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	if v.Signals != nil {
		if err := v.Signals.Validate(); err != nil {
			return err
		}
	}
	if err := v.Totals.Validate(); err != nil {
		return err
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// LoyaltyEarningBreakdown Breakdown rule of the reward earnings
type LoyaltyEarningBreakdown struct {
	// Rewards earned from this rule.
	Amount LoyaltyRewardAmount `json:"amount"`
	// Optional `id` of the membership_tier_benefit that produced this rewards rule. Resolves against `membership_tier_benefit.id` within the same parent loyalty membership.
	BenefitID *string `json:"benefit_id,omitzero"`
	// A display-ready, human-readable rationale for the specific rewards (e.g. 2x on footwear).
	Description string `json:"description"`
	// Unique rewards breakdown rule identifier.
	ID string `json:"id"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LoyaltyEarningBreakdown) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LoyaltyEarningBreakdown: null is not a valid object")
	}
	type LoyaltyEarningBreakdownAlias LoyaltyEarningBreakdown
	var named LoyaltyEarningBreakdownAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LoyaltyEarningBreakdown(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 3)
	for _, name := range []string{"amount", "description", "id"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "amount")
	delete(all, "benefit_id")
	delete(all, "description")
	delete(all, "id")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LoyaltyEarningBreakdown) MarshalJSON() ([]byte, error) {
	type LoyaltyEarningBreakdownAlias LoyaltyEarningBreakdown
	named, err := json.Marshal(LoyaltyEarningBreakdownAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LoyaltyEarningBreakdown) Validate() error {
	if v.present != nil {
		if !v.present["amount"] {
			return errors.New("amount: required property is missing")
		}
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
	}
	if err := v.Amount.Validate(); err != nil {
		return err
	}
	return nil
}

// LoyaltyEarningForecast Preview of rewards to be earned from the current transaction.
type LoyaltyEarningForecast struct {
	// Total rewards to be earned if the transaction completes.
	Amount LoyaltyRewardAmount `json:"amount"`
	// List of breakdown of earning contributing to the total.
	Breakdown []LoyaltyEarningBreakdown `json:"breakdown,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LoyaltyEarningForecast) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LoyaltyEarningForecast: null is not a valid object")
	}
	type LoyaltyEarningForecastAlias LoyaltyEarningForecast
	var named LoyaltyEarningForecastAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LoyaltyEarningForecast(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"amount"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "amount")
	delete(all, "breakdown")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LoyaltyEarningForecast) MarshalJSON() ([]byte, error) {
	type LoyaltyEarningForecastAlias LoyaltyEarningForecast
	named, err := json.Marshal(LoyaltyEarningForecastAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LoyaltyEarningForecast) Validate() error {
	if v.present != nil {
		if !v.present["amount"] {
			return errors.New("amount: required property is missing")
		}
	}
	if err := v.Amount.Validate(); err != nil {
		return err
	}
	for i := range v.Breakdown {
		if err := v.Breakdown[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// LoyaltyLookup Catalog Lookup response extended with Loyalty capability.
//
// This schema is a oneOf of 2 alternatives with no shared
// properties, so it is carried as raw JSON. Typed alternatives are
// phase 4.
type LoyaltyLookup json.RawMessage

// Validate reports the first constraint violation, or nil.
func (v *LoyaltyLookup) Validate() error {
	return nil
}

// LoyaltyLoyalty Key-value map whose keys represent buyer/platform asserted eligibility claims and whose values represent associated membership information. All loyalty keys MUST use reverse-domain naming to ensure provenance and prevent collisions when multiple extensions contribute to the shared namespace.
type LoyaltyLoyalty map[string]LoyaltyLoyaltyMembership

// UnmarshalJSON rejects a bare null. encoding/json treats null as a
// no-op for every Go type, so without this the zero value would pass
// every check and a null document would validate as though it were a
// real value.
func (v *LoyaltyLoyalty) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LoyaltyLoyalty: null is not a valid object")
	}
	type LoyaltyLoyaltyAlias LoyaltyLoyalty
	var alias LoyaltyLoyaltyAlias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = LoyaltyLoyalty(alias)
	return nil
}

// Validate reports the first constraint violation, or nil.
func (v *LoyaltyLoyalty) Validate() error {
	for _, m := range *v {
		if err := m.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// LoyaltyLoyaltyMembership Loyalty membership the business has accepted for the eligibility claim represented by the parent map key. Programs that can be joined independently MUST be modeled as separate sibling entries under the loyalty map, distinguished by reverse-domain naming (e.g., 'com.example.rewards' and 'com.example.rewards.card').
type LoyaltyLoyaltyMembership struct {
	// A masked or partial version of the membership id for user recognition (e.g., '****5678'). MUST NOT be set if the membership has not been verified.
	DisplayID *string `json:"display_id,omitzero"`
	// Unique loyalty membership identifier.
	ID string `json:"id"`
	// Business specific name of the loyalty membership/program.
	Name string `json:"name"`
	// True if this membership requires additional verification.
	Provisional bool `json:"provisional"`
	// Reward types and earning forecasts associated with this membership. Each object encapsulates one type of reward.
	Rewards []LoyaltyMembershipReward `json:"rewards,omitzero"`
	// Active or display-safe tier context for this membership. Most programs are single-status (one entry); programs with parallel status dimensions (e.g., current and lifetime) populate one entry per active tier. Omitted when no tier context has been resolved.
	Tiers []LoyaltyMembershipTier `json:"tiers,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LoyaltyLoyaltyMembership) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LoyaltyLoyaltyMembership: null is not a valid object")
	}
	type LoyaltyLoyaltyMembershipAlias LoyaltyLoyaltyMembership
	var named LoyaltyLoyaltyMembershipAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LoyaltyLoyaltyMembership(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 3)
	for _, name := range []string{"id", "name", "provisional"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "display_id")
	delete(all, "id")
	delete(all, "name")
	delete(all, "provisional")
	delete(all, "rewards")
	delete(all, "tiers")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LoyaltyLoyaltyMembership) MarshalJSON() ([]byte, error) {
	type LoyaltyLoyaltyMembershipAlias LoyaltyLoyaltyMembership
	named, err := json.Marshal(LoyaltyLoyaltyMembershipAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LoyaltyLoyaltyMembership) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["name"] {
			return errors.New("name: required property is missing")
		}
		if !v.present["provisional"] {
			return errors.New("provisional: required property is missing")
		}
	}
	for i := range v.Rewards {
		if err := v.Rewards[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Tiers {
		if err := v.Tiers[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// LoyaltyMembershipReward Quantifiable reward type and optional earning forecast for the current transaction.
type LoyaltyMembershipReward struct {
	// A unit of value that customers can accumulate through various commercial activities.
	Currency LoyaltyRewardCurrency `json:"currency"`
	// Preview of rewards to be earned from the current transaction.
	EarningForecast *LoyaltyEarningForecast `json:"earning_forecast,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LoyaltyMembershipReward) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LoyaltyMembershipReward: null is not a valid object")
	}
	type LoyaltyMembershipRewardAlias LoyaltyMembershipReward
	var named LoyaltyMembershipRewardAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LoyaltyMembershipReward(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"currency"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "currency")
	delete(all, "earning_forecast")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LoyaltyMembershipReward) MarshalJSON() ([]byte, error) {
	type LoyaltyMembershipRewardAlias LoyaltyMembershipReward
	named, err := json.Marshal(LoyaltyMembershipRewardAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LoyaltyMembershipReward) Validate() error {
	if v.present != nil {
		if !v.present["currency"] {
			return errors.New("currency: required property is missing")
		}
	}
	if err := v.Currency.Validate(); err != nil {
		return err
	}
	if v.EarningForecast != nil {
		if err := v.EarningForecast.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// LoyaltyMembershipTier Specific achievement rank or status milestone that unlocks escalating value as a member progresses through activity or spend.
type LoyaltyMembershipTier struct {
	// List of benefits associated with this tier.
	Benefits []LoyaltyMembershipTierBenefit `json:"benefits,omitzero"`
	// Unique identifier for the membership tier.
	ID string `json:"id"`
	// The human-readable name of the tier (e.g., 'Platinum').
	Name string `json:"name"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LoyaltyMembershipTier) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LoyaltyMembershipTier: null is not a valid object")
	}
	type LoyaltyMembershipTierAlias LoyaltyMembershipTier
	var named LoyaltyMembershipTierAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LoyaltyMembershipTier(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"id", "name"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "benefits")
	delete(all, "id")
	delete(all, "name")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LoyaltyMembershipTier) MarshalJSON() ([]byte, error) {
	type LoyaltyMembershipTierAlias LoyaltyMembershipTier
	named, err := json.Marshal(LoyaltyMembershipTierAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LoyaltyMembershipTier) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["name"] {
			return errors.New("name: required property is missing")
		}
	}
	for i := range v.Benefits {
		if err := v.Benefits[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// LoyaltyMembershipTierBenefit Benefits associated with a membership tier.
type LoyaltyMembershipTierBenefit struct {
	// A display-ready, human-readable explanation of this benefit (e.g. 'Early access to sales').
	Description string `json:"description"`
	// Unique identifier for the tier benefit.
	ID string `json:"id"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LoyaltyMembershipTierBenefit) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LoyaltyMembershipTierBenefit: null is not a valid object")
	}
	type LoyaltyMembershipTierBenefitAlias LoyaltyMembershipTierBenefit
	var named LoyaltyMembershipTierBenefitAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LoyaltyMembershipTierBenefit(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"description", "id"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "description")
	delete(all, "id")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LoyaltyMembershipTierBenefit) MarshalJSON() ([]byte, error) {
	type LoyaltyMembershipTierBenefitAlias LoyaltyMembershipTierBenefit
	named, err := json.Marshal(LoyaltyMembershipTierBenefitAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LoyaltyMembershipTierBenefit) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
	}
	return nil
}

// LoyaltyRewardAmount Non-negative integer amount denominated in the minor unit of the associated reward currency. The associated reward currency's `decimal_places` defines the minor-to-major ratio and defaults to 0 when omitted.
type LoyaltyRewardAmount int64

// UnmarshalJSON rejects a bare null. encoding/json treats null as a
// no-op for every Go type, so without this the zero value would pass
// every check and a null document would validate as though it were a
// real value.
func (v *LoyaltyRewardAmount) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LoyaltyRewardAmount: null is not a valid integer")
	}
	type LoyaltyRewardAmountAlias LoyaltyRewardAmount
	var alias LoyaltyRewardAmountAlias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = LoyaltyRewardAmount(alias)
	return nil
}

// Validate reports the first constraint violation, or nil.
func (v *LoyaltyRewardAmount) Validate() error {
	if int64(*v) < 0 {
		return errors.New("LoyaltyRewardAmount: below minimum 0")
	}
	return nil
}

// LoyaltyRewardCurrency The currency of the loyalty reward.
type LoyaltyRewardCurrency struct {
	// Business-specific representation of the currency (e.g. 'LST').
	Code string `json:"code"`
	// The position of a digit to the right of a decimal point. Applies to all amount related fields for rewards.
	DecimalPlaces *int64 `json:"decimal_places,omitzero"`
	// Human-readable name of the currency (e.g. 'LoyaltyStars').
	Name string `json:"name"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LoyaltyRewardCurrency) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LoyaltyRewardCurrency: null is not a valid object")
	}
	type LoyaltyRewardCurrencyAlias LoyaltyRewardCurrency
	var named LoyaltyRewardCurrencyAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LoyaltyRewardCurrency(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"code", "name"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "code")
	delete(all, "decimal_places")
	delete(all, "name")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LoyaltyRewardCurrency) MarshalJSON() ([]byte, error) {
	type LoyaltyRewardCurrencyAlias LoyaltyRewardCurrency
	named, err := json.Marshal(LoyaltyRewardCurrencyAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LoyaltyRewardCurrency) Validate() error {
	if v.present != nil {
		if !v.present["code"] {
			return errors.New("code: required property is missing")
		}
		if !v.present["name"] {
			return errors.New("name: required property is missing")
		}
	}
	if v.DecimalPlaces != nil && *v.DecimalPlaces < 0 {
		return errors.New("decimal_places: below minimum 0")
	}
	return nil
}

// LoyaltySearch Catalog Search response extended with Loyalty capability.
type LoyaltySearch struct {
	// Outstanding extension-defined Actions for this catalog search response.
	Actions *types.Actions  `json:"actions,omitzero"`
	Loyalty *LoyaltyLoyalty `json:"loyalty,omitzero"`
	// Errors, warnings, or informational messages about the search results.
	Messages   []types.Message           `json:"messages,omitzero"`
	Pagination *types.PaginationResponse `json:"pagination,omitzero"`
	// Policies (e.g., return/refund terms) that apply to the products in these search results. `applies_to` targets are relative to the response root; when absent or empty, refer to the URLs in `links[]`.
	Policies []types.Policy `json:"policies,omitzero"`
	// Products matching the search criteria.
	Products []shoppingtypes.Product      `json:"products"`
	UCP      ucp.UCPResponseCatalogSchema `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LoyaltySearch) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LoyaltySearch: null is not a valid object")
	}
	type LoyaltySearchAlias LoyaltySearch
	var named LoyaltySearchAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LoyaltySearch(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"products", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "actions")
	delete(all, "loyalty")
	delete(all, "messages")
	delete(all, "pagination")
	delete(all, "policies")
	delete(all, "products")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LoyaltySearch) MarshalJSON() ([]byte, error) {
	type LoyaltySearchAlias LoyaltySearch
	named, err := json.Marshal(LoyaltySearchAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LoyaltySearch) Validate() error {
	if v.present != nil {
		if !v.present["products"] {
			return errors.New("products: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	if v.Actions != nil {
		if err := v.Actions.Validate(); err != nil {
			return err
		}
	}
	if v.Loyalty != nil {
		if err := v.Loyalty.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	if v.Pagination != nil {
		if err := v.Pagination.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Products {
		if err := v.Products[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}
