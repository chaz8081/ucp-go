// Code generated by ucpgen. DO NOT EDIT.
// Source: common/payment_terms.json (spec release/2026-08-25@cd78fb3)

package common

import (
	"encoding/json"
	"errors"
	ucp "github.com/chaz8081/ucp-go"
	"github.com/chaz8081/ucp-go/common/types"
	shoppingtypes "github.com/chaz8081/ucp-go/shopping/types"
)

// PaymentTermsCheckout Checkout extended with selectable payment terms.
type PaymentTermsCheckout struct {
	// Outstanding extension-defined Actions for this checkout.
	Actions     *types.Actions             `json:"actions,omitzero"`
	Attribution *shoppingtypes.Attribution `json:"attribution,omitzero"`
	// Representation of the buyer.
	Buyer   *shoppingtypes.Buyer `json:"buyer,omitzero"`
	Context *types.Context       `json:"context,omitzero"`
	// URL for checkout handoff and session recovery. MUST be provided when status is requires_escalation. See specification for format and availability requirements.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ContinueURL *string `json:"continue_url,omitzero"`
	// ISO 4217 currency code reflecting the merchant's market determination. Derived from address, context, and geo IP—buyers provide signals, merchants determine currency.
	Currency string `json:"currency"`
	// RFC 3339 expiry timestamp. Default TTL is 6 hours from creation if not sent.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ExpiresAt *string `json:"expires_at,omitzero"`
	// Unique identifier of the checkout session.
	ID string `json:"id"`
	// List of line items being checked out.
	LineItems []shoppingtypes.LineItem `json:"line_items"`
	// Links to be displayed by the platform (Privacy Policy, TOS). Mandatory for legal compliance.
	Links []types.Link `json:"links"`
	// List of messages with error and info about the checkout session state.
	Messages []types.Message `json:"messages,omitzero"`
	// Details about an order created for this checkout session.
	Order *shoppingtypes.OrderConfirmation `json:"order,omitzero"`
	// Payment details with available and selected payment terms.
	Payment *PaymentTermsPayment `json:"payment,omitzero"`
	// Policies (e.g., return/refund terms) that apply to the items in this checkout. `applies_to` targets are relative to the response root; when absent or empty, refer to the URLs in `links[]`.
	Policies []types.Policy `json:"policies,omitzero"`
	Signals  *types.Signals `json:"signals,omitzero"`
	// Checkout state indicating the current phase and required processing. See Checkout Status lifecycle documentation for state transition details.
	Status string `json:"status"`
	// Different cart totals.
	Totals types.Totals    `json:"totals"`
	UCP    ucp.UCPMetadata `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *PaymentTermsCheckout) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("PaymentTermsCheckout: null is not a valid object")
	}
	type PaymentTermsCheckoutAlias PaymentTermsCheckout
	var named PaymentTermsCheckoutAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = PaymentTermsCheckout(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 7)
	for _, name := range []string{"currency", "id", "line_items", "links", "status", "totals", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "actions")
	delete(all, "attribution")
	delete(all, "buyer")
	delete(all, "context")
	delete(all, "continue_url")
	delete(all, "currency")
	delete(all, "expires_at")
	delete(all, "id")
	delete(all, "line_items")
	delete(all, "links")
	delete(all, "messages")
	delete(all, "order")
	delete(all, "payment")
	delete(all, "policies")
	delete(all, "signals")
	delete(all, "status")
	delete(all, "totals")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v PaymentTermsCheckout) MarshalJSON() ([]byte, error) {
	type PaymentTermsCheckoutAlias PaymentTermsCheckout
	named, err := json.Marshal(PaymentTermsCheckoutAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *PaymentTermsCheckout) Validate() error {
	if v.present != nil {
		if !v.present["currency"] {
			return errors.New("currency: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["line_items"] {
			return errors.New("line_items: required property is missing")
		}
		if !v.present["links"] {
			return errors.New("links: required property is missing")
		}
		if !v.present["status"] {
			return errors.New("status: required property is missing")
		}
		if !v.present["totals"] {
			return errors.New("totals: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	if v.Status != "incomplete" && v.Status != "requires_escalation" && v.Status != "ready_for_complete" && v.Status != "complete_in_progress" && v.Status != "completed" && v.Status != "canceled" {
		return errors.New("status: not one of the permitted values")
	}
	if v.Actions != nil {
		if err := v.Actions.Validate(); err != nil {
			return err
		}
	}
	if v.Attribution != nil {
		if err := v.Attribution.Validate(); err != nil {
			return err
		}
	}
	if v.Buyer != nil {
		if err := v.Buyer.Validate(); err != nil {
			return err
		}
	}
	if v.Context != nil {
		if err := v.Context.Validate(); err != nil {
			return err
		}
	}
	for i := range v.LineItems {
		if err := v.LineItems[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Links {
		if err := v.Links[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	if v.Order != nil {
		if err := v.Order.Validate(); err != nil {
			return err
		}
	}
	if v.Payment != nil {
		if err := v.Payment.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	if v.Signals != nil {
		if err := v.Signals.Validate(); err != nil {
			return err
		}
	}
	if err := v.Totals.Validate(); err != nil {
		return err
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// PaymentTermsOrder Order extended with the payment term accepted at checkout.
type PaymentTermsOrder struct {
	// Post-order events (refunds, returns, credits, disputes, cancellations, etc.) that exist independently of fulfillment.
	Adjustments []shoppingtypes.Adjustment `json:"adjustments,omitzero"`
	// Snapshot of the attribution associated with the originating checkout. Read-only on the order.
	Attribution *shoppingtypes.Attribution `json:"attribution,omitzero"`
	// Associated checkout ID for reconciliation.
	CheckoutID string `json:"checkout_id"`
	// ISO 4217 currency code. MUST match the currency from the originating checkout session.
	Currency string `json:"currency"`
	// Fulfillment data: buyer expectations and what actually happened.
	Fulfillment PaymentTermsOrderFulfillment `json:"fulfillment"`
	// Unique order identifier.
	ID string `json:"id"`
	// Human-readable label for identifying the order. MUST only be provided by the business.
	Label *string `json:"label,omitzero"`
	// Line items representing what was purchased — can change post-order via edits or exchanges.
	LineItems []shoppingtypes.OrderLineItem `json:"line_items"`
	// Business outcome messages (errors, warnings, informational). Present when the business needs to communicate status or issues to the platform.
	Messages []types.Message `json:"messages,omitzero"`
	// Payment details for the Order, including the accepted payment term.
	Payment *PaymentTermsOrderPayment `json:"payment,omitzero"`
	// Permalink to access the order on merchant site.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	PermalinkURL string `json:"permalink_url"`
	// Snapshot of the policies that applied to the items at checkout, captured on the order as a durable record. `applies_to` targets are relative to the response root.
	Policies []types.Policy `json:"policies,omitzero"`
	// Different totals for the order.
	Totals types.Totals    `json:"totals"`
	UCP    ucp.UCPMetadata `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *PaymentTermsOrder) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("PaymentTermsOrder: null is not a valid object")
	}
	type PaymentTermsOrderAlias PaymentTermsOrder
	var named PaymentTermsOrderAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = PaymentTermsOrder(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 8)
	for _, name := range []string{"checkout_id", "currency", "fulfillment", "id", "line_items", "permalink_url", "totals", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "adjustments")
	delete(all, "attribution")
	delete(all, "checkout_id")
	delete(all, "currency")
	delete(all, "fulfillment")
	delete(all, "id")
	delete(all, "label")
	delete(all, "line_items")
	delete(all, "messages")
	delete(all, "payment")
	delete(all, "permalink_url")
	delete(all, "policies")
	delete(all, "totals")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v PaymentTermsOrder) MarshalJSON() ([]byte, error) {
	type PaymentTermsOrderAlias PaymentTermsOrder
	named, err := json.Marshal(PaymentTermsOrderAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *PaymentTermsOrder) Validate() error {
	if v.present != nil {
		if !v.present["checkout_id"] {
			return errors.New("checkout_id: required property is missing")
		}
		if !v.present["currency"] {
			return errors.New("currency: required property is missing")
		}
		if !v.present["fulfillment"] {
			return errors.New("fulfillment: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["line_items"] {
			return errors.New("line_items: required property is missing")
		}
		if !v.present["permalink_url"] {
			return errors.New("permalink_url: required property is missing")
		}
		if !v.present["totals"] {
			return errors.New("totals: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	for i := range v.Adjustments {
		if err := v.Adjustments[i].Validate(); err != nil {
			return err
		}
	}
	if v.Attribution != nil {
		if err := v.Attribution.Validate(); err != nil {
			return err
		}
	}
	if err := v.Fulfillment.Validate(); err != nil {
		return err
	}
	for i := range v.LineItems {
		if err := v.LineItems[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	if v.Payment != nil {
		if err := v.Payment.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Totals.Validate(); err != nil {
		return err
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// PaymentTermsOrderPayment Order payment details carrying the term the Buyer accepted at checkout.
type PaymentTermsOrderPayment struct {
	// The payment term the Buyer accepted at checkout. Businesses MUST carry it forward so the Order states the amounts owed and when, and MUST ensure its schedule amounts sum to the Order total. The available terms are checkout state and are not projected.
	AcceptedTerm *PaymentTermsPaymentTerm `json:"accepted_term,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *PaymentTermsOrderPayment) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("PaymentTermsOrderPayment: null is not a valid object")
	}
	type PaymentTermsOrderPaymentAlias PaymentTermsOrderPayment
	var named PaymentTermsOrderPaymentAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = PaymentTermsOrderPayment(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "accepted_term")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v PaymentTermsOrderPayment) MarshalJSON() ([]byte, error) {
	type PaymentTermsOrderPaymentAlias PaymentTermsOrderPayment
	named, err := json.Marshal(PaymentTermsOrderPaymentAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *PaymentTermsOrderPayment) Validate() error {
	if v.AcceptedTerm != nil {
		if err := v.AcceptedTerm.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// PaymentTermsPayment Payment object extended with selectable payment terms.
type PaymentTermsPayment struct {
	// ID of the selected payment term. MUST match one `terms[].id` from the latest Checkout response. Present in a response whenever `terms` is, and absent when it is not: the Checkout total is the selected term's total, so a list of terms without a selection would show an amount that matches no stated term. Where the Buyer has made no choice, the Business selects a default. Omitted on create requests because term IDs are checkout-scoped and no terms exist yet, and on complete requests because the term is already agreed by then. Selecting a term is an Update Checkout mutation: the Business response is authoritative for all derived state.
	SelectedTermID *string `json:"selected_term_id,omitzero"`
	// Payment terms the Buyer can choose from. An unselected term's amounts are indicative; the selected term's schedule amounts sum to the checkout total.
	Terms []PaymentTermsPaymentTerm `json:"terms,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *PaymentTermsPayment) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("PaymentTermsPayment: null is not a valid object")
	}
	type PaymentTermsPaymentAlias PaymentTermsPayment
	var named PaymentTermsPaymentAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = PaymentTermsPayment(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "selected_term_id")
	delete(all, "terms")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v PaymentTermsPayment) MarshalJSON() ([]byte, error) {
	type PaymentTermsPaymentAlias PaymentTermsPayment
	named, err := json.Marshal(PaymentTermsPaymentAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *PaymentTermsPayment) Validate() error {
	if v.Terms != nil && len(v.Terms) < 1 {
		return errors.New("terms: has fewer than minItems 1")
	}
	for i := range v.Terms {
		if err := v.Terms[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// PaymentTermsPaymentTerm is generated from common/payment_terms.json.
type PaymentTermsPaymentTerm = types.PaymentTerm

// PaymentTermsOrderFulfillment Fulfillment data: buyer expectations and what actually happened.
type PaymentTermsOrderFulfillment struct {
	// Append-only event log of actual shipments. Each event references line items by ID.
	Events []shoppingtypes.FulfillmentEvent `json:"events,omitzero"`
	// Buyer-facing groups representing when/how items will be delivered. Can be split, merged, or adjusted post-order.
	Expectations []shoppingtypes.Expectation `json:"expectations,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *PaymentTermsOrderFulfillment) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("PaymentTermsOrderFulfillment: null is not a valid object")
	}
	type PaymentTermsOrderFulfillmentAlias PaymentTermsOrderFulfillment
	var named PaymentTermsOrderFulfillmentAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = PaymentTermsOrderFulfillment(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "events")
	delete(all, "expectations")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v PaymentTermsOrderFulfillment) MarshalJSON() ([]byte, error) {
	type PaymentTermsOrderFulfillmentAlias PaymentTermsOrderFulfillment
	named, err := json.Marshal(PaymentTermsOrderFulfillmentAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *PaymentTermsOrderFulfillment) Validate() error {
	for i := range v.Events {
		if err := v.Events[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Expectations {
		if err := v.Expectations[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}
