// Code generated by ucpgen. DO NOT EDIT.
// Source: common/types/binding.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
	"unicode/utf8"
)

// Binding Binds a credential or token to a specific capability resource. Prevents reuse across different resources.
type Binding struct {
	// Opaque identifier of the bound resource within the owning capability, for example a checkout identifier.
	ID string `json:"id"`
	// The capability that owns the bound resource, for example dev.ucp.shopping.checkout. MUST be a capability name declared in the UCP namespace.
	Type ReverseDomainName `json:"type"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *Binding) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("Binding: null is not a valid object")
	}
	type BindingAlias Binding
	var named BindingAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = Binding(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"id", "type"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "id")
	delete(all, "type")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v Binding) MarshalJSON() ([]byte, error) {
	type BindingAlias Binding
	named, err := json.Marshal(BindingAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *Binding) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["type"] {
			return errors.New("type: required property is missing")
		}
	}
	if utf8.RuneCountInString(string(v.ID)) < 1 {
		return errors.New("id: is shorter than minLength 1")
	}
	if err := v.Type.Validate(); err != nil {
		return err
	}
	return nil
}
