// Code generated by ucpgen. DO NOT EDIT.
// Source: common/types/business_split_payments_config.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
)

// BusinessSplitPaymentsConfig Business-level configuration for split payments. Declaring the capability means multiple payment instruments are supported; this config declares which combinations are valid.
type BusinessSplitPaymentsConfig struct {
	// Array of valid instrument combinations. Each combination is an array of instrument groups. A payment is valid if it matches any combination.
	AllowedCombinations [][]InstrumentGroup `json:"allowed_combinations"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *BusinessSplitPaymentsConfig) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("BusinessSplitPaymentsConfig: null is not a valid object")
	}
	type BusinessSplitPaymentsConfigAlias BusinessSplitPaymentsConfig
	var named BusinessSplitPaymentsConfigAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = BusinessSplitPaymentsConfig(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"allowed_combinations"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "allowed_combinations")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v BusinessSplitPaymentsConfig) MarshalJSON() ([]byte, error) {
	type BusinessSplitPaymentsConfigAlias BusinessSplitPaymentsConfig
	named, err := json.Marshal(BusinessSplitPaymentsConfigAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *BusinessSplitPaymentsConfig) Validate() error {
	if v.present != nil {
		if !v.present["allowed_combinations"] {
			return errors.New("allowed_combinations: required property is missing")
		}
	}
	if len(v.AllowedCombinations) < 1 {
		return errors.New("allowed_combinations: has fewer than minItems 1")
	}
	for _, k := range v.AllowedCombinations {
		if len(k) < 1 {
			return errors.New("allowed_combinations item: has fewer than minItems 1")
		}
	}
	for i := range v.AllowedCombinations {
		for i1 := range v.AllowedCombinations[i] {
			if err := v.AllowedCombinations[i][i1].Validate(); err != nil {
				return err
			}
		}
	}
	return nil
}
