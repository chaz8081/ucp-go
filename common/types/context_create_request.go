// Code generated by ucpgen. DO NOT EDIT.
// Source: common/types/context_create_request.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
)

// ContextCreateRequest Provisional buyer signals for relevance and localization—not authoritative data. Businesses SHOULD use these values when verified inputs (e.g., shipping address) are absent, and MAY ignore or down-rank them if inconsistent with higher-confidence signals (authenticated account, risk detection) or regulatory constraints (export controls). Eligibility and policy enforcement MUST occur at checkout time using binding transaction data. Context SHOULD be non-identifying and can be disclosed progressively—coarse signals early, finer resolution as the session progresses. Higher-resolution data (shipping address, billing address) supersedes context.
type ContextCreateRequest struct {
	// The country, as a 2-letter ISO 3166-1 alpha-2 code (e.g. "US"). A 3-letter alpha-3 code or full country name MAY also be used.
	AddressCountry *string `json:"address_country,omitzero"`
	// The first-level administrative region within the country (e.g. a state or province such as California).
	AddressRegion *string `json:"address_region,omitzero"`
	// Preferred currency (ISO 4217, e.g., 'EUR', 'USD'). Businesses determine presentment currency from context and authoritative signals; this hint MAY inform selection in multi-currency markets. Also serves as the denomination for price filter values — platforms SHOULD include this field when sending price filters. Response prices include explicit currency confirming the resolution.
	Currency *string `json:"currency,omitzero"`
	// Buyer claims about eligible benefits such as loyalty membership, payment instrument perks, and similar. Recognized claims MAY inform the Business response (e.g., member-only product availability, adjusted pricing in catalog, provisional discounts at cart or checkout). Businesses MUST ignore unrecognized values without error. Values MUST use reverse-domain naming (e.g., 'com.example.loyalty_gold', 'org.school.student') and MUST be non-identifying.
	Eligibility []ReverseDomainNameCreateRequest `json:"eligibility,omitzero"`
	// Background context describing buyer's intent (e.g., 'looking for a gift under $50', 'need something durable for outdoor use'). Informs relevance, recommendations, and personalization.
	Intent *string `json:"intent,omitzero"`
	// Preferred language for content. Use IETF BCP 47 language tags (e.g., 'en', 'fr-CA', 'zh-Hans'). For REST, equivalent to Accept-Language header—platforms SHOULD fall back to Accept-Language when this field is absent; when provided, overrides Accept-Language. Businesses MAY return content in a different language if unavailable.
	Language *string `json:"language,omitzero"`
	// Stable, opaque identifier for a Location in the Business's namespace. This provisional, non-binding hint is distinct from the Buyer's locality. The operation specification or an active capability/extension defines its effects. A common example in retail shopping is the default home store ID selected and saved by the user when purchasing groceries.
	Location *string `json:"location,omitzero"`
	// Buyer-preferred payment handlers in priority order (most preferred first). Each entry names a handler advertised in the Business profile's `ucp.payment_handlers`, optionally narrowed to preferred instrument types. The Business SHOULD use it to preselect or prioritize the handler (and type, when given) and MAY ignore unavailable or ineligible entries; unrecognized values MUST be ignored without error.
	Payment []ContextCreateRequestPaymentItem `json:"payment,omitzero"`
	// The postal code (e.g. "94043").
	PostalCode *string `json:"postal_code,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *ContextCreateRequest) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("ContextCreateRequest: null is not a valid object")
	}
	type ContextCreateRequestAlias ContextCreateRequest
	var named ContextCreateRequestAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = ContextCreateRequest(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "address_country")
	delete(all, "address_region")
	delete(all, "currency")
	delete(all, "eligibility")
	delete(all, "intent")
	delete(all, "language")
	delete(all, "location")
	delete(all, "payment")
	delete(all, "postal_code")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v ContextCreateRequest) MarshalJSON() ([]byte, error) {
	type ContextCreateRequestAlias ContextCreateRequest
	named, err := json.Marshal(ContextCreateRequestAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *ContextCreateRequest) Validate() error {
	if v.Eligibility != nil {
		{
			k2 := make(map[string]bool, len(v.Eligibility))
			for _, k := range v.Eligibility {
				k3, err := json.Marshal(k)
				if err != nil {
					return err
				}
				if k2[string(k3)] {
					return errors.New("eligibility: contains duplicate items")
				}
				k2[string(k3)] = true
			}
		}
	}
	for i := range v.Eligibility {
		if err := v.Eligibility[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Payment {
		if err := v.Payment[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// ContextCreateRequestPaymentItem is generated from common/types/context_create_request.json.
type ContextCreateRequestPaymentItem struct {
	// Handler registry key advertised in the Business profile's `ucp.payment_handlers`.
	Handler ReverseDomainNameCreateRequest `json:"handler"`
	// Optional preferred instrument types for this handler, in priority order, aligned with the handler's advertised `payment_instrument.type` values (for example `card` or `bank`). Unrecognized values MUST be ignored.
	Types []string `json:"types,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *ContextCreateRequestPaymentItem) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("ContextCreateRequestPaymentItem: null is not a valid object")
	}
	type ContextCreateRequestPaymentItemAlias ContextCreateRequestPaymentItem
	var named ContextCreateRequestPaymentItemAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = ContextCreateRequestPaymentItem(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"handler"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "handler")
	delete(all, "types")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v ContextCreateRequestPaymentItem) MarshalJSON() ([]byte, error) {
	type ContextCreateRequestPaymentItemAlias ContextCreateRequestPaymentItem
	named, err := json.Marshal(ContextCreateRequestPaymentItemAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *ContextCreateRequestPaymentItem) Validate() error {
	if v.present != nil {
		if !v.present["handler"] {
			return errors.New("handler: required property is missing")
		}
	}
	if err := v.Handler.Validate(); err != nil {
		return err
	}
	return nil
}
