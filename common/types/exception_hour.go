// Code generated by ucpgen. DO NOT EDIT.
// Source: common/types/exception_hour.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
	"regexp"
	"sync"
)

// ExceptionHour A date-specific operating interval or full closure. Its `valid_from`, `valid_through`, `opens`, and `closes` are local civil values interpreted in the containing Location's `timezone`. Date bounds are inclusive.
//
// Not enforced yet on the object itself: dependentRequired.
type ExceptionHour struct {
	// Closing time in 24-hour HH:MM format.
	Closes *string `json:"closes,omitzero"`
	// Opening time in 24-hour HH:MM format.
	Opens *string `json:"opens,omitzero"`
	// A short human-readable heading naming the exception (for example, 'Thanksgiving'). Presentation metadata that does not affect schedule evaluation.
	Title *string `json:"title,omitzero"`
	// The first local civil date to which this exception applies, interpreted in the containing Location's `timezone`.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ValidFrom string `json:"valid_from"`
	// The last local civil date to which this exception applies, interpreted in the containing Location's `timezone`.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ValidThrough string `json:"valid_through"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *ExceptionHour) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("ExceptionHour: null is not a valid object")
	}
	type ExceptionHourAlias ExceptionHour
	var named ExceptionHourAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = ExceptionHour(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 4)
	for _, name := range []string{"closes", "opens", "valid_from", "valid_through"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "closes")
	delete(all, "opens")
	delete(all, "title")
	delete(all, "valid_from")
	delete(all, "valid_through")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v ExceptionHour) MarshalJSON() ([]byte, error) {
	type ExceptionHourAlias ExceptionHour
	named, err := json.Marshal(ExceptionHourAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

var pattern_ExceptionHour_Closes = sync.OnceValue(func() *regexp.Regexp { return regexp.MustCompile("^([01][0-9]|2[0-3]):[0-5][0-9]$") })

var pattern_ExceptionHour_Opens = sync.OnceValue(func() *regexp.Regexp { return regexp.MustCompile("^([01][0-9]|2[0-3]):[0-5][0-9]$") })

// Validate reports the first constraint violation, or nil.
func (v *ExceptionHour) Validate() error {
	if v.present != nil {
		if !v.present["valid_from"] {
			return errors.New("valid_from: required property is missing")
		}
		if !v.present["valid_through"] {
			return errors.New("valid_through: required property is missing")
		}
	}
	if v.present != nil {
		if v.present["closes"] && !v.present["opens"] {
			return errors.New("opens: required when closes is present")
		}
		if v.present["opens"] && !v.present["closes"] {
			return errors.New("closes: required when opens is present")
		}
	}
	if v.Closes != nil && !pattern_ExceptionHour_Closes().MatchString(string(*v.Closes)) {
		return errors.New("closes: does not match pattern")
	}
	if v.Opens != nil && !pattern_ExceptionHour_Opens().MatchString(string(*v.Opens)) {
		return errors.New("opens: does not match pattern")
	}
	return nil
}
