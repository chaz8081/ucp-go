// Code generated by ucpgen. DO NOT EDIT.
// Source: common/types/location.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
	"unicode/utf8"
)

// Location The full, rich representation of a physical business location. Builds on the Location Summary schema with discovery-centric details such as geographic coordinates, operating hours, timezone, and amenities.
//
// Not enforced yet on the object itself: if, then.
type Location struct {
	// Physical address of the location.
	Address *PostalAddress `json:"address,omitzero"`
	// Static features, services, or capabilities of the Location, keyed by reverse-domain amenity identifier. Each value provides a buyer-facing description; the key alone defines amenity identity and filter matching.
	//
	// Not enforced yet: propertyNames.
	Amenities map[string]LocationAmenity `json:"amenities,omitzero"`
	// Date-specific operating-hour exceptions, including full closures, whose date and time values use this Location's canonical local civil-time frame.
	ExceptionHours []ExceptionHour `json:"exception_hours,omitzero"`
	// Geographic coordinates for the location.
	Geo *Geo `json:"geo,omitzero"`
	// Regular weekly operating hours whose day and time values use this Location's canonical local civil-time frame. Multiple entries for the same day support split shifts. An omitted day has no regular interval beginning that day; an interval beginning on the preceding day can carry into it. Omission of the entire `hours` property means the regular schedule is unknown.
	Hours []DailyHour `json:"hours,omitzero"`
	// Stable, opaque, Business-scoped Location identifier.
	ID string `json:"id"`
	// Buyer-facing, Business-owned display name.
	Name string `json:"name"`
	// The Business-owned IANA Time Zone Database identifier (e.g., 'America/New_York') defining this Location's canonical local civil-time frame for all returned schedule day, time, and date fields. The Business does not vary this canonical framing by the requesting Platform's or Buyer's timezone. Required when hours or exception_hours is present.
	Timezone *string `json:"timezone,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *Location) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("Location: null is not a valid object")
	}
	type LocationAlias Location
	var named LocationAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = Location(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"id", "name"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "address")
	delete(all, "amenities")
	delete(all, "exception_hours")
	delete(all, "geo")
	delete(all, "hours")
	delete(all, "id")
	delete(all, "name")
	delete(all, "timezone")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v Location) MarshalJSON() ([]byte, error) {
	type LocationAlias Location
	named, err := json.Marshal(LocationAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *Location) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["name"] {
			return errors.New("name: required property is missing")
		}
	}
	if v.Address != nil {
		if err := v.Address.Validate(); err != nil {
			return err
		}
	}
	for _, m := range v.Amenities {
		if err := m.Validate(); err != nil {
			return err
		}
	}
	for i := range v.ExceptionHours {
		if err := v.ExceptionHours[i].Validate(); err != nil {
			return err
		}
	}
	if v.Geo != nil {
		if err := v.Geo.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Hours {
		if err := v.Hours[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// LocationAmenity Buyer-facing presentation metadata for one amenity identifier. The containing map key, not this metadata, defines amenity identity and filter matching.
type LocationAmenity struct {
	// Short, plain-text, buyer-facing label or phrase for the amenity, suitable for direct use in a compact list (e.g., 'Curbside pickup'). The Business SHOULD localize it for the request when possible. This content does not participate in amenity identity or filter matching.
	Description string `json:"description"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LocationAmenity) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LocationAmenity: null is not a valid object")
	}
	type LocationAmenityAlias LocationAmenity
	var named LocationAmenityAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LocationAmenity(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"description"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "description")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LocationAmenity) MarshalJSON() ([]byte, error) {
	type LocationAmenityAlias LocationAmenity
	named, err := json.Marshal(LocationAmenityAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LocationAmenity) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
	}
	if utf8.RuneCountInString(string(v.Description)) < 1 {
		return errors.New("description: is shorter than minLength 1")
	}
	return nil
}
