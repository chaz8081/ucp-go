// Code generated by ucpgen. DO NOT EDIT.
// Source: common/types/location_create_request.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
	"unicode/utf8"
)

// LocationCreateRequest The full, rich representation of a physical business location. Builds on the Location Summary schema with discovery-centric details such as geographic coordinates, operating hours, timezone, and amenities.
//
// Not enforced yet on the object itself: if, then.
type LocationCreateRequest struct {
	// Stable, opaque, Business-scoped Location identifier.
	ID string `json:"id"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LocationCreateRequest) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LocationCreateRequest: null is not a valid object")
	}
	type LocationCreateRequestAlias LocationCreateRequest
	var named LocationCreateRequestAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LocationCreateRequest(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"id"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "id")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LocationCreateRequest) MarshalJSON() ([]byte, error) {
	type LocationCreateRequestAlias LocationCreateRequest
	named, err := json.Marshal(LocationCreateRequestAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LocationCreateRequest) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
	}
	return nil
}

// LocationCreateRequestAmenity Buyer-facing presentation metadata for one amenity identifier. The containing map key, not this metadata, defines amenity identity and filter matching.
type LocationCreateRequestAmenity struct {
	// Short, plain-text, buyer-facing label or phrase for the amenity, suitable for direct use in a compact list (e.g., 'Curbside pickup'). The Business SHOULD localize it for the request when possible. This content does not participate in amenity identity or filter matching.
	Description string `json:"description"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LocationCreateRequestAmenity) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LocationCreateRequestAmenity: null is not a valid object")
	}
	type LocationCreateRequestAmenityAlias LocationCreateRequestAmenity
	var named LocationCreateRequestAmenityAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LocationCreateRequestAmenity(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"description"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "description")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LocationCreateRequestAmenity) MarshalJSON() ([]byte, error) {
	type LocationCreateRequestAmenityAlias LocationCreateRequestAmenity
	named, err := json.Marshal(LocationCreateRequestAmenityAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LocationCreateRequestAmenity) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
	}
	if utf8.RuneCountInString(string(v.Description)) < 1 {
		return errors.New("description: is shorter than minLength 1")
	}
	return nil
}
