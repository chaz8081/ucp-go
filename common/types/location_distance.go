// Code generated by ucpgen. DO NOT EDIT.
// Source: common/types/location_distance.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
)

// LocationDistance An explicit-center inclusive-radius predicate. The Business compares the unrounded shortest WGS 84 ellipsoidal geodesic distance in RFC 7035 distance unit (meters) from `center` to the Location's authoritative `geo`; a value less than or equal to `max` matches. Implementations MAY use any algorithm that produces the WGS 84 inverse-geodesic result at sufficient precision such that the match outcome agrees with this unrounded comparison. No context, signals, IP, or `serves` fallback, radius clamping, tolerance, or operand substitution is permitted.
type LocationDistance struct {
	// Explicit center of the radius. The Platform MUST supply it; the Business MUST NOT derive it from context, signals, an IP address, or `serves`.
	Center Geo `json:"center"`
	// Inclusive maximum distance in RFC 7035 distance unit (meters). A Business unable to honor the supplied value MUST reject the request rather than clamp it or substitute another radius.
	Max float64 `json:"max"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LocationDistance) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LocationDistance: null is not a valid object")
	}
	type LocationDistanceAlias LocationDistance
	var named LocationDistanceAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LocationDistance(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"center", "max"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "center")
	delete(all, "max")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LocationDistance) MarshalJSON() ([]byte, error) {
	type LocationDistanceAlias LocationDistance
	named, err := json.Marshal(LocationDistanceAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LocationDistance) Validate() error {
	if v.present != nil {
		if !v.present["center"] {
			return errors.New("center: required property is missing")
		}
		if !v.present["max"] {
			return errors.New("max: required property is missing")
		}
	}
	if v.Max < 0 {
		return errors.New("max: below minimum 0")
	}
	if err := v.Center.Validate(); err != nil {
		return err
	}
	return nil
}
