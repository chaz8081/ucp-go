// Code generated by ucpgen. DO NOT EDIT.
// Source: common/types/location_serves.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
)

// LocationServes A one-entry map whose key names the authoritative service-target representation. The Platform MUST supply exactly one target form. A Business that cannot evaluate a well-formed target, or receives an extension form that was not negotiated, MUST reject the request rather than ignore it, fall back, or broaden results. This dictionary-like representation map cannot host an ambient `ucp` member.
//
// Not enforced yet on the object itself: propertyNames.
type LocationServes struct {
	// Coarse locality of the service target.
	Address *LocationServesAddress `json:"address,omitzero"`
	// WGS 84 coordinates of the service target.
	Point *Geo `json:"point,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LocationServes) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LocationServes: null is not a valid object")
	}
	type LocationServesAlias LocationServes
	var named LocationServesAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LocationServes(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "address")
	delete(all, "point")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LocationServes) MarshalJSON() ([]byte, error) {
	type LocationServesAlias LocationServes
	named, err := json.Marshal(LocationServesAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LocationServes) Validate() error {
	{
		n := 0
		if v.Address != nil {
			n++
		}
		if v.Point != nil {
			n++
		}
		n += len(v.Extra)
		if n < 1 {
			return errors.New("LocationServes: has fewer than minProperties 1")
		}
	}
	{
		n := 0
		if v.Address != nil {
			n++
		}
		if v.Point != nil {
			n++
		}
		n += len(v.Extra)
		if n > 1 {
			return errors.New("LocationServes: has more than maxProperties 1")
		}
	}
	if v.Address != nil {
		if err := v.Address.Validate(); err != nil {
			return err
		}
	}
	if v.Point != nil {
		if err := v.Point.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// LocationServesAddress Coarse locality of the service target.
//
// The schema also declares 3 anyOf variants that narrow or replace
// these fields; they are not modeled as distinct types yet (phase 4),
// so this type reflects only the shared base.
type LocationServesAddress struct {
	// The country, as a 2-letter ISO 3166-1 alpha-2 code (e.g. "US"). A 3-letter alpha-3 code or full country name MAY also be used.
	AddressCountry *string `json:"address_country,omitzero"`
	// The first-level administrative region within the country (e.g. a state or province such as California).
	AddressRegion *string `json:"address_region,omitzero"`
	// The postal code (e.g. "94043").
	PostalCode *string `json:"postal_code,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *LocationServesAddress) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("LocationServesAddress: null is not a valid object")
	}
	type LocationServesAddressAlias LocationServesAddress
	var named LocationServesAddressAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = LocationServesAddress(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "address_country")
	delete(all, "address_region")
	delete(all, "postal_code")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v LocationServesAddress) MarshalJSON() ([]byte, error) {
	type LocationServesAddressAlias LocationServesAddress
	named, err := json.Marshal(LocationServesAddressAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *LocationServesAddress) Validate() error {
	return nil
}
