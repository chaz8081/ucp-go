// Code generated by ucpgen. DO NOT EDIT.
// Source: common/types/network_token_credential.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
)

// NetworkTokenCredential A card-network token credential verified with a transaction cryptogram. The `number` field carries the network token or wallet-provisioned token rather than the underlying FPAN.
type NetworkTokenCredential struct {
	// Transaction cryptogram or dynamic CVC (dCVV), in the long or short form expected by the card network or processor.
	Cryptogram string `json:"cryptogram"`
	// Electronic Commerce Indicator / Security Level Indicator associated with the transaction.
	EciValue *string `json:"eci_value,omitzero"`
	// The month of the token's expiration date (1-12).
	ExpiryMonth *int64 `json:"expiry_month,omitzero"`
	// The year of the token's expiration date.
	ExpiryYear *int64 `json:"expiry_year,omitzero"`
	// Cardholder name.
	Name *string `json:"name,omitzero"`
	// Network token or wallet-provisioned token replacing the underlying FPAN.
	Number string `json:"number"`
	// Payment network token requestor identifier, when required by the processor or network-token program.
	TokenRequestorID *string `json:"token_requestor_id,omitzero"`
	// The credential type identifier for network token credentials.
	Type string `json:"type"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *NetworkTokenCredential) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("NetworkTokenCredential: null is not a valid object")
	}
	type NetworkTokenCredentialAlias NetworkTokenCredential
	var named NetworkTokenCredentialAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = NetworkTokenCredential(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 3)
	for _, name := range []string{"cryptogram", "number", "type"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "cryptogram")
	delete(all, "eci_value")
	delete(all, "expiry_month")
	delete(all, "expiry_year")
	delete(all, "name")
	delete(all, "number")
	delete(all, "token_requestor_id")
	delete(all, "type")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v NetworkTokenCredential) MarshalJSON() ([]byte, error) {
	type NetworkTokenCredentialAlias NetworkTokenCredential
	named, err := json.Marshal(NetworkTokenCredentialAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *NetworkTokenCredential) Validate() error {
	if v.present != nil {
		if !v.present["cryptogram"] {
			return errors.New("cryptogram: required property is missing")
		}
		if !v.present["number"] {
			return errors.New("number: required property is missing")
		}
		if !v.present["type"] {
			return errors.New("type: required property is missing")
		}
	}
	if v.Type != "network_token" {
		return errors.New("type: must be \"network_token\"")
	}
	return nil
}
