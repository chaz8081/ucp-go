{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "description": "Common base for a fulfillment option: an addressable, renderable choice (e.g. Standard, Express). Catalog uses this base directly; checkout composes it with cost and timing.",
  "properties": {
    "description": {
      "$ref": "../../common/types/description.json",
      "description": "Supplementary context for the title (e.g. 'Arrives in 4 business days', 'Arrives Dec 12-15 via FedEx'). Directly renderable; MUST NOT repeat the title.",
      "ucp_request": "omit"
    },
    "id": {
      "description": "Unique identifier for this fulfillment option.",
      "type": "string",
      "ucp_request": "omit"
    },
    "title": {
      "description": "Short label that distinguishes this option from its siblings (e.g. 'Standard', 'Express Shipping', 'Curbside Pickup').",
      "type": "string",
      "ucp_request": "omit"
    }
  },
  "required": [
    "id",
    "title"
  ],
  "title": "Fulfillment Option Base",
  "type": "object"
}
