{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A purchasable variant of a product with specific option selections.",
  "properties": {
    "availability": {
      "$ref": "availability.json",
      "description": "Variant availability for purchase."
    },
    "barcodes": {
      "description": "Industry-standard product identifiers for cross-reference and correlation.",
      "items": {
        "properties": {
          "type": {
            "description": "Barcode standard. Well-known values: UPC, EAN, ISBN, GTIN, JAN.",
            "type": "string"
          },
          "value": {
            "description": "Barcode value.",
            "type": "string"
          }
        },
        "required": [
          "type",
          "value"
        ],
        "type": "object"
      },
      "type": "array"
    },
    "categories": {
      "description": "Variant categories with optional taxonomy identifiers.",
      "items": {
        "$ref": "category.json"
      },
      "type": "array"
    },
    "description": {
      "$ref": "../../common/types/description.json",
      "description": "Variant description in one or more formats."
    },
    "handle": {
      "description": "URL-safe variant handle/slug.",
      "type": "string"
    },
    "id": {
      "description": "Global ID (GID) uniquely identifying this variant. Used as item.id in checkout.",
      "type": "string"
    },
    "list_price": {
      "$ref": "../../common/types/price.json",
      "description": "List price before discounts (for strikethrough display)."
    },
    "media": {
      "description": "Variant media (images, videos, 3D models). First item is the featured media for listings.",
      "items": {
        "$ref": "../../common/types/media.json"
      },
      "type": "array"
    },
    "metadata": {
      "description": "Business-defined custom data extending the standard variant model.",
      "type": "object"
    },
    "options": {
      "description": "Option values that define this variant (e.g., Color: Blue, Size: Large).",
      "items": {
        "$ref": "selected_option.json"
      },
      "type": "array"
    },
    "price": {
      "$ref": "../../common/types/price.json",
      "description": "Current selling price. Price is the amount per one whole `quantity_unit.unit` (for example, per lb or per hour); when `quantity_unit` is absent, it is per `each`. Line total is `price × quantity × 10^-scale`, computed and rounded once by the Business; `totals` remain authoritative."
    },
    "quantity_unit": {
      "$ref": "../../common/types/quantity_unit.json",
      "description": "Sale basis this variant's `quantity` is denominated in. The default sale basis is `each`, whose machine identity is (`C62`, 0); `C62` is the UN/CEFACT Rec20 code for one/each. An absent catalog descriptor encodes that default. An `increment` advertises the ordering granularity in steps (for example, `scale` 2 with `increment` 25 sells in 0.25-unit multiples)."
    },
    "rating": {
      "$ref": "rating.json",
      "description": "Variant rating."
    },
    "seller": {
      "description": "Optional seller context for this variant.",
      "properties": {
        "links": {
          "description": "Seller policy and information links.",
          "items": {
            "$ref": "../../common/types/link.json"
          },
          "type": "array"
        },
        "name": {
          "description": "Seller display name.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "sku": {
      "description": "Business-assigned identifier for inventory and fulfillment.",
      "type": "string"
    },
    "tags": {
      "description": "Variant tags for categorization and search.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "title": {
      "description": "Variant display title (e.g., 'Blue / Large').",
      "type": "string"
    },
    "unit_price": {
      "$ref": "unit_price.json",
      "description": "Price per standard unit of measurement, for shelf-style comparison display. MAY be omitted when unit pricing does not apply."
    },
    "url": {
      "description": "Canonical variant page URL.",
      "format": "uri",
      "type": "string"
    }
  },
  "required": [
    "description",
    "id",
    "price",
    "title"
  ],
  "title": "Variant",
  "type": "object"
}
