// Code generated by ucpgen. DO NOT EDIT.
// Source: payment_handler.json (spec release/2026-04-08@a2d8bf0)

package ucp

import (
	"encoding/json"
	"errors"
	"github.com/chaz8081/ucp-go/shopping/types"
)

// PaymentHandlerBase is generated from payment_handler.json.
type PaymentHandlerBase struct {
	// Instrument types this handler supports, with optional constraints. When absent, every instrument should be considered available.
	AvailableInstruments []types.AvailablePaymentInstrument `json:"available_instruments,omitzero"`
	// Entity-specific configuration. Structure defined by each entity's schema.
	Config map[string]any `json:"config,omitzero"`
	// Unique identifier for this entity instance. Used to disambiguate when multiple instances exist.
	ID string `json:"id"`
	// URL to JSON Schema defining this entity's structure and payloads.
	//
	// Not enforced yet (phase 4): format.
	Schema *string `json:"schema,omitzero"`
	// URL to human-readable specification document.
	//
	// Not enforced yet (phase 4): format.
	Spec *string `json:"spec,omitzero"`
	// Entity version in YYYY-MM-DD format.
	Version UCPVersion `json:"version"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *PaymentHandlerBase) UnmarshalJSON(data []byte) error {
	type PaymentHandlerBaseAlias PaymentHandlerBase
	var named PaymentHandlerBaseAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = PaymentHandlerBase(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"id", "version"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "available_instruments")
	delete(all, "config")
	delete(all, "id")
	delete(all, "schema")
	delete(all, "spec")
	delete(all, "version")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v PaymentHandlerBase) MarshalJSON() ([]byte, error) {
	type PaymentHandlerBaseAlias PaymentHandlerBase
	named, err := json.Marshal(PaymentHandlerBaseAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *PaymentHandlerBase) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["version"] {
			return errors.New("version: required property is missing")
		}
	}
	if v.AvailableInstruments != nil && len(v.AvailableInstruments) < 1 {
		return errors.New("available_instruments: has fewer than minItems 1")
	}
	for i := range v.AvailableInstruments {
		if err := v.AvailableInstruments[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Version.Validate(); err != nil {
		return err
	}
	return nil
}

// PaymentHandlerBusinessSchema Business declaration for discovery profiles. May include partial config state required for discovery.
type PaymentHandlerBusinessSchema struct {
	// Instrument types this handler supports, with optional constraints. When absent, every instrument should be considered available.
	AvailableInstruments []types.AvailablePaymentInstrument `json:"available_instruments,omitzero"`
	// Entity-specific configuration. Structure defined by each entity's schema.
	Config map[string]any `json:"config,omitzero"`
	// Unique identifier for this entity instance. Used to disambiguate when multiple instances exist.
	ID string `json:"id"`
	// URL to JSON Schema defining this entity's structure and payloads.
	//
	// Not enforced yet (phase 4): format.
	Schema *string `json:"schema,omitzero"`
	// URL to human-readable specification document.
	//
	// Not enforced yet (phase 4): format.
	Spec *string `json:"spec,omitzero"`
	// Entity version in YYYY-MM-DD format.
	Version UCPVersion `json:"version"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *PaymentHandlerBusinessSchema) UnmarshalJSON(data []byte) error {
	type PaymentHandlerBusinessSchemaAlias PaymentHandlerBusinessSchema
	var named PaymentHandlerBusinessSchemaAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = PaymentHandlerBusinessSchema(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"id", "version"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "available_instruments")
	delete(all, "config")
	delete(all, "id")
	delete(all, "schema")
	delete(all, "spec")
	delete(all, "version")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v PaymentHandlerBusinessSchema) MarshalJSON() ([]byte, error) {
	type PaymentHandlerBusinessSchemaAlias PaymentHandlerBusinessSchema
	named, err := json.Marshal(PaymentHandlerBusinessSchemaAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *PaymentHandlerBusinessSchema) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["version"] {
			return errors.New("version: required property is missing")
		}
	}
	if v.AvailableInstruments != nil && len(v.AvailableInstruments) < 1 {
		return errors.New("available_instruments: has fewer than minItems 1")
	}
	for i := range v.AvailableInstruments {
		if err := v.AvailableInstruments[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Version.Validate(); err != nil {
		return err
	}
	return nil
}

// PaymentHandlerPlatformSchema Platform declaration for discovery profiles. May include partial config state required for discovery.
type PaymentHandlerPlatformSchema struct {
	// Instrument types this handler supports, with optional constraints. When absent, every instrument should be considered available.
	AvailableInstruments []types.AvailablePaymentInstrument `json:"available_instruments,omitzero"`
	// Entity-specific configuration. Structure defined by each entity's schema.
	Config map[string]any `json:"config,omitzero"`
	// Unique identifier for this entity instance. Used to disambiguate when multiple instances exist.
	ID string `json:"id"`
	// URL to JSON Schema defining this entity's structure and payloads.
	//
	// Not enforced yet (phase 4): format.
	Schema string `json:"schema"`
	// URL to human-readable specification document.
	//
	// Not enforced yet (phase 4): format.
	Spec string `json:"spec"`
	// Entity version in YYYY-MM-DD format.
	Version UCPVersion `json:"version"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *PaymentHandlerPlatformSchema) UnmarshalJSON(data []byte) error {
	type PaymentHandlerPlatformSchemaAlias PaymentHandlerPlatformSchema
	var named PaymentHandlerPlatformSchemaAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = PaymentHandlerPlatformSchema(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 4)
	for _, name := range []string{"id", "schema", "spec", "version"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "available_instruments")
	delete(all, "config")
	delete(all, "id")
	delete(all, "schema")
	delete(all, "spec")
	delete(all, "version")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v PaymentHandlerPlatformSchema) MarshalJSON() ([]byte, error) {
	type PaymentHandlerPlatformSchemaAlias PaymentHandlerPlatformSchema
	named, err := json.Marshal(PaymentHandlerPlatformSchemaAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *PaymentHandlerPlatformSchema) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["schema"] {
			return errors.New("schema: required property is missing")
		}
		if !v.present["spec"] {
			return errors.New("spec: required property is missing")
		}
		if !v.present["version"] {
			return errors.New("version: required property is missing")
		}
	}
	if v.AvailableInstruments != nil && len(v.AvailableInstruments) < 1 {
		return errors.New("available_instruments: has fewer than minItems 1")
	}
	for i := range v.AvailableInstruments {
		if err := v.AvailableInstruments[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Version.Validate(); err != nil {
		return err
	}
	return nil
}

// PaymentHandlerResponseSchema Handler reference in responses. May include full config state for runtime usage of the handler.
type PaymentHandlerResponseSchema struct {
	// Instrument types this handler supports, with optional constraints. When absent, every instrument should be considered available.
	AvailableInstruments []types.AvailablePaymentInstrument `json:"available_instruments,omitzero"`
	// Entity-specific configuration. Structure defined by each entity's schema.
	Config map[string]any `json:"config,omitzero"`
	// Unique identifier for this entity instance. Used to disambiguate when multiple instances exist.
	ID string `json:"id"`
	// URL to JSON Schema defining this entity's structure and payloads.
	//
	// Not enforced yet (phase 4): format.
	Schema *string `json:"schema,omitzero"`
	// URL to human-readable specification document.
	//
	// Not enforced yet (phase 4): format.
	Spec *string `json:"spec,omitzero"`
	// Entity version in YYYY-MM-DD format.
	Version UCPVersion `json:"version"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *PaymentHandlerResponseSchema) UnmarshalJSON(data []byte) error {
	type PaymentHandlerResponseSchemaAlias PaymentHandlerResponseSchema
	var named PaymentHandlerResponseSchemaAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = PaymentHandlerResponseSchema(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"id", "version"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "available_instruments")
	delete(all, "config")
	delete(all, "id")
	delete(all, "schema")
	delete(all, "spec")
	delete(all, "version")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v PaymentHandlerResponseSchema) MarshalJSON() ([]byte, error) {
	type PaymentHandlerResponseSchemaAlias PaymentHandlerResponseSchema
	named, err := json.Marshal(PaymentHandlerResponseSchemaAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *PaymentHandlerResponseSchema) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["version"] {
			return errors.New("version: required property is missing")
		}
	}
	if v.AvailableInstruments != nil && len(v.AvailableInstruments) < 1 {
		return errors.New("available_instruments: has fewer than minItems 1")
	}
	for i := range v.AvailableInstruments {
		if err := v.AvailableInstruments[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Version.Validate(); err != nil {
		return err
	}
	return nil
}
