// Code generated by ucpgen. DO NOT EDIT.
// Source: profile.json (spec release/2026-08-25@cd78fb3)

package ucp

import (
	"encoding/json"
	"errors"
)

// UCPProfileDocument Variant-neutral wrapper schema for UCP profile documents. Use the business_schema definition to validate business profiles and the platform_schema definition to validate platform profiles.
type UCPProfileDocument struct {
	// Canonical UCP profile field for publishing signing keys, as a JWK Set per RFC 7517. When a profile publishes signing keys, they MUST appear here; this is where every UCP verifier reads them. Publishing keys[] makes the UCP profile a valid JWK Set that a signer can reuse as its Web Bot Auth key source: a WBA-shape verifier resolving via Signature-Agent type=jwks_uri pointed at this profile reads these keys, and the cimd and directory variants reach them through their own documents. See the Deployment Patterns for WBA Interop section in the overview for hosting patterns.
	Keys []ProfileJWKPublicKey `json:"keys,omitzero"`
	// Protocol metadata, capabilities, services, and payment handlers advertised by this party.
	UCP UCPBase `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *UCPProfileDocument) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("UCPProfileDocument: null is not a valid object")
	}
	type UCPProfileDocumentAlias UCPProfileDocument
	var named UCPProfileDocumentAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = UCPProfileDocument(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "keys")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v UCPProfileDocument) MarshalJSON() ([]byte, error) {
	type UCPProfileDocumentAlias UCPProfileDocument
	named, err := json.Marshal(UCPProfileDocumentAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *UCPProfileDocument) Validate() error {
	if v.present != nil {
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	for i := range v.Keys {
		if err := v.Keys[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// ProfileBase Common wrapper for UCP profile documents.
type ProfileBase struct {
	// Canonical UCP profile field for publishing signing keys, as a JWK Set per RFC 7517. When a profile publishes signing keys, they MUST appear here; this is where every UCP verifier reads them. Publishing keys[] makes the UCP profile a valid JWK Set that a signer can reuse as its Web Bot Auth key source: a WBA-shape verifier resolving via Signature-Agent type=jwks_uri pointed at this profile reads these keys, and the cimd and directory variants reach them through their own documents. See the Deployment Patterns for WBA Interop section in the overview for hosting patterns.
	Keys []ProfileJWKPublicKey `json:"keys,omitzero"`
	// Protocol metadata, capabilities, services, and payment handlers advertised by this party.
	UCP UCPBase `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *ProfileBase) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("ProfileBase: null is not a valid object")
	}
	type ProfileBaseAlias ProfileBase
	var named ProfileBaseAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = ProfileBase(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "keys")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v ProfileBase) MarshalJSON() ([]byte, error) {
	type ProfileBaseAlias ProfileBase
	named, err := json.Marshal(ProfileBaseAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *ProfileBase) Validate() error {
	if v.present != nil {
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	for i := range v.Keys {
		if err := v.Keys[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// ProfileBusinessSchema Profile document hosted by a business at /.well-known/ucp.
type ProfileBusinessSchema struct {
	// Canonical UCP profile field for publishing signing keys, as a JWK Set per RFC 7517. When a profile publishes signing keys, they MUST appear here; this is where every UCP verifier reads them. Publishing keys[] makes the UCP profile a valid JWK Set that a signer can reuse as its Web Bot Auth key source: a WBA-shape verifier resolving via Signature-Agent type=jwks_uri pointed at this profile reads these keys, and the cimd and directory variants reach them through their own documents. See the Deployment Patterns for WBA Interop section in the overview for hosting patterns.
	Keys []ProfileJWKPublicKey `json:"keys,omitzero"`
	UCP  UCPBusinessSchema     `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *ProfileBusinessSchema) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("ProfileBusinessSchema: null is not a valid object")
	}
	type ProfileBusinessSchemaAlias ProfileBusinessSchema
	var named ProfileBusinessSchemaAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = ProfileBusinessSchema(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "keys")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v ProfileBusinessSchema) MarshalJSON() ([]byte, error) {
	type ProfileBusinessSchemaAlias ProfileBusinessSchema
	named, err := json.Marshal(ProfileBusinessSchemaAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *ProfileBusinessSchema) Validate() error {
	if v.present != nil {
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	for i := range v.Keys {
		if err := v.Keys[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// ProfileJWKPublicKey Public JSON Web Key used for HTTP Message Signatures and signed webhook verification. UCP profiles publish public keys only; private key material MUST NOT appear in a profile. Well-known key types: EC (ECDSA P-256, P-384) and OKP (EdDSA Ed25519); OKP keys are RECOMMENDED for signers opting into Web Bot Auth (WBA) interop on HTTP transport. A single profile MAY publish keys of either or both types; consumers select keys by kid. The kty, crv, and alg vocabularies are OPEN: verifiers MUST tolerate key types, curves, and algorithms they do not recognize, selecting keys by kid at verification time. An unsupported key affects only the signature that references it (algorithm_unsupported) and MUST NOT cause whole-profile rejection. Additional public JWK members are permitted; consumers ignore unknown members.
//
// Not enforced yet on the object itself: not.
type ProfileJWKPublicKey struct {
	// JWA algorithm associated with this public key. Optional; verifiers derive the algorithm from crv when alg is omitted. When present for a well-known curve it MUST match: ES256 with P-256, ES384 with P-384, EdDSA with Ed25519.
	Alg *string `json:"alg,omitzero"`
	// Curve name. Well-known values: P-256, P-384 (EC); Ed25519 (OKP). Open vocabulary.
	Crv *string `json:"crv,omitzero"`
	// Key identifier referenced by Signature-Input keyid. For keys used in dual-audience (Web Bot Auth) signatures, the kid MUST be the key's JWK SHA-256 Thumbprint (RFC 7638) so UCP-Agent and Signature-Agent lookups resolve the same key; otherwise the kid MAY be any stable string.
	Kid string `json:"kid"`
	// JWK key type. Well-known values: EC for ECDSA (P-256, P-384); OKP for EdDSA (Ed25519). Open vocabulary; verifiers tolerate unrecognized types and select keys by kid.
	Kty string `json:"kty"`
	// JWK public key use. UCP examples use sig for signatures.
	Use *string `json:"use,omitzero"`
	// Public key value, base64url-encoded. For EC, the x coordinate (RFC 7518 §6.2); for OKP, the public key (RFC 8037 §2).
	X *string `json:"x,omitzero"`
	// EC public key y coordinate, base64url-encoded (RFC 7518 §6.2). Not used by OKP keys.
	Y *string `json:"y,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *ProfileJWKPublicKey) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("ProfileJWKPublicKey: null is not a valid object")
	}
	type ProfileJWKPublicKeyAlias ProfileJWKPublicKey
	var named ProfileJWKPublicKeyAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = ProfileJWKPublicKey(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"kid", "kty"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "alg")
	delete(all, "crv")
	delete(all, "kid")
	delete(all, "kty")
	delete(all, "use")
	delete(all, "x")
	delete(all, "y")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v ProfileJWKPublicKey) MarshalJSON() ([]byte, error) {
	type ProfileJWKPublicKeyAlias ProfileJWKPublicKey
	named, err := json.Marshal(ProfileJWKPublicKeyAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *ProfileJWKPublicKey) Validate() error {
	if v.present != nil {
		if !v.present["kid"] {
			return errors.New("kid: required property is missing")
		}
		if !v.present["kty"] {
			return errors.New("kty: required property is missing")
		}
	}
	if v.Kty == "EC" {
		if v.Crv == nil {
			return errors.New("crv: required property is missing when kty is \"EC\"")
		}
		if v.X == nil {
			return errors.New("x: required property is missing when kty is \"EC\"")
		}
		if v.Y == nil {
			return errors.New("y: required property is missing when kty is \"EC\"")
		}
	}
	if v.Kty == "OKP" {
		if v.Crv == nil {
			return errors.New("crv: required property is missing when kty is \"OKP\"")
		}
		if v.X == nil {
			return errors.New("x: required property is missing when kty is \"OKP\"")
		}
	}
	if v.Crv != nil && *v.Crv == "P-256" {
		if v.Alg != nil && *v.Alg != "ES256" {
			return errors.New("alg: must be \"ES256\"")
		}
	}
	if v.Crv != nil && *v.Crv == "P-384" {
		if v.Alg != nil && *v.Alg != "ES384" {
			return errors.New("alg: must be \"ES384\"")
		}
	}
	if v.Crv != nil && *v.Crv == "Ed25519" {
		if v.Alg != nil && *v.Alg != "EdDSA" {
			return errors.New("alg: must be \"EdDSA\"")
		}
	}
	return nil
}

// ProfilePlatformSchema Profile document hosted by a platform and advertised to businesses via UCP-Agent.
type ProfilePlatformSchema struct {
	// Canonical UCP profile field for publishing signing keys, as a JWK Set per RFC 7517. When a profile publishes signing keys, they MUST appear here; this is where every UCP verifier reads them. Publishing keys[] makes the UCP profile a valid JWK Set that a signer can reuse as its Web Bot Auth key source: a WBA-shape verifier resolving via Signature-Agent type=jwks_uri pointed at this profile reads these keys, and the cimd and directory variants reach them through their own documents. See the Deployment Patterns for WBA Interop section in the overview for hosting patterns.
	Keys []ProfileJWKPublicKey `json:"keys,omitzero"`
	UCP  UCPPlatformSchema     `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *ProfilePlatformSchema) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("ProfilePlatformSchema: null is not a valid object")
	}
	type ProfilePlatformSchemaAlias ProfilePlatformSchema
	var named ProfilePlatformSchemaAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = ProfilePlatformSchema(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "keys")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v ProfilePlatformSchema) MarshalJSON() ([]byte, error) {
	type ProfilePlatformSchemaAlias ProfilePlatformSchema
	named, err := json.Marshal(ProfilePlatformSchemaAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *ProfilePlatformSchema) Validate() error {
	if v.present != nil {
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	for i := range v.Keys {
		if err := v.Keys[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}
