// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/buyer_consent.json (spec release/2026-08-25@cd78fb3)

package shopping

import (
	"encoding/json"
	"errors"
	ucp "github.com/chaz8081/ucp-go"
	"github.com/chaz8081/ucp-go/common/types"
	shoppingtypes "github.com/chaz8081/ucp-go/shopping/types"
	"regexp"
	"sync"
)

// BuyerConsentBuyer Buyer object extended with per-purpose consent.
type BuyerConsentBuyer struct {
	// Per-purpose consent decisions and business-advertised consent options.
	Consent *BuyerConsentConsent `json:"consent,omitzero"`
	// Email of the buyer.
	Email *string `json:"email,omitzero"`
	// First name of the buyer.
	FirstName *string `json:"first_name,omitzero"`
	// Last name of the buyer.
	LastName *string `json:"last_name,omitzero"`
	// E.164 standard.
	PhoneNumber *string `json:"phone_number,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *BuyerConsentBuyer) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("BuyerConsentBuyer: null is not a valid object")
	}
	type BuyerConsentBuyerAlias BuyerConsentBuyer
	var named BuyerConsentBuyerAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = BuyerConsentBuyer(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "consent")
	delete(all, "email")
	delete(all, "first_name")
	delete(all, "last_name")
	delete(all, "phone_number")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v BuyerConsentBuyer) MarshalJSON() ([]byte, error) {
	type BuyerConsentBuyerAlias BuyerConsentBuyer
	named, err := json.Marshal(BuyerConsentBuyerAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *BuyerConsentBuyer) Validate() error {
	if v.Consent != nil {
		if err := v.Consent.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// BuyerConsentCart Cart extended with buyer consent.
type BuyerConsentCart struct {
	// Outstanding extension-defined Actions for this cart.
	Actions     *types.Actions             `json:"actions,omitzero"`
	Attribution *shoppingtypes.Attribution `json:"attribution,omitzero"`
	// Buyer with consent tracking.
	Buyer *BuyerConsentBuyer `json:"buyer,omitzero"`
	// Buyer signals for localization (country, region, postal_code). Merchant uses for pricing, availability, currency. Falls back to geo-IP if omitted.
	Context *types.Context `json:"context,omitzero"`
	// URL for cart handoff and session recovery. Enables sharing and human-in-the-loop flows.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ContinueURL *string `json:"continue_url,omitzero"`
	// ISO 4217 currency code. Determined by merchant based on context or geo-IP.
	Currency string `json:"currency"`
	// Cart expiry timestamp (RFC 3339). Optional.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ExpiresAt *string `json:"expires_at,omitzero"`
	// Unique cart identifier.
	ID string `json:"id"`
	// Cart line items. Same structure as checkout. Full replacement on update.
	LineItems []shoppingtypes.LineItem `json:"line_items"`
	// Optional merchant links (policies, FAQs).
	Links []types.Link `json:"links,omitzero"`
	// Validation messages, warnings, or informational notices.
	Messages []types.Message `json:"messages,omitzero"`
	// Policies (e.g., return/refund terms) that apply to the items in this cart. `applies_to` targets are relative to the response root; when absent or empty, refer to the URLs in `links[]`.
	Policies []types.Policy `json:"policies,omitzero"`
	Signals  *types.Signals `json:"signals,omitzero"`
	// Estimated cost breakdown. May be partial if shipping/tax not yet calculable.
	Totals types.Totals    `json:"totals"`
	UCP    ucp.UCPMetadata `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *BuyerConsentCart) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("BuyerConsentCart: null is not a valid object")
	}
	type BuyerConsentCartAlias BuyerConsentCart
	var named BuyerConsentCartAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = BuyerConsentCart(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 5)
	for _, name := range []string{"currency", "id", "line_items", "totals", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "actions")
	delete(all, "attribution")
	delete(all, "buyer")
	delete(all, "context")
	delete(all, "continue_url")
	delete(all, "currency")
	delete(all, "expires_at")
	delete(all, "id")
	delete(all, "line_items")
	delete(all, "links")
	delete(all, "messages")
	delete(all, "policies")
	delete(all, "signals")
	delete(all, "totals")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v BuyerConsentCart) MarshalJSON() ([]byte, error) {
	type BuyerConsentCartAlias BuyerConsentCart
	named, err := json.Marshal(BuyerConsentCartAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *BuyerConsentCart) Validate() error {
	if v.present != nil {
		if !v.present["currency"] {
			return errors.New("currency: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["line_items"] {
			return errors.New("line_items: required property is missing")
		}
		if !v.present["totals"] {
			return errors.New("totals: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	if v.Actions != nil {
		if err := v.Actions.Validate(); err != nil {
			return err
		}
	}
	if v.Attribution != nil {
		if err := v.Attribution.Validate(); err != nil {
			return err
		}
	}
	if v.Buyer != nil {
		if err := v.Buyer.Validate(); err != nil {
			return err
		}
	}
	if v.Context != nil {
		if err := v.Context.Validate(); err != nil {
			return err
		}
	}
	for i := range v.LineItems {
		if err := v.LineItems[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Links {
		if err := v.Links[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	if v.Signals != nil {
		if err := v.Signals.Validate(); err != nil {
			return err
		}
	}
	if err := v.Totals.Validate(); err != nil {
		return err
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// BuyerConsentCheckout Checkout extended with buyer consent.
type BuyerConsentCheckout struct {
	// Outstanding extension-defined Actions for this checkout.
	Actions     *types.Actions             `json:"actions,omitzero"`
	Attribution *shoppingtypes.Attribution `json:"attribution,omitzero"`
	// Buyer with consent tracking.
	Buyer   *BuyerConsentBuyer `json:"buyer,omitzero"`
	Context *types.Context     `json:"context,omitzero"`
	// URL for checkout handoff and session recovery. MUST be provided when status is requires_escalation. See specification for format and availability requirements.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ContinueURL *string `json:"continue_url,omitzero"`
	// ISO 4217 currency code reflecting the merchant's market determination. Derived from address, context, and geo IP—buyers provide signals, merchants determine currency.
	Currency string `json:"currency"`
	// RFC 3339 expiry timestamp. Default TTL is 6 hours from creation if not sent.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ExpiresAt *string `json:"expires_at,omitzero"`
	// Unique identifier of the checkout session.
	ID string `json:"id"`
	// List of line items being checked out.
	LineItems []shoppingtypes.LineItem `json:"line_items"`
	// Links to be displayed by the platform (Privacy Policy, TOS). Mandatory for legal compliance.
	Links []types.Link `json:"links"`
	// List of messages with error and info about the checkout session state.
	Messages []types.Message `json:"messages,omitzero"`
	// Details about an order created for this checkout session.
	Order   *shoppingtypes.OrderConfirmation `json:"order,omitzero"`
	Payment *types.Payment                   `json:"payment,omitzero"`
	// Policies (e.g., return/refund terms) that apply to the items in this checkout. `applies_to` targets are relative to the response root; when absent or empty, refer to the URLs in `links[]`.
	Policies []types.Policy `json:"policies,omitzero"`
	Signals  *types.Signals `json:"signals,omitzero"`
	// Checkout state indicating the current phase and required processing. See Checkout Status lifecycle documentation for state transition details.
	Status string `json:"status"`
	// Different cart totals.
	Totals types.Totals    `json:"totals"`
	UCP    ucp.UCPMetadata `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *BuyerConsentCheckout) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("BuyerConsentCheckout: null is not a valid object")
	}
	type BuyerConsentCheckoutAlias BuyerConsentCheckout
	var named BuyerConsentCheckoutAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = BuyerConsentCheckout(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 7)
	for _, name := range []string{"currency", "id", "line_items", "links", "status", "totals", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "actions")
	delete(all, "attribution")
	delete(all, "buyer")
	delete(all, "context")
	delete(all, "continue_url")
	delete(all, "currency")
	delete(all, "expires_at")
	delete(all, "id")
	delete(all, "line_items")
	delete(all, "links")
	delete(all, "messages")
	delete(all, "order")
	delete(all, "payment")
	delete(all, "policies")
	delete(all, "signals")
	delete(all, "status")
	delete(all, "totals")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v BuyerConsentCheckout) MarshalJSON() ([]byte, error) {
	type BuyerConsentCheckoutAlias BuyerConsentCheckout
	named, err := json.Marshal(BuyerConsentCheckoutAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *BuyerConsentCheckout) Validate() error {
	if v.present != nil {
		if !v.present["currency"] {
			return errors.New("currency: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["line_items"] {
			return errors.New("line_items: required property is missing")
		}
		if !v.present["links"] {
			return errors.New("links: required property is missing")
		}
		if !v.present["status"] {
			return errors.New("status: required property is missing")
		}
		if !v.present["totals"] {
			return errors.New("totals: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	if v.Status != "incomplete" && v.Status != "requires_escalation" && v.Status != "ready_for_complete" && v.Status != "complete_in_progress" && v.Status != "completed" && v.Status != "canceled" {
		return errors.New("status: not one of the permitted values")
	}
	if v.Actions != nil {
		if err := v.Actions.Validate(); err != nil {
			return err
		}
	}
	if v.Attribution != nil {
		if err := v.Attribution.Validate(); err != nil {
			return err
		}
	}
	if v.Buyer != nil {
		if err := v.Buyer.Validate(); err != nil {
			return err
		}
	}
	if v.Context != nil {
		if err := v.Context.Validate(); err != nil {
			return err
		}
	}
	for i := range v.LineItems {
		if err := v.LineItems[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Links {
		if err := v.Links[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	if v.Order != nil {
		if err := v.Order.Validate(); err != nil {
			return err
		}
	}
	if v.Payment != nil {
		if err := v.Payment.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	if v.Signals != nil {
		if err := v.Signals.Validate(); err != nil {
			return err
		}
	}
	if err := v.Totals.Validate(); err != nil {
		return err
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// BuyerConsentConsent Per-purpose consent. Keys are reverse-DNS purpose identifiers. UCP defines four well-known purposes: `dev.ucp.consent.marketing`, `dev.ucp.consent.analytics`, `dev.ucp.consent.preferences`, `dev.ucp.consent.sale_or_sharing`. Vendors and merchants may define additional purposes under their own reverse-DNS namespace.
type BuyerConsentConsent map[string]BuyerConsentConsentPurpose

// UnmarshalJSON rejects a bare null. encoding/json treats null as a
// no-op for every Go type, so without this the zero value would pass
// every check and a null document would validate as though it were a
// real value.
func (v *BuyerConsentConsent) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("BuyerConsentConsent: null is not a valid object")
	}
	type BuyerConsentConsentAlias BuyerConsentConsent
	var alias BuyerConsentConsentAlias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = BuyerConsentConsent(alias)
	return nil
}

var pattern_BuyerConsentConsent_Key = sync.OnceValue(func() *regexp.Regexp {
	return regexp.MustCompile("^[a-z](?:[a-z0-9-]*[a-z0-9])?(?:\\.[a-z0-9](?:[a-z0-9_-]*[a-z0-9_])?)+$")
})

// Validate reports the first constraint violation, or nil.
func (v *BuyerConsentConsent) Validate() error {
	for k := range *v {
		if !pattern_BuyerConsentConsent_Key().MatchString(string(k)) {
			return errors.New("BuyerConsentConsent property name: does not match pattern")
		}
	}
	for _, m := range *v {
		if err := m.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// BuyerConsentConsentPurpose A buyer's consent decision for a purpose (e.g., marketing, analytics). Carries the current binary state, its source (business default or platform-captured buyer decision), human-readable context, and optional refinements scoping the decision to specific channels, vendors, or programs.
type BuyerConsentConsentPurpose struct {
	// Human-readable description of what the buyer is consenting to (e.g., 'Promotional communications across all channels').
	Description string `json:"description"`
	// Whether consent has been granted for this purpose. The `source` field identifies who asserted this state (business default or platform-captured buyer preference).
	Granted bool `json:"granted"`
	// Optional links providing context (e.g., privacy policy, terms).
	Links []types.Link `json:"links,omitzero"`
	// Optional refinements scoping this purpose to specific channels, vendors, or programs. Keys are reverse-DNS identifiers. UCP currently defines two well-known segment identifiers under `dev.ucp.consent.marketing`: `dev.ucp.consent.marketing.email`, `dev.ucp.consent.marketing.sms`. Other segments follow vendor or merchant reverse-DNS conventions.
	Segments map[string]BuyerConsentConsentSegment `json:"segments,omitzero"`
	// Identifies the party that asserted the current `granted` value. `business` means the value reflects the business's default policy; `platform` means the value reflects an explicit buyer decision captured by the platform.
	Source string `json:"source"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *BuyerConsentConsentPurpose) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("BuyerConsentConsentPurpose: null is not a valid object")
	}
	type BuyerConsentConsentPurposeAlias BuyerConsentConsentPurpose
	var named BuyerConsentConsentPurposeAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = BuyerConsentConsentPurpose(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 3)
	for _, name := range []string{"description", "granted", "source"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "description")
	delete(all, "granted")
	delete(all, "links")
	delete(all, "segments")
	delete(all, "source")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v BuyerConsentConsentPurpose) MarshalJSON() ([]byte, error) {
	type BuyerConsentConsentPurposeAlias BuyerConsentConsentPurpose
	named, err := json.Marshal(BuyerConsentConsentPurposeAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

var pattern_BuyerConsentConsentPurpose_Segments_Key = sync.OnceValue(func() *regexp.Regexp {
	return regexp.MustCompile("^[a-z](?:[a-z0-9-]*[a-z0-9])?(?:\\.[a-z0-9](?:[a-z0-9_-]*[a-z0-9_])?)+$")
})

// Validate reports the first constraint violation, or nil.
func (v *BuyerConsentConsentPurpose) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["granted"] {
			return errors.New("granted: required property is missing")
		}
		if !v.present["source"] {
			return errors.New("source: required property is missing")
		}
	}
	if v.Segments != nil {
		for k := range v.Segments {
			if !pattern_BuyerConsentConsentPurpose_Segments_Key().MatchString(string(k)) {
				return errors.New("segments property name: does not match pattern")
			}
		}
	}
	if v.Source != "business" && v.Source != "platform" {
		return errors.New("source: not one of the permitted values")
	}
	for i := range v.Links {
		if err := v.Links[i].Validate(); err != nil {
			return err
		}
	}
	for _, m := range v.Segments {
		if err := m.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// BuyerConsentConsentSegment A buyer's consent decision for a specific refinement of a parent purpose (e.g., email marketing under the marketing purpose). Overrides the parent's `granted` value for this scope. Segments do not nest further.
type BuyerConsentConsentSegment struct {
	// Human-readable description of what the buyer is consenting to within this segment (e.g., 'Promotional emails and exclusive offers').
	Description string `json:"description"`
	// Whether consent has been granted for this segment. Overrides the parent purpose's `granted` value for this specific scope.
	Granted bool `json:"granted"`
	// Optional segment-specific links (e.g., channel terms or privacy disclosures).
	Links []types.Link `json:"links,omitzero"`
	// Identifies the party that asserted the current `granted` value for this segment. `business` means the value reflects the business's default policy; `platform` means the value reflects an explicit buyer decision captured by the platform.
	Source string `json:"source"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *BuyerConsentConsentSegment) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("BuyerConsentConsentSegment: null is not a valid object")
	}
	type BuyerConsentConsentSegmentAlias BuyerConsentConsentSegment
	var named BuyerConsentConsentSegmentAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = BuyerConsentConsentSegment(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 3)
	for _, name := range []string{"description", "granted", "source"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "description")
	delete(all, "granted")
	delete(all, "links")
	delete(all, "source")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v BuyerConsentConsentSegment) MarshalJSON() ([]byte, error) {
	type BuyerConsentConsentSegmentAlias BuyerConsentConsentSegment
	named, err := json.Marshal(BuyerConsentConsentSegmentAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *BuyerConsentConsentSegment) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["granted"] {
			return errors.New("granted: required property is missing")
		}
		if !v.present["source"] {
			return errors.New("source: required property is missing")
		}
	}
	if v.Source != "business" && v.Source != "platform" {
		return errors.New("source: not one of the permitted values")
	}
	for i := range v.Links {
		if err := v.Links[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}
