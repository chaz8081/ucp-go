// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/catalog_lookup.json (spec release/2026-08-25@cd78fb3)

package shopping

import (
	"encoding/json"
	"errors"
	ucp "github.com/chaz8081/ucp-go"
	commontypes "github.com/chaz8081/ucp-go/common/types"
	"github.com/chaz8081/ucp-go/shopping/types"
)

// CatalogLookup Product/variant lookup by identifier. Supports batch retrieval (lookup_catalog) and single-product detail (get_product).
type CatalogLookup map[string]any

// UnmarshalJSON rejects a bare null. encoding/json treats null as a
// no-op for every Go type, so without this the zero value would pass
// every check and a null document would validate as though it were a
// real value.
func (v *CatalogLookup) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("CatalogLookup: null is not a valid object")
	}
	type CatalogLookupAlias CatalogLookup
	var alias CatalogLookupAlias
	if err := json.Unmarshal(data, &alias); err != nil {
		return err
	}
	*v = CatalogLookup(alias)
	return nil
}

// Validate reports the first constraint violation, or nil.
func (v *CatalogLookup) Validate() error {
	return nil
}

// CatalogLookupDetailProduct A product in a get_product response, extended with effective selections and availability signals on option values.
type CatalogLookupDetailProduct struct {
	// Product categories with optional taxonomy identifiers.
	Categories []types.Category `json:"categories,omitzero"`
	// Product description in one or more formats.
	Description commontypes.Description `json:"description"`
	// URL-safe slug for SEO-friendly URLs (e.g., 'blue-runner-pro'). Use id for stable API references.
	Handle *string `json:"handle,omitzero"`
	// Global ID (GID) uniquely identifying this product.
	ID string `json:"id"`
	// List price range before discounts (for strikethrough display).
	ListPriceRange *commontypes.PriceRange `json:"list_price_range,omitzero"`
	// Product media (images, videos, 3D models). First item is the featured media for listings.
	Media []commontypes.Media `json:"media,omitzero"`
	// Business-defined custom data extending the standard product model.
	Metadata map[string]any `json:"metadata,omitzero"`
	// Product options with availability signals relative to the effective selections.
	Options []CatalogLookupDetailProductOptionsItem `json:"options,omitzero"`
	// Price range across all variants.
	PriceRange commontypes.PriceRange `json:"price_range"`
	// Aggregate product rating.
	Rating *types.Rating `json:"rating,omitzero"`
	// Effective option selections that anchor the featured variant and availability signals. Required when the product has configurable options; may be empty or omitted for products with no option axes.
	Selected []types.SelectedOption `json:"selected,omitzero"`
	// Product tags for categorization and search.
	Tags []string `json:"tags,omitzero"`
	// Product title.
	Title string `json:"title"`
	// Canonical product page URL.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	URL *string `json:"url,omitzero"`
	// Purchasable variants of this product. First item is the featured variant for listings.
	Variants []types.Variant `json:"variants"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *CatalogLookupDetailProduct) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("CatalogLookupDetailProduct: null is not a valid object")
	}
	type CatalogLookupDetailProductAlias CatalogLookupDetailProduct
	var named CatalogLookupDetailProductAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = CatalogLookupDetailProduct(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 5)
	for _, name := range []string{"description", "id", "price_range", "title", "variants"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "categories")
	delete(all, "description")
	delete(all, "handle")
	delete(all, "id")
	delete(all, "list_price_range")
	delete(all, "media")
	delete(all, "metadata")
	delete(all, "options")
	delete(all, "price_range")
	delete(all, "rating")
	delete(all, "selected")
	delete(all, "tags")
	delete(all, "title")
	delete(all, "url")
	delete(all, "variants")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v CatalogLookupDetailProduct) MarshalJSON() ([]byte, error) {
	type CatalogLookupDetailProductAlias CatalogLookupDetailProduct
	named, err := json.Marshal(CatalogLookupDetailProductAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *CatalogLookupDetailProduct) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["price_range"] {
			return errors.New("price_range: required property is missing")
		}
		if !v.present["title"] {
			return errors.New("title: required property is missing")
		}
		if !v.present["variants"] {
			return errors.New("variants: required property is missing")
		}
	}
	if len(v.Variants) < 1 {
		return errors.New("variants: has fewer than minItems 1")
	}
	for i := range v.Categories {
		if err := v.Categories[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Description.Validate(); err != nil {
		return err
	}
	if v.ListPriceRange != nil {
		if err := v.ListPriceRange.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Media {
		if err := v.Media[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Options {
		if err := v.Options[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.PriceRange.Validate(); err != nil {
		return err
	}
	if v.Rating != nil {
		if err := v.Rating.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Selected {
		if err := v.Selected[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Variants {
		if err := v.Variants[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// CatalogLookupGetProductRequest Request body for single-product retrieval. Supports interactive variant narrowing via selected and preferences.
type CatalogLookupGetProductRequest struct {
	Attribution *types.Attribution   `json:"attribution,omitzero"`
	Context     *commontypes.Context `json:"context,omitzero"`
	// Filter criteria to narrow returned variants. All specified filters combine with AND logic.
	Filters *types.SearchFilters `json:"filters,omitzero"`
	// Product or variant identifier. Implementations MUST support product ID and variant ID.
	ID string `json:"id"`
	// Option names in relaxation priority order. When no exact variant matches all selections, the server drops options from the end of this list first. E.g., ['Color', 'Size'] keeps Color and relaxes Size.
	Preferences []string `json:"preferences,omitzero"`
	// Partial or full option selections for interactive variant narrowing. When provided, response option values include availability signals (available, exists) relative to these selections.
	Selected []types.SelectedOption `json:"selected,omitzero"`
	Signals  *commontypes.Signals   `json:"signals,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *CatalogLookupGetProductRequest) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("CatalogLookupGetProductRequest: null is not a valid object")
	}
	type CatalogLookupGetProductRequestAlias CatalogLookupGetProductRequest
	var named CatalogLookupGetProductRequestAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = CatalogLookupGetProductRequest(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"id"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "attribution")
	delete(all, "context")
	delete(all, "filters")
	delete(all, "id")
	delete(all, "preferences")
	delete(all, "selected")
	delete(all, "signals")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v CatalogLookupGetProductRequest) MarshalJSON() ([]byte, error) {
	type CatalogLookupGetProductRequestAlias CatalogLookupGetProductRequest
	named, err := json.Marshal(CatalogLookupGetProductRequestAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *CatalogLookupGetProductRequest) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
	}
	if v.Attribution != nil {
		if err := v.Attribution.Validate(); err != nil {
			return err
		}
	}
	if v.Context != nil {
		if err := v.Context.Validate(); err != nil {
			return err
		}
	}
	if v.Filters != nil {
		if err := v.Filters.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Selected {
		if err := v.Selected[i].Validate(); err != nil {
			return err
		}
	}
	if v.Signals != nil {
		if err := v.Signals.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// CatalogLookupGetProductResponse is generated from shopping/catalog_lookup.json.
type CatalogLookupGetProductResponse struct {
	// Outstanding extension-defined Actions for this product response.
	Actions *commontypes.Actions `json:"actions,omitzero"`
	// Warnings or informational messages about the product (e.g., price recently changed, limited availability).
	Messages []commontypes.Message `json:"messages,omitzero"`
	// Policies (e.g., return/refund terms) that apply to this product. `applies_to` targets are relative to the response root; when absent or empty, refer to the URLs in `links[]`.
	Policies []commontypes.Policy `json:"policies,omitzero"`
	// The requested product with full detail. Singular — this is a single-resource operation.
	Product CatalogLookupDetailProduct   `json:"product"`
	UCP     ucp.UCPResponseCatalogSchema `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *CatalogLookupGetProductResponse) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("CatalogLookupGetProductResponse: null is not a valid object")
	}
	type CatalogLookupGetProductResponseAlias CatalogLookupGetProductResponse
	var named CatalogLookupGetProductResponseAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = CatalogLookupGetProductResponse(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"product", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "actions")
	delete(all, "messages")
	delete(all, "policies")
	delete(all, "product")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v CatalogLookupGetProductResponse) MarshalJSON() ([]byte, error) {
	type CatalogLookupGetProductResponseAlias CatalogLookupGetProductResponse
	named, err := json.Marshal(CatalogLookupGetProductResponseAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *CatalogLookupGetProductResponse) Validate() error {
	if v.present != nil {
		if !v.present["product"] {
			return errors.New("product: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	if v.Actions != nil {
		if err := v.Actions.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Product.Validate(); err != nil {
		return err
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// CatalogLookupLookupRequest Request body for catalog lookup.
type CatalogLookupLookupRequest struct {
	Attribution *types.Attribution   `json:"attribution,omitzero"`
	Context     *commontypes.Context `json:"context,omitzero"`
	// Filter criteria to narrow returned products and variants. All specified filters combine with AND logic.
	Filters *types.SearchFilters `json:"filters,omitzero"`
	// Identifiers to lookup. Implementations MUST support product ID and variant ID; MAY support secondary identifiers (SKU, handle, etc.).
	Ids     []string             `json:"ids"`
	Signals *commontypes.Signals `json:"signals,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *CatalogLookupLookupRequest) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("CatalogLookupLookupRequest: null is not a valid object")
	}
	type CatalogLookupLookupRequestAlias CatalogLookupLookupRequest
	var named CatalogLookupLookupRequestAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = CatalogLookupLookupRequest(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"ids"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "attribution")
	delete(all, "context")
	delete(all, "filters")
	delete(all, "ids")
	delete(all, "signals")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v CatalogLookupLookupRequest) MarshalJSON() ([]byte, error) {
	type CatalogLookupLookupRequestAlias CatalogLookupLookupRequest
	named, err := json.Marshal(CatalogLookupLookupRequestAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *CatalogLookupLookupRequest) Validate() error {
	if v.present != nil {
		if !v.present["ids"] {
			return errors.New("ids: required property is missing")
		}
	}
	if len(v.Ids) < 1 {
		return errors.New("ids: has fewer than minItems 1")
	}
	if v.Attribution != nil {
		if err := v.Attribution.Validate(); err != nil {
			return err
		}
	}
	if v.Context != nil {
		if err := v.Context.Validate(); err != nil {
			return err
		}
	}
	if v.Filters != nil {
		if err := v.Filters.Validate(); err != nil {
			return err
		}
	}
	if v.Signals != nil {
		if err := v.Signals.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// CatalogLookupLookupResponse is generated from shopping/catalog_lookup.json.
type CatalogLookupLookupResponse struct {
	// Outstanding extension-defined Actions for this catalog lookup response.
	Actions *commontypes.Actions `json:"actions,omitzero"`
	// Errors, warnings, or informational messages about the requested items.
	Messages []commontypes.Message `json:"messages,omitzero"`
	// Policies (e.g., return/refund terms) that apply to the products in this response. `applies_to` targets are relative to the response root; when absent or empty, refer to the URLs in `links[]`.
	Policies []commontypes.Policy `json:"policies,omitzero"`
	// Products matching the requested identifiers. May contain fewer items if some identifiers not found, or more if identifiers match multiple products.
	Products []CatalogLookupLookupResponseProductsItem `json:"products"`
	UCP      ucp.UCPResponseCatalogSchema              `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *CatalogLookupLookupResponse) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("CatalogLookupLookupResponse: null is not a valid object")
	}
	type CatalogLookupLookupResponseAlias CatalogLookupLookupResponse
	var named CatalogLookupLookupResponseAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = CatalogLookupLookupResponse(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"products", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "actions")
	delete(all, "messages")
	delete(all, "policies")
	delete(all, "products")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v CatalogLookupLookupResponse) MarshalJSON() ([]byte, error) {
	type CatalogLookupLookupResponseAlias CatalogLookupLookupResponse
	named, err := json.Marshal(CatalogLookupLookupResponseAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *CatalogLookupLookupResponse) Validate() error {
	if v.present != nil {
		if !v.present["products"] {
			return errors.New("products: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	if v.Actions != nil {
		if err := v.Actions.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Products {
		if err := v.Products[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// CatalogLookupLookupVariant Variant with required correlation metadata for lookup responses.
type CatalogLookupLookupVariant struct {
	// Variant availability for purchase.
	Availability *types.Availability `json:"availability,omitzero"`
	// Industry-standard product identifiers for cross-reference and correlation.
	Barcodes []CatalogLookupLookupVariantBarcodesItem `json:"barcodes,omitzero"`
	// Variant categories with optional taxonomy identifiers.
	Categories []types.Category `json:"categories,omitzero"`
	// Variant description in one or more formats.
	Description commontypes.Description `json:"description"`
	// URL-safe variant handle/slug.
	Handle *string `json:"handle,omitzero"`
	// Global ID (GID) uniquely identifying this variant. Used as item.id in checkout.
	ID string `json:"id"`
	// Which request identifiers resolved to this variant, and how. Each entry maps a request ID to its match type.
	Inputs []types.InputCorrelation `json:"inputs"`
	// List price before discounts (for strikethrough display).
	ListPrice *commontypes.Price `json:"list_price,omitzero"`
	// Variant media (images, videos, 3D models). First item is the featured media for listings.
	Media []commontypes.Media `json:"media,omitzero"`
	// Business-defined custom data extending the standard variant model.
	Metadata map[string]any `json:"metadata,omitzero"`
	// Option values that define this variant (e.g., Color: Blue, Size: Large).
	Options []types.SelectedOption `json:"options,omitzero"`
	// Current selling price. Price is the amount per one whole `quantity_unit.unit` (for example, per lb or per hour); when `quantity_unit` is absent, it is per `each`. Line total is `price × quantity × 10^-scale`, computed and rounded once by the Business; `totals` remain authoritative.
	Price commontypes.Price `json:"price"`
	// Sale basis this variant's `quantity` is denominated in. The default sale basis is `each`, whose machine identity is (`C62`, 0); `C62` is the UN/CEFACT Rec20 code for one/each. An absent catalog descriptor encodes that default. An `increment` advertises the ordering granularity in steps (for example, `scale` 2 with `increment` 25 sells in 0.25-unit multiples).
	QuantityUnit *commontypes.QuantityUnit `json:"quantity_unit,omitzero"`
	// Variant rating.
	Rating *types.Rating `json:"rating,omitzero"`
	// Optional seller context for this variant.
	Seller *CatalogLookupLookupVariantSeller `json:"seller,omitzero"`
	// Business-assigned identifier for inventory and fulfillment.
	SKU *string `json:"sku,omitzero"`
	// Variant tags for categorization and search.
	Tags []string `json:"tags,omitzero"`
	// Variant display title (e.g., 'Blue / Large').
	Title string `json:"title"`
	// Price per standard unit of measurement, for shelf-style comparison display. MAY be omitted when unit pricing does not apply.
	UnitPrice *types.UnitPrice `json:"unit_price,omitzero"`
	// Canonical variant page URL.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	URL *string `json:"url,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *CatalogLookupLookupVariant) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("CatalogLookupLookupVariant: null is not a valid object")
	}
	type CatalogLookupLookupVariantAlias CatalogLookupLookupVariant
	var named CatalogLookupLookupVariantAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = CatalogLookupLookupVariant(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 5)
	for _, name := range []string{"description", "id", "inputs", "price", "title"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "availability")
	delete(all, "barcodes")
	delete(all, "categories")
	delete(all, "description")
	delete(all, "handle")
	delete(all, "id")
	delete(all, "inputs")
	delete(all, "list_price")
	delete(all, "media")
	delete(all, "metadata")
	delete(all, "options")
	delete(all, "price")
	delete(all, "quantity_unit")
	delete(all, "rating")
	delete(all, "seller")
	delete(all, "sku")
	delete(all, "tags")
	delete(all, "title")
	delete(all, "unit_price")
	delete(all, "url")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v CatalogLookupLookupVariant) MarshalJSON() ([]byte, error) {
	type CatalogLookupLookupVariantAlias CatalogLookupLookupVariant
	named, err := json.Marshal(CatalogLookupLookupVariantAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *CatalogLookupLookupVariant) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["inputs"] {
			return errors.New("inputs: required property is missing")
		}
		if !v.present["price"] {
			return errors.New("price: required property is missing")
		}
		if !v.present["title"] {
			return errors.New("title: required property is missing")
		}
	}
	if len(v.Inputs) < 1 {
		return errors.New("inputs: has fewer than minItems 1")
	}
	if v.Availability != nil {
		if err := v.Availability.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Barcodes {
		if err := v.Barcodes[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Categories {
		if err := v.Categories[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Description.Validate(); err != nil {
		return err
	}
	for i := range v.Inputs {
		if err := v.Inputs[i].Validate(); err != nil {
			return err
		}
	}
	if v.ListPrice != nil {
		if err := v.ListPrice.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Media {
		if err := v.Media[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Options {
		if err := v.Options[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Price.Validate(); err != nil {
		return err
	}
	if v.QuantityUnit != nil {
		if err := v.QuantityUnit.Validate(); err != nil {
			return err
		}
	}
	if v.Rating != nil {
		if err := v.Rating.Validate(); err != nil {
			return err
		}
	}
	if v.Seller != nil {
		if err := v.Seller.Validate(); err != nil {
			return err
		}
	}
	if v.UnitPrice != nil {
		if err := v.UnitPrice.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// CatalogLookupDetailProductOptionsItem is generated from shopping/catalog_lookup.json.
type CatalogLookupDetailProductOptionsItem struct {
	Name   string                    `json:"name"`
	Values []types.DetailOptionValue `json:"values"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *CatalogLookupDetailProductOptionsItem) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("CatalogLookupDetailProductOptionsItem: null is not a valid object")
	}
	type CatalogLookupDetailProductOptionsItemAlias CatalogLookupDetailProductOptionsItem
	var named CatalogLookupDetailProductOptionsItemAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = CatalogLookupDetailProductOptionsItem(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"name", "values"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "name")
	delete(all, "values")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v CatalogLookupDetailProductOptionsItem) MarshalJSON() ([]byte, error) {
	type CatalogLookupDetailProductOptionsItemAlias CatalogLookupDetailProductOptionsItem
	named, err := json.Marshal(CatalogLookupDetailProductOptionsItemAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *CatalogLookupDetailProductOptionsItem) Validate() error {
	if v.present != nil {
		if !v.present["name"] {
			return errors.New("name: required property is missing")
		}
		if !v.present["values"] {
			return errors.New("values: required property is missing")
		}
	}
	if len(v.Values) < 1 {
		return errors.New("values: has fewer than minItems 1")
	}
	for i := range v.Values {
		if err := v.Values[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// CatalogLookupLookupResponseProductsItem is generated from shopping/catalog_lookup.json.
type CatalogLookupLookupResponseProductsItem struct {
	// Product categories with optional taxonomy identifiers.
	Categories []types.Category `json:"categories,omitzero"`
	// Product description in one or more formats.
	Description commontypes.Description `json:"description"`
	// URL-safe slug for SEO-friendly URLs (e.g., 'blue-runner-pro'). Use id for stable API references.
	Handle *string `json:"handle,omitzero"`
	// Global ID (GID) uniquely identifying this product.
	ID string `json:"id"`
	// List price range before discounts (for strikethrough display).
	ListPriceRange *commontypes.PriceRange `json:"list_price_range,omitzero"`
	// Product media (images, videos, 3D models). First item is the featured media for listings.
	Media []commontypes.Media `json:"media,omitzero"`
	// Business-defined custom data extending the standard product model.
	Metadata map[string]any `json:"metadata,omitzero"`
	// Product options (Size, Color, etc.).
	Options []types.ProductOption `json:"options,omitzero"`
	// Price range across all variants.
	PriceRange commontypes.PriceRange `json:"price_range"`
	// Aggregate product rating.
	Rating *types.Rating `json:"rating,omitzero"`
	// Product tags for categorization and search.
	Tags []string `json:"tags,omitzero"`
	// Product title.
	Title string `json:"title"`
	// Canonical product page URL.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	URL *string `json:"url,omitzero"`
	// Purchasable variants of this product. First item is the featured variant for listings.
	Variants []CatalogLookupLookupVariant `json:"variants"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *CatalogLookupLookupResponseProductsItem) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("CatalogLookupLookupResponseProductsItem: null is not a valid object")
	}
	type CatalogLookupLookupResponseProductsItemAlias CatalogLookupLookupResponseProductsItem
	var named CatalogLookupLookupResponseProductsItemAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = CatalogLookupLookupResponseProductsItem(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 5)
	for _, name := range []string{"description", "id", "price_range", "title", "variants"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "categories")
	delete(all, "description")
	delete(all, "handle")
	delete(all, "id")
	delete(all, "list_price_range")
	delete(all, "media")
	delete(all, "metadata")
	delete(all, "options")
	delete(all, "price_range")
	delete(all, "rating")
	delete(all, "tags")
	delete(all, "title")
	delete(all, "url")
	delete(all, "variants")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v CatalogLookupLookupResponseProductsItem) MarshalJSON() ([]byte, error) {
	type CatalogLookupLookupResponseProductsItemAlias CatalogLookupLookupResponseProductsItem
	named, err := json.Marshal(CatalogLookupLookupResponseProductsItemAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *CatalogLookupLookupResponseProductsItem) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["price_range"] {
			return errors.New("price_range: required property is missing")
		}
		if !v.present["title"] {
			return errors.New("title: required property is missing")
		}
		if !v.present["variants"] {
			return errors.New("variants: required property is missing")
		}
	}
	if len(v.Variants) < 1 {
		return errors.New("variants: has fewer than minItems 1")
	}
	for i := range v.Categories {
		if err := v.Categories[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Description.Validate(); err != nil {
		return err
	}
	if v.ListPriceRange != nil {
		if err := v.ListPriceRange.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Media {
		if err := v.Media[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Options {
		if err := v.Options[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.PriceRange.Validate(); err != nil {
		return err
	}
	if v.Rating != nil {
		if err := v.Rating.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Variants {
		if err := v.Variants[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// CatalogLookupLookupVariantBarcodesItem is generated from shopping/catalog_lookup.json.
type CatalogLookupLookupVariantBarcodesItem struct {
	// Barcode standard. Well-known values: UPC, EAN, ISBN, GTIN, JAN.
	Type string `json:"type"`
	// Barcode value.
	Value string `json:"value"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *CatalogLookupLookupVariantBarcodesItem) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("CatalogLookupLookupVariantBarcodesItem: null is not a valid object")
	}
	type CatalogLookupLookupVariantBarcodesItemAlias CatalogLookupLookupVariantBarcodesItem
	var named CatalogLookupLookupVariantBarcodesItemAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = CatalogLookupLookupVariantBarcodesItem(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"type", "value"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "type")
	delete(all, "value")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v CatalogLookupLookupVariantBarcodesItem) MarshalJSON() ([]byte, error) {
	type CatalogLookupLookupVariantBarcodesItemAlias CatalogLookupLookupVariantBarcodesItem
	named, err := json.Marshal(CatalogLookupLookupVariantBarcodesItemAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *CatalogLookupLookupVariantBarcodesItem) Validate() error {
	if v.present != nil {
		if !v.present["type"] {
			return errors.New("type: required property is missing")
		}
		if !v.present["value"] {
			return errors.New("value: required property is missing")
		}
	}
	return nil
}

// CatalogLookupLookupVariantSeller Optional seller context for this variant.
type CatalogLookupLookupVariantSeller struct {
	// Seller policy and information links.
	Links []commontypes.Link `json:"links,omitzero"`
	// Seller display name.
	Name *string `json:"name,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *CatalogLookupLookupVariantSeller) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("CatalogLookupLookupVariantSeller: null is not a valid object")
	}
	type CatalogLookupLookupVariantSellerAlias CatalogLookupLookupVariantSeller
	var named CatalogLookupLookupVariantSellerAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = CatalogLookupLookupVariantSeller(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "links")
	delete(all, "name")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v CatalogLookupLookupVariantSeller) MarshalJSON() ([]byte, error) {
	type CatalogLookupLookupVariantSellerAlias CatalogLookupLookupVariantSeller
	named, err := json.Marshal(CatalogLookupLookupVariantSellerAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *CatalogLookupLookupVariantSeller) Validate() error {
	for i := range v.Links {
		if err := v.Links[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}
