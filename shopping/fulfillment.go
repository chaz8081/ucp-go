// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/fulfillment.json (spec release/2026-08-25@cd78fb3)

package shopping

import (
	"encoding/json"
	"errors"
	ucp "github.com/chaz8081/ucp-go"
	commontypes "github.com/chaz8081/ucp-go/common/types"
	"github.com/chaz8081/ucp-go/shopping/types"
)

// FulfillmentCatalogFulfillment How a catalog variant can be fulfilled. Mirrors checkout `fulfillment`.
type FulfillmentCatalogFulfillment struct {
	// Fulfillment methods for this variant.
	Methods []FulfillmentCatalogFulfillmentMethod `json:"methods,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentCatalogFulfillment) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentCatalogFulfillment: null is not a valid object")
	}
	type FulfillmentCatalogFulfillmentAlias FulfillmentCatalogFulfillment
	var named FulfillmentCatalogFulfillmentAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentCatalogFulfillment(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "methods")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentCatalogFulfillment) MarshalJSON() ([]byte, error) {
	type FulfillmentCatalogFulfillmentAlias FulfillmentCatalogFulfillment
	named, err := json.Marshal(FulfillmentCatalogFulfillmentAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentCatalogFulfillment) Validate() error {
	for i := range v.Methods {
		if err := v.Methods[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// FulfillmentCatalogFulfillmentMethod A fulfillment method on a catalog variant: how the variant can be fulfilled, and its availability.
type FulfillmentCatalogFulfillmentMethod struct {
	// Availability of this variant via this method at the specified or inferred location.
	Availability *types.Availability `json:"availability,omitzero"`
	// Short buyer-facing summary (e.g. 'Ships in 2–4 business days').
	Description *commontypes.Description `json:"description,omitzero"`
	// Stable, opaque identifier for the Business Location resolved for this place-based fulfillment method. The Business recognizes the same ID when submitted as `selected_destination_id` for that method; recognition does not reserve inventory or guarantee eligibility, and current terms are revalidated.
	Location *string `json:"location,omitzero"`
	// Representative fulfillment options for this method (e.g. Standard, Express). Without a destination or full cart, a Business SHOULD preview meaningful boundary options (e.g. cheapest, fastest); more specific options are negotiated in Checkout once line items and destination are known.
	Options []types.FulfillmentOptionBase `json:"options,omitzero"`
	// Fulfillment method type. Well-known values: `shipping`, `pickup`. Businesses MAY use additional values.
	Type string `json:"type"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentCatalogFulfillmentMethod) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentCatalogFulfillmentMethod: null is not a valid object")
	}
	type FulfillmentCatalogFulfillmentMethodAlias FulfillmentCatalogFulfillmentMethod
	var named FulfillmentCatalogFulfillmentMethodAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentCatalogFulfillmentMethod(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"type"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "availability")
	delete(all, "description")
	delete(all, "location")
	delete(all, "options")
	delete(all, "type")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentCatalogFulfillmentMethod) MarshalJSON() ([]byte, error) {
	type FulfillmentCatalogFulfillmentMethodAlias FulfillmentCatalogFulfillmentMethod
	named, err := json.Marshal(FulfillmentCatalogFulfillmentMethodAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentCatalogFulfillmentMethod) Validate() error {
	if v.present != nil {
		if !v.present["type"] {
			return errors.New("type: required property is missing")
		}
	}
	if v.Availability != nil {
		if err := v.Availability.Validate(); err != nil {
			return err
		}
	}
	if v.Description != nil {
		if err := v.Description.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Options {
		if err := v.Options[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// FulfillmentCheckout Checkout extended with hierarchical fulfillment.
type FulfillmentCheckout struct {
	// Outstanding extension-defined Actions for this checkout.
	Actions     *commontypes.Actions `json:"actions,omitzero"`
	Attribution *types.Attribution   `json:"attribution,omitzero"`
	// Representation of the buyer.
	Buyer   *types.Buyer         `json:"buyer,omitzero"`
	Context *commontypes.Context `json:"context,omitzero"`
	// URL for checkout handoff and session recovery. MUST be provided when status is requires_escalation. See specification for format and availability requirements.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ContinueURL *string `json:"continue_url,omitzero"`
	// ISO 4217 currency code reflecting the merchant's market determination. Derived from address, context, and geo IP—buyers provide signals, merchants determine currency.
	Currency string `json:"currency"`
	// RFC 3339 expiry timestamp. Default TTL is 6 hours from creation if not sent.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	ExpiresAt *string `json:"expires_at,omitzero"`
	// Fulfillment details.
	Fulfillment *FulfillmentFulfillment `json:"fulfillment,omitzero"`
	// Unique identifier of the checkout session.
	ID string `json:"id"`
	// List of line items being checked out.
	LineItems []types.LineItem `json:"line_items"`
	// Links to be displayed by the platform (Privacy Policy, TOS). Mandatory for legal compliance.
	Links []commontypes.Link `json:"links"`
	// List of messages with error and info about the checkout session state.
	Messages []commontypes.Message `json:"messages,omitzero"`
	// Details about an order created for this checkout session.
	Order   *types.OrderConfirmation `json:"order,omitzero"`
	Payment *commontypes.Payment     `json:"payment,omitzero"`
	// Policies (e.g., return/refund terms) that apply to the items in this checkout. `applies_to` targets are relative to the response root; when absent or empty, refer to the URLs in `links[]`.
	Policies []commontypes.Policy `json:"policies,omitzero"`
	Signals  *commontypes.Signals `json:"signals,omitzero"`
	// Checkout state indicating the current phase and required processing. See Checkout Status lifecycle documentation for state transition details.
	Status string `json:"status"`
	// Different cart totals.
	Totals commontypes.Totals `json:"totals"`
	UCP    ucp.UCPMetadata    `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentCheckout) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentCheckout: null is not a valid object")
	}
	type FulfillmentCheckoutAlias FulfillmentCheckout
	var named FulfillmentCheckoutAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentCheckout(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 7)
	for _, name := range []string{"currency", "id", "line_items", "links", "status", "totals", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "actions")
	delete(all, "attribution")
	delete(all, "buyer")
	delete(all, "context")
	delete(all, "continue_url")
	delete(all, "currency")
	delete(all, "expires_at")
	delete(all, "fulfillment")
	delete(all, "id")
	delete(all, "line_items")
	delete(all, "links")
	delete(all, "messages")
	delete(all, "order")
	delete(all, "payment")
	delete(all, "policies")
	delete(all, "signals")
	delete(all, "status")
	delete(all, "totals")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentCheckout) MarshalJSON() ([]byte, error) {
	type FulfillmentCheckoutAlias FulfillmentCheckout
	named, err := json.Marshal(FulfillmentCheckoutAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentCheckout) Validate() error {
	if v.present != nil {
		if !v.present["currency"] {
			return errors.New("currency: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["line_items"] {
			return errors.New("line_items: required property is missing")
		}
		if !v.present["links"] {
			return errors.New("links: required property is missing")
		}
		if !v.present["status"] {
			return errors.New("status: required property is missing")
		}
		if !v.present["totals"] {
			return errors.New("totals: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	if v.Status != "incomplete" && v.Status != "requires_escalation" && v.Status != "ready_for_complete" && v.Status != "complete_in_progress" && v.Status != "completed" && v.Status != "canceled" {
		return errors.New("status: not one of the permitted values")
	}
	if v.Actions != nil {
		if err := v.Actions.Validate(); err != nil {
			return err
		}
	}
	if v.Attribution != nil {
		if err := v.Attribution.Validate(); err != nil {
			return err
		}
	}
	if v.Buyer != nil {
		if err := v.Buyer.Validate(); err != nil {
			return err
		}
	}
	if v.Context != nil {
		if err := v.Context.Validate(); err != nil {
			return err
		}
	}
	if v.Fulfillment != nil {
		if err := v.Fulfillment.Validate(); err != nil {
			return err
		}
	}
	for i := range v.LineItems {
		if err := v.LineItems[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Links {
		if err := v.Links[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	if v.Order != nil {
		if err := v.Order.Validate(); err != nil {
			return err
		}
	}
	if v.Payment != nil {
		if err := v.Payment.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	if v.Signals != nil {
		if err := v.Signals.Validate(); err != nil {
			return err
		}
	}
	if err := v.Totals.Validate(); err != nil {
		return err
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// $defs/dev_ucp_shopping_fulfillment is an extension mount point (a namespace of schemas,
// not a schema); no type is emitted for it.

// FulfillmentFulfillment is generated from shopping/fulfillment.json.
type FulfillmentFulfillment = types.Fulfillment

// FulfillmentFulfillmentAvailableMethod is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentAvailableMethod = types.FulfillmentAvailableMethod

// FulfillmentFulfillmentDetailProduct A get_product detail product (carrying selected/options availability signals) whose variants are fulfillment-enriched. Used by get_product.
type FulfillmentFulfillmentDetailProduct struct {
	// Product categories with optional taxonomy identifiers.
	Categories []types.Category `json:"categories,omitzero"`
	// Product description in one or more formats.
	Description commontypes.Description `json:"description"`
	// URL-safe slug for SEO-friendly URLs (e.g., 'blue-runner-pro'). Use id for stable API references.
	Handle *string `json:"handle,omitzero"`
	// Global ID (GID) uniquely identifying this product.
	ID string `json:"id"`
	// List price range before discounts (for strikethrough display).
	ListPriceRange *commontypes.PriceRange `json:"list_price_range,omitzero"`
	// Product media (images, videos, 3D models). First item is the featured media for listings.
	Media []commontypes.Media `json:"media,omitzero"`
	// Business-defined custom data extending the standard product model.
	Metadata map[string]any `json:"metadata,omitzero"`
	// Product options with availability signals relative to the effective selections.
	Options []FulfillmentFulfillmentDetailProductOptionsItem `json:"options,omitzero"`
	// Price range across all variants.
	PriceRange commontypes.PriceRange `json:"price_range"`
	// Aggregate product rating.
	Rating *types.Rating `json:"rating,omitzero"`
	// Effective option selections that anchor the featured variant and availability signals. Required when the product has configurable options; may be empty or omitted for products with no option axes.
	Selected []types.SelectedOption `json:"selected,omitzero"`
	// Product tags for categorization and search.
	Tags []string `json:"tags,omitzero"`
	// Product title.
	Title string `json:"title"`
	// Canonical product page URL.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	URL *string `json:"url,omitzero"`
	// Purchasable variants of this product. First item is the featured variant for listings.
	Variants []FulfillmentFulfillmentVariant `json:"variants"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentDetailProduct) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentDetailProduct: null is not a valid object")
	}
	type FulfillmentFulfillmentDetailProductAlias FulfillmentFulfillmentDetailProduct
	var named FulfillmentFulfillmentDetailProductAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentDetailProduct(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 5)
	for _, name := range []string{"description", "id", "price_range", "title", "variants"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "categories")
	delete(all, "description")
	delete(all, "handle")
	delete(all, "id")
	delete(all, "list_price_range")
	delete(all, "media")
	delete(all, "metadata")
	delete(all, "options")
	delete(all, "price_range")
	delete(all, "rating")
	delete(all, "selected")
	delete(all, "tags")
	delete(all, "title")
	delete(all, "url")
	delete(all, "variants")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentDetailProduct) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentDetailProductAlias FulfillmentFulfillmentDetailProduct
	named, err := json.Marshal(FulfillmentFulfillmentDetailProductAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentDetailProduct) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["price_range"] {
			return errors.New("price_range: required property is missing")
		}
		if !v.present["title"] {
			return errors.New("title: required property is missing")
		}
		if !v.present["variants"] {
			return errors.New("variants: required property is missing")
		}
	}
	if len(v.Variants) < 1 {
		return errors.New("variants: has fewer than minItems 1")
	}
	for i := range v.Categories {
		if err := v.Categories[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Description.Validate(); err != nil {
		return err
	}
	if v.ListPriceRange != nil {
		if err := v.ListPriceRange.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Media {
		if err := v.Media[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Options {
		if err := v.Options[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.PriceRange.Validate(); err != nil {
		return err
	}
	if v.Rating != nil {
		if err := v.Rating.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Selected {
		if err := v.Selected[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Variants {
		if err := v.Variants[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// FulfillmentFulfillmentGetProductRequest is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentGetProductRequest struct {
	Attribution *types.Attribution   `json:"attribution,omitzero"`
	Context     *commontypes.Context `json:"context,omitzero"`
	// Filter criteria to narrow returned variants. All specified filters combine with AND logic.
	Filters *FulfillmentFulfillmentSearchFilters `json:"filters,omitzero"`
	// Product or variant identifier. Implementations MUST support product ID and variant ID.
	ID string `json:"id"`
	// Option names in relaxation priority order. When no exact variant matches all selections, the server drops options from the end of this list first. E.g., ['Color', 'Size'] keeps Color and relaxes Size.
	Preferences []string `json:"preferences,omitzero"`
	// Partial or full option selections for interactive variant narrowing. When provided, response option values include availability signals (available, exists) relative to these selections.
	Selected []types.SelectedOption `json:"selected,omitzero"`
	Signals  *commontypes.Signals   `json:"signals,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentGetProductRequest) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentGetProductRequest: null is not a valid object")
	}
	type FulfillmentFulfillmentGetProductRequestAlias FulfillmentFulfillmentGetProductRequest
	var named FulfillmentFulfillmentGetProductRequestAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentGetProductRequest(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"id"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "attribution")
	delete(all, "context")
	delete(all, "filters")
	delete(all, "id")
	delete(all, "preferences")
	delete(all, "selected")
	delete(all, "signals")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentGetProductRequest) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentGetProductRequestAlias FulfillmentFulfillmentGetProductRequest
	named, err := json.Marshal(FulfillmentFulfillmentGetProductRequestAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentGetProductRequest) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
	}
	if v.Attribution != nil {
		if err := v.Attribution.Validate(); err != nil {
			return err
		}
	}
	if v.Context != nil {
		if err := v.Context.Validate(); err != nil {
			return err
		}
	}
	if v.Filters != nil {
		if err := v.Filters.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Selected {
		if err := v.Selected[i].Validate(); err != nil {
			return err
		}
	}
	if v.Signals != nil {
		if err := v.Signals.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// FulfillmentFulfillmentGetProductResponse is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentGetProductResponse struct {
	// Outstanding extension-defined Actions for this product response.
	Actions *commontypes.Actions `json:"actions,omitzero"`
	// Warnings or informational messages about the product (e.g., price recently changed, limited availability).
	Messages []commontypes.Message `json:"messages,omitzero"`
	// Policies (e.g., return/refund terms) that apply to this product. `applies_to` targets are relative to the response root; when absent or empty, refer to the URLs in `links[]`.
	Policies []commontypes.Policy `json:"policies,omitzero"`
	// The requested product with full detail. Singular — this is a single-resource operation.
	Product FulfillmentFulfillmentDetailProduct `json:"product"`
	UCP     ucp.UCPResponseCatalogSchema        `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentGetProductResponse) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentGetProductResponse: null is not a valid object")
	}
	type FulfillmentFulfillmentGetProductResponseAlias FulfillmentFulfillmentGetProductResponse
	var named FulfillmentFulfillmentGetProductResponseAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentGetProductResponse(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"product", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "actions")
	delete(all, "messages")
	delete(all, "policies")
	delete(all, "product")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentGetProductResponse) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentGetProductResponseAlias FulfillmentFulfillmentGetProductResponse
	named, err := json.Marshal(FulfillmentFulfillmentGetProductResponseAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentGetProductResponse) Validate() error {
	if v.present != nil {
		if !v.present["product"] {
			return errors.New("product: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	if v.Actions != nil {
		if err := v.Actions.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Product.Validate(); err != nil {
		return err
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// FulfillmentFulfillmentGroup is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentGroup = types.FulfillmentGroup

// FulfillmentFulfillmentLookupProduct A lookup product whose variants are fulfillment-enriched, preserving input correlation. Used by lookup.
type FulfillmentFulfillmentLookupProduct struct {
	// Product categories with optional taxonomy identifiers.
	Categories []types.Category `json:"categories,omitzero"`
	// Product description in one or more formats.
	Description commontypes.Description `json:"description"`
	// URL-safe slug for SEO-friendly URLs (e.g., 'blue-runner-pro'). Use id for stable API references.
	Handle *string `json:"handle,omitzero"`
	// Global ID (GID) uniquely identifying this product.
	ID string `json:"id"`
	// List price range before discounts (for strikethrough display).
	ListPriceRange *commontypes.PriceRange `json:"list_price_range,omitzero"`
	// Product media (images, videos, 3D models). First item is the featured media for listings.
	Media []commontypes.Media `json:"media,omitzero"`
	// Business-defined custom data extending the standard product model.
	Metadata map[string]any `json:"metadata,omitzero"`
	// Product options (Size, Color, etc.).
	Options []types.ProductOption `json:"options,omitzero"`
	// Price range across all variants.
	PriceRange commontypes.PriceRange `json:"price_range"`
	// Aggregate product rating.
	Rating *types.Rating `json:"rating,omitzero"`
	// Product tags for categorization and search.
	Tags []string `json:"tags,omitzero"`
	// Product title.
	Title string `json:"title"`
	// Canonical product page URL.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	URL *string `json:"url,omitzero"`
	// Purchasable variants of this product. First item is the featured variant for listings.
	Variants []FulfillmentFulfillmentLookupVariant `json:"variants"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentLookupProduct) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentLookupProduct: null is not a valid object")
	}
	type FulfillmentFulfillmentLookupProductAlias FulfillmentFulfillmentLookupProduct
	var named FulfillmentFulfillmentLookupProductAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentLookupProduct(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 5)
	for _, name := range []string{"description", "id", "price_range", "title", "variants"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "categories")
	delete(all, "description")
	delete(all, "handle")
	delete(all, "id")
	delete(all, "list_price_range")
	delete(all, "media")
	delete(all, "metadata")
	delete(all, "options")
	delete(all, "price_range")
	delete(all, "rating")
	delete(all, "tags")
	delete(all, "title")
	delete(all, "url")
	delete(all, "variants")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentLookupProduct) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentLookupProductAlias FulfillmentFulfillmentLookupProduct
	named, err := json.Marshal(FulfillmentFulfillmentLookupProductAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentLookupProduct) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["price_range"] {
			return errors.New("price_range: required property is missing")
		}
		if !v.present["title"] {
			return errors.New("title: required property is missing")
		}
		if !v.present["variants"] {
			return errors.New("variants: required property is missing")
		}
	}
	if len(v.Variants) < 1 {
		return errors.New("variants: has fewer than minItems 1")
	}
	for i := range v.Categories {
		if err := v.Categories[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Description.Validate(); err != nil {
		return err
	}
	if v.ListPriceRange != nil {
		if err := v.ListPriceRange.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Media {
		if err := v.Media[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Options {
		if err := v.Options[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.PriceRange.Validate(); err != nil {
		return err
	}
	if v.Rating != nil {
		if err := v.Rating.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Variants {
		if err := v.Variants[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// FulfillmentFulfillmentLookupRequest is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentLookupRequest struct {
	Attribution *types.Attribution   `json:"attribution,omitzero"`
	Context     *commontypes.Context `json:"context,omitzero"`
	// Filter criteria to narrow returned products and variants. All specified filters combine with AND logic.
	Filters *FulfillmentFulfillmentSearchFilters `json:"filters,omitzero"`
	// Identifiers to lookup. Implementations MUST support product ID and variant ID; MAY support secondary identifiers (SKU, handle, etc.).
	Ids     []string             `json:"ids"`
	Signals *commontypes.Signals `json:"signals,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentLookupRequest) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentLookupRequest: null is not a valid object")
	}
	type FulfillmentFulfillmentLookupRequestAlias FulfillmentFulfillmentLookupRequest
	var named FulfillmentFulfillmentLookupRequestAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentLookupRequest(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"ids"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "attribution")
	delete(all, "context")
	delete(all, "filters")
	delete(all, "ids")
	delete(all, "signals")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentLookupRequest) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentLookupRequestAlias FulfillmentFulfillmentLookupRequest
	named, err := json.Marshal(FulfillmentFulfillmentLookupRequestAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentLookupRequest) Validate() error {
	if v.present != nil {
		if !v.present["ids"] {
			return errors.New("ids: required property is missing")
		}
	}
	if len(v.Ids) < 1 {
		return errors.New("ids: has fewer than minItems 1")
	}
	if v.Attribution != nil {
		if err := v.Attribution.Validate(); err != nil {
			return err
		}
	}
	if v.Context != nil {
		if err := v.Context.Validate(); err != nil {
			return err
		}
	}
	if v.Filters != nil {
		if err := v.Filters.Validate(); err != nil {
			return err
		}
	}
	if v.Signals != nil {
		if err := v.Signals.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// FulfillmentFulfillmentLookupResponse is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentLookupResponse struct {
	// Outstanding extension-defined Actions for this catalog lookup response.
	Actions *commontypes.Actions `json:"actions,omitzero"`
	// Errors, warnings, or informational messages about the requested items.
	Messages []commontypes.Message `json:"messages,omitzero"`
	// Policies (e.g., return/refund terms) that apply to the products in this response. `applies_to` targets are relative to the response root; when absent or empty, refer to the URLs in `links[]`.
	Policies []commontypes.Policy `json:"policies,omitzero"`
	// Products matching the requested identifiers. May contain fewer items if some identifiers not found, or more if identifiers match multiple products.
	Products []FulfillmentFulfillmentLookupProduct `json:"products"`
	UCP      ucp.UCPResponseCatalogSchema          `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentLookupResponse) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentLookupResponse: null is not a valid object")
	}
	type FulfillmentFulfillmentLookupResponseAlias FulfillmentFulfillmentLookupResponse
	var named FulfillmentFulfillmentLookupResponseAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentLookupResponse(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"products", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "actions")
	delete(all, "messages")
	delete(all, "policies")
	delete(all, "products")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentLookupResponse) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentLookupResponseAlias FulfillmentFulfillmentLookupResponse
	named, err := json.Marshal(FulfillmentFulfillmentLookupResponseAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentLookupResponse) Validate() error {
	if v.present != nil {
		if !v.present["products"] {
			return errors.New("products: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	if v.Actions != nil {
		if err := v.Actions.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Products {
		if err := v.Products[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// FulfillmentFulfillmentLookupVariant A lookup variant (carrying input correlation) enriched with fulfillment.
type FulfillmentFulfillmentLookupVariant struct {
	// Variant availability for purchase.
	Availability *types.Availability `json:"availability,omitzero"`
	// Industry-standard product identifiers for cross-reference and correlation.
	Barcodes []FulfillmentFulfillmentLookupVariantBarcodesItem `json:"barcodes,omitzero"`
	// Variant categories with optional taxonomy identifiers.
	Categories []types.Category `json:"categories,omitzero"`
	// Variant description in one or more formats.
	Description commontypes.Description        `json:"description"`
	Fulfillment *FulfillmentCatalogFulfillment `json:"fulfillment,omitzero"`
	// URL-safe variant handle/slug.
	Handle *string `json:"handle,omitzero"`
	// Global ID (GID) uniquely identifying this variant. Used as item.id in checkout.
	ID string `json:"id"`
	// Which request identifiers resolved to this variant, and how. Each entry maps a request ID to its match type.
	Inputs []types.InputCorrelation `json:"inputs"`
	// List price before discounts (for strikethrough display).
	ListPrice *commontypes.Price `json:"list_price,omitzero"`
	// Variant media (images, videos, 3D models). First item is the featured media for listings.
	Media []commontypes.Media `json:"media,omitzero"`
	// Business-defined custom data extending the standard variant model.
	Metadata map[string]any `json:"metadata,omitzero"`
	// Option values that define this variant (e.g., Color: Blue, Size: Large).
	Options []types.SelectedOption `json:"options,omitzero"`
	// Current selling price. Price is the amount per one whole `quantity_unit.unit` (for example, per lb or per hour); when `quantity_unit` is absent, it is per `each`. Line total is `price × quantity × 10^-scale`, computed and rounded once by the Business; `totals` remain authoritative.
	Price commontypes.Price `json:"price"`
	// Sale basis this variant's `quantity` is denominated in. The default sale basis is `each`, whose machine identity is (`C62`, 0); `C62` is the UN/CEFACT Rec20 code for one/each. An absent catalog descriptor encodes that default. An `increment` advertises the ordering granularity in steps (for example, `scale` 2 with `increment` 25 sells in 0.25-unit multiples).
	QuantityUnit *commontypes.QuantityUnit `json:"quantity_unit,omitzero"`
	// Variant rating.
	Rating *types.Rating `json:"rating,omitzero"`
	// Optional seller context for this variant.
	Seller *FulfillmentFulfillmentLookupVariantSeller `json:"seller,omitzero"`
	// Business-assigned identifier for inventory and fulfillment.
	SKU *string `json:"sku,omitzero"`
	// Variant tags for categorization and search.
	Tags []string `json:"tags,omitzero"`
	// Variant display title (e.g., 'Blue / Large').
	Title string `json:"title"`
	// Price per standard unit of measurement, for shelf-style comparison display. MAY be omitted when unit pricing does not apply.
	UnitPrice *types.UnitPrice `json:"unit_price,omitzero"`
	// Canonical variant page URL.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	URL *string `json:"url,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentLookupVariant) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentLookupVariant: null is not a valid object")
	}
	type FulfillmentFulfillmentLookupVariantAlias FulfillmentFulfillmentLookupVariant
	var named FulfillmentFulfillmentLookupVariantAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentLookupVariant(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 5)
	for _, name := range []string{"description", "id", "inputs", "price", "title"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "availability")
	delete(all, "barcodes")
	delete(all, "categories")
	delete(all, "description")
	delete(all, "fulfillment")
	delete(all, "handle")
	delete(all, "id")
	delete(all, "inputs")
	delete(all, "list_price")
	delete(all, "media")
	delete(all, "metadata")
	delete(all, "options")
	delete(all, "price")
	delete(all, "quantity_unit")
	delete(all, "rating")
	delete(all, "seller")
	delete(all, "sku")
	delete(all, "tags")
	delete(all, "title")
	delete(all, "unit_price")
	delete(all, "url")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentLookupVariant) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentLookupVariantAlias FulfillmentFulfillmentLookupVariant
	named, err := json.Marshal(FulfillmentFulfillmentLookupVariantAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentLookupVariant) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["inputs"] {
			return errors.New("inputs: required property is missing")
		}
		if !v.present["price"] {
			return errors.New("price: required property is missing")
		}
		if !v.present["title"] {
			return errors.New("title: required property is missing")
		}
	}
	if len(v.Inputs) < 1 {
		return errors.New("inputs: has fewer than minItems 1")
	}
	if v.Availability != nil {
		if err := v.Availability.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Barcodes {
		if err := v.Barcodes[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Categories {
		if err := v.Categories[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Description.Validate(); err != nil {
		return err
	}
	if v.Fulfillment != nil {
		if err := v.Fulfillment.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Inputs {
		if err := v.Inputs[i].Validate(); err != nil {
			return err
		}
	}
	if v.ListPrice != nil {
		if err := v.ListPrice.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Media {
		if err := v.Media[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Options {
		if err := v.Options[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Price.Validate(); err != nil {
		return err
	}
	if v.QuantityUnit != nil {
		if err := v.QuantityUnit.Validate(); err != nil {
			return err
		}
	}
	if v.Rating != nil {
		if err := v.Rating.Validate(); err != nil {
			return err
		}
	}
	if v.Seller != nil {
		if err := v.Seller.Validate(); err != nil {
			return err
		}
	}
	if v.UnitPrice != nil {
		if err := v.UnitPrice.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// FulfillmentFulfillmentMethod is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentMethod = types.FulfillmentMethod

// FulfillmentFulfillmentOption is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentOption = types.FulfillmentOption

// FulfillmentFulfillmentProduct A catalog product whose variants are fulfillment-enriched. Used by search.
type FulfillmentFulfillmentProduct struct {
	// Product categories with optional taxonomy identifiers.
	Categories []types.Category `json:"categories,omitzero"`
	// Product description in one or more formats.
	Description commontypes.Description `json:"description"`
	// URL-safe slug for SEO-friendly URLs (e.g., 'blue-runner-pro'). Use id for stable API references.
	Handle *string `json:"handle,omitzero"`
	// Global ID (GID) uniquely identifying this product.
	ID string `json:"id"`
	// List price range before discounts (for strikethrough display).
	ListPriceRange *commontypes.PriceRange `json:"list_price_range,omitzero"`
	// Product media (images, videos, 3D models). First item is the featured media for listings.
	Media []commontypes.Media `json:"media,omitzero"`
	// Business-defined custom data extending the standard product model.
	Metadata map[string]any `json:"metadata,omitzero"`
	// Product options (Size, Color, etc.).
	Options []types.ProductOption `json:"options,omitzero"`
	// Price range across all variants.
	PriceRange commontypes.PriceRange `json:"price_range"`
	// Aggregate product rating.
	Rating *types.Rating `json:"rating,omitzero"`
	// Product tags for categorization and search.
	Tags []string `json:"tags,omitzero"`
	// Product title.
	Title string `json:"title"`
	// Canonical product page URL.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	URL *string `json:"url,omitzero"`
	// Purchasable variants of this product. First item is the featured variant for listings.
	Variants []FulfillmentFulfillmentVariant `json:"variants"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentProduct) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentProduct: null is not a valid object")
	}
	type FulfillmentFulfillmentProductAlias FulfillmentFulfillmentProduct
	var named FulfillmentFulfillmentProductAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentProduct(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 5)
	for _, name := range []string{"description", "id", "price_range", "title", "variants"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "categories")
	delete(all, "description")
	delete(all, "handle")
	delete(all, "id")
	delete(all, "list_price_range")
	delete(all, "media")
	delete(all, "metadata")
	delete(all, "options")
	delete(all, "price_range")
	delete(all, "rating")
	delete(all, "tags")
	delete(all, "title")
	delete(all, "url")
	delete(all, "variants")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentProduct) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentProductAlias FulfillmentFulfillmentProduct
	named, err := json.Marshal(FulfillmentFulfillmentProductAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentProduct) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["price_range"] {
			return errors.New("price_range: required property is missing")
		}
		if !v.present["title"] {
			return errors.New("title: required property is missing")
		}
		if !v.present["variants"] {
			return errors.New("variants: required property is missing")
		}
	}
	if len(v.Variants) < 1 {
		return errors.New("variants: has fewer than minItems 1")
	}
	for i := range v.Categories {
		if err := v.Categories[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Description.Validate(); err != nil {
		return err
	}
	if v.ListPriceRange != nil {
		if err := v.ListPriceRange.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Media {
		if err := v.Media[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Options {
		if err := v.Options[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.PriceRange.Validate(); err != nil {
		return err
	}
	if v.Rating != nil {
		if err := v.Rating.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Variants {
		if err := v.Variants[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// FulfillmentFulfillmentSearchFilters Catalog filters extended with a fulfillment destination filter and a method-type filter.
type FulfillmentFulfillmentSearchFilters struct {
	// Filter by product categories (OR logic — matches products in any listed categories). Values match against the value field in product category entries. Valid values can be discovered from the categories field in search results, merchant documentation, or standard taxonomies that businesses may align with.
	Categories []string `json:"categories,omitzero"`
	// Explicit destination where items are fulfilled. It may differ from the locality or Business Location supplied in `context` (e.g. a gift delivered directly to the recipient). The filter restricts results to what can be fulfilled there and seeds method `availability`. It supersedes `context` only for fulfillment destination and availability resolution.
	FulfillsTo *types.FulfillmentDestinationFilter `json:"fulfills_to,omitzero"`
	// Restrict results to these fulfillment method types (e.g. ["pickup"]). Well-known values: `shipping`, `pickup`.
	Methods []string                 `json:"methods,omitzero"`
	Price   *commontypes.PriceFilter `json:"price,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentSearchFilters) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentSearchFilters: null is not a valid object")
	}
	type FulfillmentFulfillmentSearchFiltersAlias FulfillmentFulfillmentSearchFilters
	var named FulfillmentFulfillmentSearchFiltersAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentSearchFilters(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "categories")
	delete(all, "fulfills_to")
	delete(all, "methods")
	delete(all, "price")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentSearchFilters) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentSearchFiltersAlias FulfillmentFulfillmentSearchFilters
	named, err := json.Marshal(FulfillmentFulfillmentSearchFiltersAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentSearchFilters) Validate() error {
	if v.FulfillsTo != nil {
		if err := v.FulfillsTo.Validate(); err != nil {
			return err
		}
	}
	if v.Price != nil {
		if err := v.Price.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// FulfillmentFulfillmentSearchRequest is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentSearchRequest struct {
	Attribution *types.Attribution                   `json:"attribution,omitzero"`
	Context     *commontypes.Context                 `json:"context,omitzero"`
	Filters     *FulfillmentFulfillmentSearchFilters `json:"filters,omitzero"`
	Pagination  *commontypes.PaginationRequest       `json:"pagination,omitzero"`
	// Free-text search query.
	Query   *string              `json:"query,omitzero"`
	Signals *commontypes.Signals `json:"signals,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentSearchRequest) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentSearchRequest: null is not a valid object")
	}
	type FulfillmentFulfillmentSearchRequestAlias FulfillmentFulfillmentSearchRequest
	var named FulfillmentFulfillmentSearchRequestAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentSearchRequest(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "attribution")
	delete(all, "context")
	delete(all, "filters")
	delete(all, "pagination")
	delete(all, "query")
	delete(all, "signals")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentSearchRequest) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentSearchRequestAlias FulfillmentFulfillmentSearchRequest
	named, err := json.Marshal(FulfillmentFulfillmentSearchRequestAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentSearchRequest) Validate() error {
	if v.Attribution != nil {
		if err := v.Attribution.Validate(); err != nil {
			return err
		}
	}
	if v.Context != nil {
		if err := v.Context.Validate(); err != nil {
			return err
		}
	}
	if v.Filters != nil {
		if err := v.Filters.Validate(); err != nil {
			return err
		}
	}
	if v.Pagination != nil {
		if err := v.Pagination.Validate(); err != nil {
			return err
		}
	}
	if v.Signals != nil {
		if err := v.Signals.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// FulfillmentFulfillmentSearchResponse is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentSearchResponse struct {
	// Outstanding extension-defined Actions for this catalog search response.
	Actions *commontypes.Actions `json:"actions,omitzero"`
	// Errors, warnings, or informational messages about the search results.
	Messages   []commontypes.Message           `json:"messages,omitzero"`
	Pagination *commontypes.PaginationResponse `json:"pagination,omitzero"`
	// Policies (e.g., return/refund terms) that apply to the products in these search results. `applies_to` targets are relative to the response root; when absent or empty, refer to the URLs in `links[]`.
	Policies []commontypes.Policy `json:"policies,omitzero"`
	// Products matching the search criteria.
	Products []FulfillmentFulfillmentProduct `json:"products"`
	UCP      ucp.UCPResponseCatalogSchema    `json:"ucp"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentSearchResponse) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentSearchResponse: null is not a valid object")
	}
	type FulfillmentFulfillmentSearchResponseAlias FulfillmentFulfillmentSearchResponse
	var named FulfillmentFulfillmentSearchResponseAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentSearchResponse(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"products", "ucp"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "actions")
	delete(all, "messages")
	delete(all, "pagination")
	delete(all, "policies")
	delete(all, "products")
	delete(all, "ucp")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentSearchResponse) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentSearchResponseAlias FulfillmentFulfillmentSearchResponse
	named, err := json.Marshal(FulfillmentFulfillmentSearchResponseAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentSearchResponse) Validate() error {
	if v.present != nil {
		if !v.present["products"] {
			return errors.New("products: required property is missing")
		}
		if !v.present["ucp"] {
			return errors.New("ucp: required property is missing")
		}
	}
	if v.Actions != nil {
		if err := v.Actions.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Messages {
		if err := v.Messages[i].Validate(); err != nil {
			return err
		}
	}
	if v.Pagination != nil {
		if err := v.Pagination.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Policies {
		if err := v.Policies[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Products {
		if err := v.Products[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.UCP.Validate(); err != nil {
		return err
	}
	return nil
}

// FulfillmentFulfillmentVariant A catalog variant with fulfillment.
type FulfillmentFulfillmentVariant struct {
	// Variant availability for purchase.
	Availability *types.Availability `json:"availability,omitzero"`
	// Industry-standard product identifiers for cross-reference and correlation.
	Barcodes []FulfillmentFulfillmentVariantBarcodesItem `json:"barcodes,omitzero"`
	// Variant categories with optional taxonomy identifiers.
	Categories []types.Category `json:"categories,omitzero"`
	// Variant description in one or more formats.
	Description commontypes.Description        `json:"description"`
	Fulfillment *FulfillmentCatalogFulfillment `json:"fulfillment,omitzero"`
	// URL-safe variant handle/slug.
	Handle *string `json:"handle,omitzero"`
	// Global ID (GID) uniquely identifying this variant. Used as item.id in checkout.
	ID string `json:"id"`
	// List price before discounts (for strikethrough display).
	ListPrice *commontypes.Price `json:"list_price,omitzero"`
	// Variant media (images, videos, 3D models). First item is the featured media for listings.
	Media []commontypes.Media `json:"media,omitzero"`
	// Business-defined custom data extending the standard variant model.
	Metadata map[string]any `json:"metadata,omitzero"`
	// Option values that define this variant (e.g., Color: Blue, Size: Large).
	Options []types.SelectedOption `json:"options,omitzero"`
	// Current selling price. Price is the amount per one whole `quantity_unit.unit` (for example, per lb or per hour); when `quantity_unit` is absent, it is per `each`. Line total is `price × quantity × 10^-scale`, computed and rounded once by the Business; `totals` remain authoritative.
	Price commontypes.Price `json:"price"`
	// Sale basis this variant's `quantity` is denominated in. The default sale basis is `each`, whose machine identity is (`C62`, 0); `C62` is the UN/CEFACT Rec20 code for one/each. An absent catalog descriptor encodes that default. An `increment` advertises the ordering granularity in steps (for example, `scale` 2 with `increment` 25 sells in 0.25-unit multiples).
	QuantityUnit *commontypes.QuantityUnit `json:"quantity_unit,omitzero"`
	// Variant rating.
	Rating *types.Rating `json:"rating,omitzero"`
	// Optional seller context for this variant.
	Seller *FulfillmentFulfillmentVariantSeller `json:"seller,omitzero"`
	// Business-assigned identifier for inventory and fulfillment.
	SKU *string `json:"sku,omitzero"`
	// Variant tags for categorization and search.
	Tags []string `json:"tags,omitzero"`
	// Variant display title (e.g., 'Blue / Large').
	Title string `json:"title"`
	// Price per standard unit of measurement, for shelf-style comparison display. MAY be omitted when unit pricing does not apply.
	UnitPrice *types.UnitPrice `json:"unit_price,omitzero"`
	// Canonical variant page URL.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	URL *string `json:"url,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentVariant) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentVariant: null is not a valid object")
	}
	type FulfillmentFulfillmentVariantAlias FulfillmentFulfillmentVariant
	var named FulfillmentFulfillmentVariantAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentVariant(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 4)
	for _, name := range []string{"description", "id", "price", "title"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "availability")
	delete(all, "barcodes")
	delete(all, "categories")
	delete(all, "description")
	delete(all, "fulfillment")
	delete(all, "handle")
	delete(all, "id")
	delete(all, "list_price")
	delete(all, "media")
	delete(all, "metadata")
	delete(all, "options")
	delete(all, "price")
	delete(all, "quantity_unit")
	delete(all, "rating")
	delete(all, "seller")
	delete(all, "sku")
	delete(all, "tags")
	delete(all, "title")
	delete(all, "unit_price")
	delete(all, "url")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentVariant) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentVariantAlias FulfillmentFulfillmentVariant
	named, err := json.Marshal(FulfillmentFulfillmentVariantAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentVariant) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["price"] {
			return errors.New("price: required property is missing")
		}
		if !v.present["title"] {
			return errors.New("title: required property is missing")
		}
	}
	if v.Availability != nil {
		if err := v.Availability.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Barcodes {
		if err := v.Barcodes[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Categories {
		if err := v.Categories[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Description.Validate(); err != nil {
		return err
	}
	if v.Fulfillment != nil {
		if err := v.Fulfillment.Validate(); err != nil {
			return err
		}
	}
	if v.ListPrice != nil {
		if err := v.ListPrice.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Media {
		if err := v.Media[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Options {
		if err := v.Options[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Price.Validate(); err != nil {
		return err
	}
	if v.QuantityUnit != nil {
		if err := v.QuantityUnit.Validate(); err != nil {
			return err
		}
	}
	if v.Rating != nil {
		if err := v.Rating.Validate(); err != nil {
			return err
		}
	}
	if v.Seller != nil {
		if err := v.Seller.Validate(); err != nil {
			return err
		}
	}
	if v.UnitPrice != nil {
		if err := v.UnitPrice.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// $defs/lookup is an extension mount point (a namespace of schemas,
// not a schema); no type is emitted for it.

// $defs/search is an extension mount point (a namespace of schemas,
// not a schema); no type is emitted for it.

// FulfillmentFulfillmentDetailProductOptionsItem is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentDetailProductOptionsItem struct {
	Name   string                    `json:"name"`
	Values []types.DetailOptionValue `json:"values"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentDetailProductOptionsItem) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentDetailProductOptionsItem: null is not a valid object")
	}
	type FulfillmentFulfillmentDetailProductOptionsItemAlias FulfillmentFulfillmentDetailProductOptionsItem
	var named FulfillmentFulfillmentDetailProductOptionsItemAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentDetailProductOptionsItem(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"name", "values"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "name")
	delete(all, "values")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentDetailProductOptionsItem) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentDetailProductOptionsItemAlias FulfillmentFulfillmentDetailProductOptionsItem
	named, err := json.Marshal(FulfillmentFulfillmentDetailProductOptionsItemAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentDetailProductOptionsItem) Validate() error {
	if v.present != nil {
		if !v.present["name"] {
			return errors.New("name: required property is missing")
		}
		if !v.present["values"] {
			return errors.New("values: required property is missing")
		}
	}
	if len(v.Values) < 1 {
		return errors.New("values: has fewer than minItems 1")
	}
	for i := range v.Values {
		if err := v.Values[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// FulfillmentFulfillmentLookupVariantBarcodesItem is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentLookupVariantBarcodesItem struct {
	// Barcode standard. Well-known values: UPC, EAN, ISBN, GTIN, JAN.
	Type string `json:"type"`
	// Barcode value.
	Value string `json:"value"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentLookupVariantBarcodesItem) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentLookupVariantBarcodesItem: null is not a valid object")
	}
	type FulfillmentFulfillmentLookupVariantBarcodesItemAlias FulfillmentFulfillmentLookupVariantBarcodesItem
	var named FulfillmentFulfillmentLookupVariantBarcodesItemAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentLookupVariantBarcodesItem(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"type", "value"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "type")
	delete(all, "value")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentLookupVariantBarcodesItem) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentLookupVariantBarcodesItemAlias FulfillmentFulfillmentLookupVariantBarcodesItem
	named, err := json.Marshal(FulfillmentFulfillmentLookupVariantBarcodesItemAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentLookupVariantBarcodesItem) Validate() error {
	if v.present != nil {
		if !v.present["type"] {
			return errors.New("type: required property is missing")
		}
		if !v.present["value"] {
			return errors.New("value: required property is missing")
		}
	}
	return nil
}

// FulfillmentFulfillmentLookupVariantSeller Optional seller context for this variant.
type FulfillmentFulfillmentLookupVariantSeller struct {
	// Seller policy and information links.
	Links []commontypes.Link `json:"links,omitzero"`
	// Seller display name.
	Name *string `json:"name,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentLookupVariantSeller) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentLookupVariantSeller: null is not a valid object")
	}
	type FulfillmentFulfillmentLookupVariantSellerAlias FulfillmentFulfillmentLookupVariantSeller
	var named FulfillmentFulfillmentLookupVariantSellerAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentLookupVariantSeller(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "links")
	delete(all, "name")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentLookupVariantSeller) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentLookupVariantSellerAlias FulfillmentFulfillmentLookupVariantSeller
	named, err := json.Marshal(FulfillmentFulfillmentLookupVariantSellerAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentLookupVariantSeller) Validate() error {
	for i := range v.Links {
		if err := v.Links[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// FulfillmentFulfillmentVariantBarcodesItem is generated from shopping/fulfillment.json.
type FulfillmentFulfillmentVariantBarcodesItem struct {
	// Barcode standard. Well-known values: UPC, EAN, ISBN, GTIN, JAN.
	Type string `json:"type"`
	// Barcode value.
	Value string `json:"value"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentVariantBarcodesItem) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentVariantBarcodesItem: null is not a valid object")
	}
	type FulfillmentFulfillmentVariantBarcodesItemAlias FulfillmentFulfillmentVariantBarcodesItem
	var named FulfillmentFulfillmentVariantBarcodesItemAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentVariantBarcodesItem(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"type", "value"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "type")
	delete(all, "value")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentVariantBarcodesItem) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentVariantBarcodesItemAlias FulfillmentFulfillmentVariantBarcodesItem
	named, err := json.Marshal(FulfillmentFulfillmentVariantBarcodesItemAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentVariantBarcodesItem) Validate() error {
	if v.present != nil {
		if !v.present["type"] {
			return errors.New("type: required property is missing")
		}
		if !v.present["value"] {
			return errors.New("value: required property is missing")
		}
	}
	return nil
}

// FulfillmentFulfillmentVariantSeller Optional seller context for this variant.
type FulfillmentFulfillmentVariantSeller struct {
	// Seller policy and information links.
	Links []commontypes.Link `json:"links,omitzero"`
	// Seller display name.
	Name *string `json:"name,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentFulfillmentVariantSeller) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentFulfillmentVariantSeller: null is not a valid object")
	}
	type FulfillmentFulfillmentVariantSellerAlias FulfillmentFulfillmentVariantSeller
	var named FulfillmentFulfillmentVariantSellerAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentFulfillmentVariantSeller(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "links")
	delete(all, "name")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentFulfillmentVariantSeller) MarshalJSON() ([]byte, error) {
	type FulfillmentFulfillmentVariantSellerAlias FulfillmentFulfillmentVariantSeller
	named, err := json.Marshal(FulfillmentFulfillmentVariantSellerAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentFulfillmentVariantSeller) Validate() error {
	for i := range v.Links {
		if err := v.Links[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}
