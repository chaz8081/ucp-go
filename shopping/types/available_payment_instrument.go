// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/available_payment_instrument.json (spec release/2026-04-08@a2d8bf0)

package types

import (
	"encoding/json"
	"errors"
)

// AvailablePaymentInstrument An instrument type available from a payment handler with optional constraints.
type AvailablePaymentInstrument struct {
	// Constraints on this instrument type. Structure depends on instrument type and active capabilities.
	Constraints map[string]any `json:"constraints,omitzero"`
	// The instrument type identifier (e.g., 'card', 'gift_card'). References an instrument schema's type constant.
	Type string `json:"type"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *AvailablePaymentInstrument) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("AvailablePaymentInstrument: null is not a valid object")
	}
	type AvailablePaymentInstrumentAlias AvailablePaymentInstrument
	var named AvailablePaymentInstrumentAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = AvailablePaymentInstrument(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"type"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "constraints")
	delete(all, "type")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v AvailablePaymentInstrument) MarshalJSON() ([]byte, error) {
	type AvailablePaymentInstrumentAlias AvailablePaymentInstrument
	named, err := json.Marshal(AvailablePaymentInstrumentAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *AvailablePaymentInstrument) Validate() error {
	if v.present != nil {
		if !v.present["type"] {
			return errors.New("type: required property is missing")
		}
	}
	if v.Constraints != nil && len(v.Constraints) < 1 {
		return errors.New("constraints: has fewer than minProperties 1")
	}
	return nil
}
