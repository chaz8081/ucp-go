// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/binding.json (spec release/2026-04-08@a2d8bf0)

package types

import (
	"encoding/json"
	"errors"
)

// Binding Binds a token to a specific checkout session and participant. Prevents token reuse across different checkouts or participants.
type Binding struct {
	// The checkout session identifier this token is bound to.
	CheckoutID string `json:"checkout_id"`
	// The participant this token is bound to. Required when acting on behalf of another participant (e.g., agent tokenizing for merchant). Omit when the authenticated caller is the binding target.
	Identity *PaymentIdentity `json:"identity,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *Binding) UnmarshalJSON(data []byte) error {
	type BindingAlias Binding
	var named BindingAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = Binding(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"checkout_id"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "checkout_id")
	delete(all, "identity")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v Binding) MarshalJSON() ([]byte, error) {
	type BindingAlias Binding
	named, err := json.Marshal(BindingAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *Binding) Validate() error {
	if v.present != nil {
		if !v.present["checkout_id"] {
			return errors.New("checkout_id: required property is missing")
		}
	}
	if v.Identity != nil {
		if err := v.Identity.Validate(); err != nil {
			return err
		}
	}
	return nil
}
