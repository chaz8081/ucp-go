// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/business_fulfillment_config.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
)

// BusinessFulfillmentConfig Business's fulfillment configuration.
type BusinessFulfillmentConfig struct {
	// Method-type combinations the business permits within one cart. Each inner array is a permitted set of method `type` values (e.g. shipping + pickup).
	MethodCombinations [][]string `json:"method_combinations,omitzero"`
	// Method types that permit multiple destinations within one cart (e.g. split shipping across addresses). Listing a method permits it; an omitted method does not. Open — businesses MAY list any method type.
	MultiDestination []BusinessFulfillmentConfigMultiDestinationItem `json:"multi_destination,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *BusinessFulfillmentConfig) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("BusinessFulfillmentConfig: null is not a valid object")
	}
	type BusinessFulfillmentConfigAlias BusinessFulfillmentConfig
	var named BusinessFulfillmentConfigAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = BusinessFulfillmentConfig(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "method_combinations")
	delete(all, "multi_destination")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v BusinessFulfillmentConfig) MarshalJSON() ([]byte, error) {
	type BusinessFulfillmentConfigAlias BusinessFulfillmentConfig
	named, err := json.Marshal(BusinessFulfillmentConfigAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *BusinessFulfillmentConfig) Validate() error {
	for i := range v.MultiDestination {
		if err := v.MultiDestination[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// BusinessFulfillmentConfigMultiDestinationItem is generated from shopping/types/business_fulfillment_config.json.
type BusinessFulfillmentConfigMultiDestinationItem struct {
	// Fulfillment method type (e.g. `shipping`, `pickup`). Optional per-method constraints MAY be added alongside.
	Method string `json:"method"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *BusinessFulfillmentConfigMultiDestinationItem) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("BusinessFulfillmentConfigMultiDestinationItem: null is not a valid object")
	}
	type BusinessFulfillmentConfigMultiDestinationItemAlias BusinessFulfillmentConfigMultiDestinationItem
	var named BusinessFulfillmentConfigMultiDestinationItemAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = BusinessFulfillmentConfigMultiDestinationItem(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"method"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "method")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v BusinessFulfillmentConfigMultiDestinationItem) MarshalJSON() ([]byte, error) {
	type BusinessFulfillmentConfigMultiDestinationItemAlias BusinessFulfillmentConfigMultiDestinationItem
	named, err := json.Marshal(BusinessFulfillmentConfigMultiDestinationItemAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *BusinessFulfillmentConfigMultiDestinationItem) Validate() error {
	if v.present != nil {
		if !v.present["method"] {
			return errors.New("method: required property is missing")
		}
	}
	return nil
}
