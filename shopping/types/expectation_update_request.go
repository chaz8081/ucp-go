// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/expectation_update_request.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
	commontypes "github.com/chaz8081/ucp-go/common/types"
)

// ExpectationUpdateRequest Buyer-facing fulfillment expectation representing logical groupings of items (e.g., 'package'). Can be split, merged, or adjusted post-order to set buyer expectations for when/how items arrive.
type ExpectationUpdateRequest struct {
	// Human-readable delivery description (e.g., 'Arrives in 5-8 business days').
	Description *string `json:"description,omitzero"`
	// Delivery destination address.
	Destination commontypes.PostalAddressUpdateRequest `json:"destination"`
	// When this expectation can be fulfilled: 'now' or ISO 8601 timestamp for future date (backorder, pre-order).
	FulfillableOn *string `json:"fulfillable_on,omitzero"`
	// Expectation identifier.
	ID string `json:"id"`
	// Which line items and quantities are in this expectation.
	LineItems []ExpectationUpdateRequestLineItemsItem `json:"line_items"`
	// Delivery method type. Well-known values: `shipping`, `pickup`, `digital`; additional values MAY be used.
	MethodType string `json:"method_type"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *ExpectationUpdateRequest) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("ExpectationUpdateRequest: null is not a valid object")
	}
	type ExpectationUpdateRequestAlias ExpectationUpdateRequest
	var named ExpectationUpdateRequestAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = ExpectationUpdateRequest(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 4)
	for _, name := range []string{"destination", "id", "line_items", "method_type"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "description")
	delete(all, "destination")
	delete(all, "fulfillable_on")
	delete(all, "id")
	delete(all, "line_items")
	delete(all, "method_type")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v ExpectationUpdateRequest) MarshalJSON() ([]byte, error) {
	type ExpectationUpdateRequestAlias ExpectationUpdateRequest
	named, err := json.Marshal(ExpectationUpdateRequestAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *ExpectationUpdateRequest) Validate() error {
	if v.present != nil {
		if !v.present["destination"] {
			return errors.New("destination: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["line_items"] {
			return errors.New("line_items: required property is missing")
		}
		if !v.present["method_type"] {
			return errors.New("method_type: required property is missing")
		}
	}
	if err := v.Destination.Validate(); err != nil {
		return err
	}
	for i := range v.LineItems {
		if err := v.LineItems[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// ExpectationUpdateRequestLineItemsItem is generated from shopping/types/expectation_update_request.json.
type ExpectationUpdateRequestLineItemsItem struct {
	// Line item ID reference.
	ID string `json:"id"`
	// Integer count of steps of the referenced line item's `quantity_unit` (`10^-scale` × `unit`); when `quantity_unit` is absent, it counts whole items (`each`).
	Quantity int64 `json:"quantity"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *ExpectationUpdateRequestLineItemsItem) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("ExpectationUpdateRequestLineItemsItem: null is not a valid object")
	}
	type ExpectationUpdateRequestLineItemsItemAlias ExpectationUpdateRequestLineItemsItem
	var named ExpectationUpdateRequestLineItemsItemAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = ExpectationUpdateRequestLineItemsItem(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"id", "quantity"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "id")
	delete(all, "quantity")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v ExpectationUpdateRequestLineItemsItem) MarshalJSON() ([]byte, error) {
	type ExpectationUpdateRequestLineItemsItemAlias ExpectationUpdateRequestLineItemsItem
	named, err := json.Marshal(ExpectationUpdateRequestLineItemsItemAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *ExpectationUpdateRequestLineItemsItem) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["quantity"] {
			return errors.New("quantity: required property is missing")
		}
	}
	if v.Quantity > 9007199254740991 {
		return errors.New("quantity: above maximum 9007199254740991")
	}
	if v.Quantity < 1 {
		return errors.New("quantity: below minimum 1")
	}
	return nil
}
