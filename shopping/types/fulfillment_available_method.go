// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/fulfillment_available_method.json (spec release/2026-04-08@a2d8bf0)

package types

import (
	"encoding/json"
	"errors"
)

// FulfillmentAvailableMethod Inventory availability hint for a fulfillment method type.
type FulfillmentAvailableMethod struct {
	// Human-readable availability info (e.g., 'Available for pickup at Downtown Store today').
	Description *string `json:"description,omitzero"`
	// 'now' for immediate availability, or ISO 8601 date for future (preorders, transfers).
	FulfillableOn **string `json:"fulfillable_on,omitzero"`
	// Line items available for this fulfillment method.
	LineItemIds []string `json:"line_item_ids"`
	// Fulfillment method type this availability applies to.
	Type string `json:"type"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentAvailableMethod) UnmarshalJSON(data []byte) error {
	type FulfillmentAvailableMethodAlias FulfillmentAvailableMethod
	var named FulfillmentAvailableMethodAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentAvailableMethod(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"line_item_ids", "type"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "description")
	delete(all, "fulfillable_on")
	delete(all, "line_item_ids")
	delete(all, "type")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentAvailableMethod) MarshalJSON() ([]byte, error) {
	type FulfillmentAvailableMethodAlias FulfillmentAvailableMethod
	named, err := json.Marshal(FulfillmentAvailableMethodAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentAvailableMethod) Validate() error {
	if v.present != nil {
		if !v.present["line_item_ids"] {
			return errors.New("line_item_ids: required property is missing")
		}
		if !v.present["type"] {
			return errors.New("type: required property is missing")
		}
	}
	if v.Type != "shipping" && v.Type != "pickup" {
		return errors.New("type: not one of the permitted values")
	}
	return nil
}
