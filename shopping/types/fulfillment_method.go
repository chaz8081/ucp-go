// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/fulfillment_method.json (spec release/2026-04-08@a2d8bf0)

package types

import (
	"encoding/json"
	"errors"
)

// FulfillmentMethod A fulfillment method (shipping or pickup) with destinations and groups.
type FulfillmentMethod struct {
	// Available destinations. For shipping: addresses. For pickup: retail locations.
	Destinations []FulfillmentDestination `json:"destinations,omitzero"`
	// Fulfillment groups for selecting options. Agent sets selected_option_id on groups to choose shipping method.
	Groups []FulfillmentGroup `json:"groups,omitzero"`
	// Unique fulfillment method identifier.
	ID string `json:"id"`
	// Line item IDs fulfilled via this method.
	LineItemIds []string `json:"line_item_ids"`
	// ID of the selected destination.
	SelectedDestinationID **string `json:"selected_destination_id,omitzero"`
	// Fulfillment method type.
	Type string `json:"type"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentMethod) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentMethod: null is not a valid object")
	}
	type FulfillmentMethodAlias FulfillmentMethod
	var named FulfillmentMethodAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentMethod(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 3)
	for _, name := range []string{"id", "line_item_ids", "type"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "destinations")
	delete(all, "groups")
	delete(all, "id")
	delete(all, "line_item_ids")
	delete(all, "selected_destination_id")
	delete(all, "type")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentMethod) MarshalJSON() ([]byte, error) {
	type FulfillmentMethodAlias FulfillmentMethod
	named, err := json.Marshal(FulfillmentMethodAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentMethod) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["line_item_ids"] {
			return errors.New("line_item_ids: required property is missing")
		}
		if !v.present["type"] {
			return errors.New("type: required property is missing")
		}
	}
	if v.Type != "shipping" && v.Type != "pickup" {
		return errors.New("type: not one of the permitted values")
	}
	for i := range v.Destinations {
		if err := v.Destinations[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Groups {
		if err := v.Groups[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}
