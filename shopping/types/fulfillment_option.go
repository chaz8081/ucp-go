// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/fulfillment_option.json (spec release/2026-04-08@a2d8bf0)

package types

import (
	"encoding/json"
	"errors"
)

// FulfillmentOption A fulfillment option within a group (e.g., Standard Shipping $5, Express $15).
type FulfillmentOption struct {
	// Carrier name (for shipping).
	Carrier *string `json:"carrier,omitzero"`
	// Complete context for buyer decision (e.g., 'Arrives Dec 12-15 via FedEx').
	Description *string `json:"description,omitzero"`
	// Earliest fulfillment date.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	EarliestFulfillmentTime *string `json:"earliest_fulfillment_time,omitzero"`
	// Unique fulfillment option identifier.
	ID string `json:"id"`
	// Latest fulfillment date.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	LatestFulfillmentTime *string `json:"latest_fulfillment_time,omitzero"`
	// Short label (e.g., 'Express Shipping', 'Curbside Pickup').
	Title string `json:"title"`
	// Fulfillment option totals breakdown.
	Totals []Total `json:"totals"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentOption) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentOption: null is not a valid object")
	}
	type FulfillmentOptionAlias FulfillmentOption
	var named FulfillmentOptionAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentOption(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 3)
	for _, name := range []string{"id", "title", "totals"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "carrier")
	delete(all, "description")
	delete(all, "earliest_fulfillment_time")
	delete(all, "id")
	delete(all, "latest_fulfillment_time")
	delete(all, "title")
	delete(all, "totals")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentOption) MarshalJSON() ([]byte, error) {
	type FulfillmentOptionAlias FulfillmentOption
	named, err := json.Marshal(FulfillmentOptionAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentOption) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["title"] {
			return errors.New("title: required property is missing")
		}
		if !v.present["totals"] {
			return errors.New("totals: required property is missing")
		}
	}
	for i := range v.Totals {
		if err := v.Totals[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}
