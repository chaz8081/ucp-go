// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/fulfillment_option_base.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
	commontypes "github.com/chaz8081/ucp-go/common/types"
)

// FulfillmentOptionBase Common base for a fulfillment option: an addressable, renderable choice (e.g. Standard, Express). Catalog uses this base directly; checkout composes it with cost and timing.
type FulfillmentOptionBase struct {
	// Supplementary context for the title (e.g. 'Arrives in 4 business days', 'Arrives Dec 12-15 via FedEx'). Directly renderable; MUST NOT repeat the title.
	Description *commontypes.Description `json:"description,omitzero"`
	// Unique identifier for this fulfillment option.
	ID string `json:"id"`
	// Short label that distinguishes this option from its siblings (e.g. 'Standard', 'Express Shipping', 'Curbside Pickup').
	Title string `json:"title"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *FulfillmentOptionBase) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("FulfillmentOptionBase: null is not a valid object")
	}
	type FulfillmentOptionBaseAlias FulfillmentOptionBase
	var named FulfillmentOptionBaseAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = FulfillmentOptionBase(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"id", "title"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "description")
	delete(all, "id")
	delete(all, "title")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v FulfillmentOptionBase) MarshalJSON() ([]byte, error) {
	type FulfillmentOptionBaseAlias FulfillmentOptionBase
	named, err := json.Marshal(FulfillmentOptionBaseAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *FulfillmentOptionBase) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["title"] {
			return errors.New("title: required property is missing")
		}
	}
	if v.Description != nil {
		if err := v.Description.Validate(); err != nil {
			return err
		}
	}
	return nil
}
