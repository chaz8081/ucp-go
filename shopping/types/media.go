// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/media.json (spec release/2026-04-08@a2d8bf0)

package types

import (
	"encoding/json"
	"errors"
)

// Media Product media item (image, video, etc.).
type Media struct {
	// Accessibility text describing the media.
	AltText *string `json:"alt_text,omitzero"`
	// Height in pixels (for images/video).
	Height *int64 `json:"height,omitzero"`
	// Media type. Well-known values: `image`, `video`, `model_3d`.
	Type string `json:"type"`
	// URL to the media resource.
	//
	// Not enforced yet (phase 4): format.
	URL string `json:"url"`
	// Width in pixels (for images/video).
	Width *int64 `json:"width,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *Media) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("Media: null is not a valid object")
	}
	type MediaAlias Media
	var named MediaAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = Media(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"type", "url"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "alt_text")
	delete(all, "height")
	delete(all, "type")
	delete(all, "url")
	delete(all, "width")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v Media) MarshalJSON() ([]byte, error) {
	type MediaAlias Media
	named, err := json.Marshal(MediaAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *Media) Validate() error {
	if v.present != nil {
		if !v.present["type"] {
			return errors.New("type: required property is missing")
		}
		if !v.present["url"] {
			return errors.New("url: required property is missing")
		}
	}
	if v.Height != nil && *v.Height < 1 {
		return errors.New("height: below minimum 1")
	}
	if v.Width != nil && *v.Width < 1 {
		return errors.New("width: below minimum 1")
	}
	return nil
}
