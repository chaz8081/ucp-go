// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/order_line_item_update_request.json (spec release/2026-08-25@cd78fb3)

package types

import (
	"encoding/json"
	"errors"
	commontypes "github.com/chaz8081/ucp-go/common/types"
)

// OrderLineItemUpdateRequest is generated from shopping/types/order_line_item_update_request.json.
type OrderLineItemUpdateRequest struct {
	// Line item identifier.
	ID string `json:"id"`
	// Purchased item data, including identity, price, and sale basis.
	Item ItemUpdateRequest `json:"item"`
	// Parent line item identifier for any nested structures.
	ParentID *string `json:"parent_id,omitzero"`
	// Tracks the line item's original, current active, and fulfilled quantities. All three values use the same inherited `item.quantity_unit`. When `item.quantity_unit` is absent on an authoritative order response, each step is one whole item (`each`) under the shared default.
	Quantity OrderLineItemUpdateRequestQuantity `json:"quantity"`
	// Derived status: removed if quantity.total == 0, fulfilled if quantity.total > 0 and quantity.fulfilled == quantity.total, partial if quantity.total > 0 and quantity.fulfilled > 0, otherwise processing.
	Status string `json:"status"`
	// Line item totals breakdown.
	Totals []commontypes.TotalUpdateRequest `json:"totals"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *OrderLineItemUpdateRequest) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("OrderLineItemUpdateRequest: null is not a valid object")
	}
	type OrderLineItemUpdateRequestAlias OrderLineItemUpdateRequest
	var named OrderLineItemUpdateRequestAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = OrderLineItemUpdateRequest(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 5)
	for _, name := range []string{"id", "item", "quantity", "status", "totals"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "id")
	delete(all, "item")
	delete(all, "parent_id")
	delete(all, "quantity")
	delete(all, "status")
	delete(all, "totals")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v OrderLineItemUpdateRequest) MarshalJSON() ([]byte, error) {
	type OrderLineItemUpdateRequestAlias OrderLineItemUpdateRequest
	named, err := json.Marshal(OrderLineItemUpdateRequestAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *OrderLineItemUpdateRequest) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["item"] {
			return errors.New("item: required property is missing")
		}
		if !v.present["quantity"] {
			return errors.New("quantity: required property is missing")
		}
		if !v.present["status"] {
			return errors.New("status: required property is missing")
		}
		if !v.present["totals"] {
			return errors.New("totals: required property is missing")
		}
	}
	if v.Status != "processing" && v.Status != "partial" && v.Status != "fulfilled" && v.Status != "removed" {
		return errors.New("status: not one of the permitted values")
	}
	if err := v.Item.Validate(); err != nil {
		return err
	}
	if err := v.Quantity.Validate(); err != nil {
		return err
	}
	for i := range v.Totals {
		if err := v.Totals[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// OrderLineItemUpdateRequestQuantity Tracks the line item's original, current active, and fulfilled quantities. All three values use the same inherited `item.quantity_unit`. When `item.quantity_unit` is absent on an authoritative order response, each step is one whole item (`each`) under the shared default.
type OrderLineItemUpdateRequestQuantity struct {
	// Quantity fulfilled so far, expressed as an integer step count.
	Fulfilled int64 `json:"fulfilled"`
	// Quantity from the original checkout, expressed as an integer step count.
	Original *int64 `json:"original,omitzero"`
	// Current active quantity after returns, cancellations, or other order changes, expressed as an integer step count.
	Total int64 `json:"total"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *OrderLineItemUpdateRequestQuantity) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("OrderLineItemUpdateRequestQuantity: null is not a valid object")
	}
	type OrderLineItemUpdateRequestQuantityAlias OrderLineItemUpdateRequestQuantity
	var named OrderLineItemUpdateRequestQuantityAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = OrderLineItemUpdateRequestQuantity(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"fulfilled", "total"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "fulfilled")
	delete(all, "original")
	delete(all, "total")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v OrderLineItemUpdateRequestQuantity) MarshalJSON() ([]byte, error) {
	type OrderLineItemUpdateRequestQuantityAlias OrderLineItemUpdateRequestQuantity
	named, err := json.Marshal(OrderLineItemUpdateRequestQuantityAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *OrderLineItemUpdateRequestQuantity) Validate() error {
	if v.present != nil {
		if !v.present["fulfilled"] {
			return errors.New("fulfilled: required property is missing")
		}
		if !v.present["total"] {
			return errors.New("total: required property is missing")
		}
	}
	if v.Fulfilled > 9007199254740991 {
		return errors.New("fulfilled: above maximum 9007199254740991")
	}
	if v.Fulfilled < 0 {
		return errors.New("fulfilled: below minimum 0")
	}
	if v.Original != nil && *v.Original > 9007199254740991 {
		return errors.New("original: above maximum 9007199254740991")
	}
	if v.Original != nil && *v.Original < 0 {
		return errors.New("original: below minimum 0")
	}
	if v.Total > 9007199254740991 {
		return errors.New("total: above maximum 9007199254740991")
	}
	if v.Total < 0 {
		return errors.New("total: below minimum 0")
	}
	return nil
}
