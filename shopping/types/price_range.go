// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/price_range.json (spec release/2026-04-08@a2d8bf0)

package types

import (
	"encoding/json"
	"errors"
)

// PriceRange A price range representing minimum and maximum values (e.g., across product variants).
type PriceRange struct {
	// Maximum price in the range.
	Max Price `json:"max"`
	// Minimum price in the range.
	Min Price `json:"min"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *PriceRange) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("PriceRange: null is not a valid object")
	}
	type PriceRangeAlias PriceRange
	var named PriceRangeAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = PriceRange(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"max", "min"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "max")
	delete(all, "min")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v PriceRange) MarshalJSON() ([]byte, error) {
	type PriceRangeAlias PriceRange
	named, err := json.Marshal(PriceRangeAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *PriceRange) Validate() error {
	if v.present != nil {
		if !v.present["max"] {
			return errors.New("max: required property is missing")
		}
		if !v.present["min"] {
			return errors.New("min: required property is missing")
		}
	}
	if err := v.Max.Validate(); err != nil {
		return err
	}
	if err := v.Min.Validate(); err != nil {
		return err
	}
	return nil
}
