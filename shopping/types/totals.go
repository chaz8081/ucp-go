// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/totals.json (spec release/2026-04-08@a2d8bf0)

package types

import (
	"encoding/json"
	"errors"
)

// Totals Pricing breakdown provided by the business. MUST contain exactly one subtotal and one total entry. Detail types (tax, fee, discount, fulfillment) may appear multiple times for itemization. Platforms MUST render all entries in order using display_text and amount.
//
// Not enforced yet (phase 4): contains, maxContains, minContains.
type Totals []TotalsTotalsItem

// Validate reports the first constraint violation, or nil.
func (v *Totals) Validate() error {
	return nil
}

// TotalsTotalsItem is generated from shopping/types/totals.json.
//
// Not enforced yet (phase 4) on the object itself: if, then.
type TotalsTotalsItem struct {
	// Optional itemized breakdown. The parent entry is always rendered; lines are supplementary. Sum of line amounts MUST equal the parent entry amount.
	Lines []TotalsTotalsItemLinesItem `json:"lines,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *TotalsTotalsItem) UnmarshalJSON(data []byte) error {
	type TotalsTotalsItemAlias TotalsTotalsItem
	var named TotalsTotalsItemAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = TotalsTotalsItem(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "lines")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v TotalsTotalsItem) MarshalJSON() ([]byte, error) {
	type TotalsTotalsItemAlias TotalsTotalsItem
	named, err := json.Marshal(TotalsTotalsItemAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *TotalsTotalsItem) Validate() error {
	for i := range v.Lines {
		if err := v.Lines[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// TotalsTotalsItemLinesItem Sub-line entry. Additional metadata MAY be included.
type TotalsTotalsItemLinesItem struct {
	Amount SignedAmount `json:"amount"`
	// Human-readable label for this sub-line.
	DisplayText string `json:"display_text"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *TotalsTotalsItemLinesItem) UnmarshalJSON(data []byte) error {
	type TotalsTotalsItemLinesItemAlias TotalsTotalsItemLinesItem
	var named TotalsTotalsItemLinesItemAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = TotalsTotalsItemLinesItem(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"amount", "display_text"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "amount")
	delete(all, "display_text")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v TotalsTotalsItemLinesItem) MarshalJSON() ([]byte, error) {
	type TotalsTotalsItemLinesItemAlias TotalsTotalsItemLinesItem
	named, err := json.Marshal(TotalsTotalsItemLinesItemAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *TotalsTotalsItemLinesItem) Validate() error {
	if v.present != nil {
		if !v.present["amount"] {
			return errors.New("amount: required property is missing")
		}
		if !v.present["display_text"] {
			return errors.New("display_text: required property is missing")
		}
	}
	if err := v.Amount.Validate(); err != nil {
		return err
	}
	return nil
}
