// Code generated by ucpgen. DO NOT EDIT.
// Source: shopping/types/variant.json (spec release/2026-04-08@a2d8bf0)

package types

import (
	"encoding/json"
	"errors"
	"regexp"
	"sync"
)

// Variant A purchasable variant of a product with specific option selections.
type Variant struct {
	// Variant availability for purchase.
	Availability *VariantAvailability `json:"availability,omitzero"`
	// Industry-standard product identifiers for cross-reference and correlation.
	Barcodes []VariantBarcodesItem `json:"barcodes,omitzero"`
	// Variant categories with optional taxonomy identifiers.
	Categories []Category `json:"categories,omitzero"`
	// Variant description in one or more formats.
	Description Description `json:"description"`
	// URL-safe variant handle/slug.
	Handle *string `json:"handle,omitzero"`
	// Global ID (GID) uniquely identifying this variant. Used as item.id in checkout.
	ID string `json:"id"`
	// List price before discounts (for strikethrough display).
	ListPrice *Price `json:"list_price,omitzero"`
	// Variant media (images, videos, 3D models). First item is the featured media for listings.
	Media []Media `json:"media,omitzero"`
	// Business-defined custom data extending the standard variant model.
	Metadata map[string]any `json:"metadata,omitzero"`
	// Option values that define this variant (e.g., Color: Blue, Size: Large).
	Options []SelectedOption `json:"options,omitzero"`
	// Current selling price.
	Price Price `json:"price"`
	// Variant rating.
	Rating *Rating `json:"rating,omitzero"`
	// Optional seller context for this variant.
	Seller *VariantSeller `json:"seller,omitzero"`
	// Business-assigned identifier for inventory and fulfillment.
	SKU *string `json:"sku,omitzero"`
	// Variant tags for categorization and search.
	Tags []string `json:"tags,omitzero"`
	// Variant display title (e.g., 'Blue / Large').
	Title string `json:"title"`
	// Price per standard unit of measurement. MAY be omitted when unit pricing does not apply.
	UnitPrice *VariantUnitPrice `json:"unit_price,omitzero"`
	// Canonical variant page URL.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	URL *string `json:"url,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *Variant) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("Variant: null is not a valid object")
	}
	type VariantAlias Variant
	var named VariantAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = Variant(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 4)
	for _, name := range []string{"description", "id", "price", "title"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "availability")
	delete(all, "barcodes")
	delete(all, "categories")
	delete(all, "description")
	delete(all, "handle")
	delete(all, "id")
	delete(all, "list_price")
	delete(all, "media")
	delete(all, "metadata")
	delete(all, "options")
	delete(all, "price")
	delete(all, "rating")
	delete(all, "seller")
	delete(all, "sku")
	delete(all, "tags")
	delete(all, "title")
	delete(all, "unit_price")
	delete(all, "url")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v Variant) MarshalJSON() ([]byte, error) {
	type VariantAlias Variant
	named, err := json.Marshal(VariantAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *Variant) Validate() error {
	if v.present != nil {
		if !v.present["description"] {
			return errors.New("description: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["price"] {
			return errors.New("price: required property is missing")
		}
		if !v.present["title"] {
			return errors.New("title: required property is missing")
		}
	}
	if v.Availability != nil {
		if err := v.Availability.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Barcodes {
		if err := v.Barcodes[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Categories {
		if err := v.Categories[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Description.Validate(); err != nil {
		return err
	}
	if v.ListPrice != nil {
		if err := v.ListPrice.Validate(); err != nil {
			return err
		}
	}
	for i := range v.Media {
		if err := v.Media[i].Validate(); err != nil {
			return err
		}
	}
	for i := range v.Options {
		if err := v.Options[i].Validate(); err != nil {
			return err
		}
	}
	if err := v.Price.Validate(); err != nil {
		return err
	}
	if v.Rating != nil {
		if err := v.Rating.Validate(); err != nil {
			return err
		}
	}
	if v.Seller != nil {
		if err := v.Seller.Validate(); err != nil {
			return err
		}
	}
	if v.UnitPrice != nil {
		if err := v.UnitPrice.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// VariantAvailability Variant availability for purchase.
type VariantAvailability struct {
	// Whether this variant can be purchased. See status for fulfillment details.
	Available *bool `json:"available,omitzero"`
	// Qualifies available with fulfillment state. Well-known values: `in_stock`, `backorder`, `preorder`, `out_of_stock`, `discontinued`.
	Status *string `json:"status,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *VariantAvailability) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("VariantAvailability: null is not a valid object")
	}
	type VariantAvailabilityAlias VariantAvailability
	var named VariantAvailabilityAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = VariantAvailability(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "available")
	delete(all, "status")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v VariantAvailability) MarshalJSON() ([]byte, error) {
	type VariantAvailabilityAlias VariantAvailability
	named, err := json.Marshal(VariantAvailabilityAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *VariantAvailability) Validate() error {
	return nil
}

// VariantBarcodesItem is generated from shopping/types/variant.json.
type VariantBarcodesItem struct {
	// Barcode standard. Well-known values: UPC, EAN, ISBN, GTIN, JAN.
	Type string `json:"type"`
	// Barcode value.
	Value string `json:"value"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *VariantBarcodesItem) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("VariantBarcodesItem: null is not a valid object")
	}
	type VariantBarcodesItemAlias VariantBarcodesItem
	var named VariantBarcodesItemAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = VariantBarcodesItem(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"type", "value"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "type")
	delete(all, "value")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v VariantBarcodesItem) MarshalJSON() ([]byte, error) {
	type VariantBarcodesItemAlias VariantBarcodesItem
	named, err := json.Marshal(VariantBarcodesItemAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *VariantBarcodesItem) Validate() error {
	if v.present != nil {
		if !v.present["type"] {
			return errors.New("type: required property is missing")
		}
		if !v.present["value"] {
			return errors.New("value: required property is missing")
		}
	}
	return nil
}

// VariantSeller Optional seller context for this variant.
type VariantSeller struct {
	// Seller policy and information links.
	Links []Link `json:"links,omitzero"`
	// Seller display name.
	Name *string `json:"name,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *VariantSeller) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("VariantSeller: null is not a valid object")
	}
	type VariantSellerAlias VariantSeller
	var named VariantSellerAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = VariantSeller(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "links")
	delete(all, "name")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v VariantSeller) MarshalJSON() ([]byte, error) {
	type VariantSellerAlias VariantSeller
	named, err := json.Marshal(VariantSellerAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *VariantSeller) Validate() error {
	for i := range v.Links {
		if err := v.Links[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}

// VariantUnitPrice Price per standard unit of measurement. MAY be omitted when unit pricing does not apply.
type VariantUnitPrice struct {
	// Unit price in ISO 4217 minor units. Business MUST return precomputed unit price value: (variant.price / measure.value) * reference.value.
	Amount Amount `json:"amount"`
	// ISO 4217 currency code.
	Currency string `json:"currency"`
	// Product quantity in packaging (e.g., 750ml bottle).
	Measure VariantUnitPriceMeasure `json:"measure"`
	// Denominator for unit price display (e.g., per 100ml, per 1kg).
	Reference VariantUnitPriceReference `json:"reference"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *VariantUnitPrice) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("VariantUnitPrice: null is not a valid object")
	}
	type VariantUnitPriceAlias VariantUnitPrice
	var named VariantUnitPriceAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = VariantUnitPrice(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 4)
	for _, name := range []string{"amount", "currency", "measure", "reference"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "amount")
	delete(all, "currency")
	delete(all, "measure")
	delete(all, "reference")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v VariantUnitPrice) MarshalJSON() ([]byte, error) {
	type VariantUnitPriceAlias VariantUnitPrice
	named, err := json.Marshal(VariantUnitPriceAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

var pattern_VariantUnitPrice_Currency = sync.OnceValue(func() *regexp.Regexp { return regexp.MustCompile("^[A-Z]{3}$") })

// Validate reports the first constraint violation, or nil.
func (v *VariantUnitPrice) Validate() error {
	if v.present != nil {
		if !v.present["amount"] {
			return errors.New("amount: required property is missing")
		}
		if !v.present["currency"] {
			return errors.New("currency: required property is missing")
		}
		if !v.present["measure"] {
			return errors.New("measure: required property is missing")
		}
		if !v.present["reference"] {
			return errors.New("reference: required property is missing")
		}
	}
	if !pattern_VariantUnitPrice_Currency().MatchString(v.Currency) {
		return errors.New("currency: does not match pattern")
	}
	if err := v.Amount.Validate(); err != nil {
		return err
	}
	if err := v.Measure.Validate(); err != nil {
		return err
	}
	if err := v.Reference.Validate(); err != nil {
		return err
	}
	return nil
}

// VariantUnitPriceMeasure Product quantity in packaging (e.g., 750ml bottle).
type VariantUnitPriceMeasure struct {
	// Unit of measurement.
	Unit string `json:"unit"`
	// Package quantity.
	Value float64 `json:"value"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *VariantUnitPriceMeasure) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("VariantUnitPriceMeasure: null is not a valid object")
	}
	type VariantUnitPriceMeasureAlias VariantUnitPriceMeasure
	var named VariantUnitPriceMeasureAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = VariantUnitPriceMeasure(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"unit", "value"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "unit")
	delete(all, "value")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v VariantUnitPriceMeasure) MarshalJSON() ([]byte, error) {
	type VariantUnitPriceMeasureAlias VariantUnitPriceMeasure
	named, err := json.Marshal(VariantUnitPriceMeasureAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *VariantUnitPriceMeasure) Validate() error {
	if v.present != nil {
		if !v.present["unit"] {
			return errors.New("unit: required property is missing")
		}
		if !v.present["value"] {
			return errors.New("value: required property is missing")
		}
	}
	return nil
}

// VariantUnitPriceReference Denominator for unit price display (e.g., per 100ml, per 1kg).
type VariantUnitPriceReference struct {
	// Unit of measurement.
	Unit string `json:"unit"`
	// Reference quantity.
	Value int64 `json:"value"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *VariantUnitPriceReference) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("VariantUnitPriceReference: null is not a valid object")
	}
	type VariantUnitPriceReferenceAlias VariantUnitPriceReference
	var named VariantUnitPriceReferenceAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = VariantUnitPriceReference(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"unit", "value"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "unit")
	delete(all, "value")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v VariantUnitPriceReference) MarshalJSON() ([]byte, error) {
	type VariantUnitPriceReferenceAlias VariantUnitPriceReference
	named, err := json.Marshal(VariantUnitPriceReferenceAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *VariantUnitPriceReference) Validate() error {
	if v.present != nil {
		if !v.present["unit"] {
			return errors.New("unit: required property is missing")
		}
		if !v.present["value"] {
			return errors.New("value: required property is missing")
		}
	}
	return nil
}
