// Code generated by ucpgen. DO NOT EDIT.
// Source: transports/embedded_config.json (spec release/2026-04-08@a2d8bf0)

package transports

import (
	"encoding/json"
	"errors"
)

// EmbeddedTransportConfig Per-session configuration for embedded transport binding. Allows businesses to vary EP availability and delegations based on cart contents, agent authorization, or policy.
type EmbeddedTransportConfig struct {
	// Color schemes the business supports. Hosts use ec_color_scheme query parameter to request a scheme from this list.
	ColorScheme []string `json:"color_scheme,omitzero"`
	// Delegations the business allows. At service-level, declares available delegations. In UCP responses, confirms accepted delegations for this session.
	Delegate []string `json:"delegate,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *EmbeddedTransportConfig) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("EmbeddedTransportConfig: null is not a valid object")
	}
	type EmbeddedTransportConfigAlias EmbeddedTransportConfig
	var named EmbeddedTransportConfigAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = EmbeddedTransportConfig(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "color_scheme")
	delete(all, "delegate")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v EmbeddedTransportConfig) MarshalJSON() ([]byte, error) {
	type EmbeddedTransportConfigAlias EmbeddedTransportConfig
	named, err := json.Marshal(EmbeddedTransportConfigAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *EmbeddedTransportConfig) Validate() error {
	if v.ColorScheme != nil {
		for _, k := range v.ColorScheme {
			if k != "light" && k != "dark" {
				return errors.New("color_scheme item: not one of the permitted values")
			}
		}
	}
	return nil
}
