// Code generated by ucpgen. DO NOT EDIT.
// Source: transports/jsonrpc.json (spec release/2026-08-25@cd78fb3)

package transports

import (
	"encoding/json"
	"errors"
	"unicode/utf8"
)

// JSONRpc20Envelope Common JSON-RPC 2.0 transport envelope used by UCP JSON-RPC-based bindings. This schema intentionally validates only the protocol envelope; binding-specific params and result payloads are validated by transport-specific schemas or extracted UCP payload schemas.
//
// JSONRpc20Envelope is a closed oneOf union: one field is set, holding the
// alternative the input matched. The schema requires that exactly one
// alternative match.
type JSONRpc20Envelope struct {
	JsonrpcErrorResponse   *JsonrpcErrorResponse   `json:"-"`
	JsonrpcRequest         *JsonrpcRequest         `json:"-"`
	JsonrpcSuccessResponse *JsonrpcSuccessResponse `json:"-"`

	// matched counts the alternatives the decoded input satisfied.
	// oneOf permits exactly one, so more than one is a violation that
	// only decoding can observe. Zero means this value was never
	// decoded from JSON.
	matched int
}

// UnmarshalJSON decodes the union member that accepts the input,
// preferring one that also validates.
func (v *JSONRpc20Envelope) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("JSONRpc20Envelope: null is not a valid object")
	}
	var matched, fallback JSONRpc20Envelope
	matches := 0
	parsed := false
	var asJsonrpcErrorResponse JsonrpcErrorResponse
	if err := json.Unmarshal(data, &asJsonrpcErrorResponse); err == nil {
		if asJsonrpcErrorResponse.Validate() == nil {
			if matches == 0 {
				matched = JSONRpc20Envelope{JsonrpcErrorResponse: &asJsonrpcErrorResponse}
			}
			matches++
		}
		if !parsed {
			fallback, parsed = JSONRpc20Envelope{JsonrpcErrorResponse: &asJsonrpcErrorResponse}, true
		}
	}
	var asJsonrpcRequest JsonrpcRequest
	if err := json.Unmarshal(data, &asJsonrpcRequest); err == nil {
		if asJsonrpcRequest.Validate() == nil {
			if matches == 0 {
				matched = JSONRpc20Envelope{JsonrpcRequest: &asJsonrpcRequest}
			}
			matches++
		}
		if !parsed {
			fallback, parsed = JSONRpc20Envelope{JsonrpcRequest: &asJsonrpcRequest}, true
		}
	}
	var asJsonrpcSuccessResponse JsonrpcSuccessResponse
	if err := json.Unmarshal(data, &asJsonrpcSuccessResponse); err == nil {
		if asJsonrpcSuccessResponse.Validate() == nil {
			if matches == 0 {
				matched = JSONRpc20Envelope{JsonrpcSuccessResponse: &asJsonrpcSuccessResponse}
			}
			matches++
		}
		if !parsed {
			fallback, parsed = JSONRpc20Envelope{JsonrpcSuccessResponse: &asJsonrpcSuccessResponse}, true
		}
	}
	if matches > 0 {
		*v = matched
		v.matched = matches
		return nil
	}
	if parsed {
		*v = fallback
		v.matched = 1
		return nil
	}
	return errors.New("JSONRpc20Envelope: no union member accepted the input")
}

// MarshalJSON encodes whichever union member is set.
func (v JSONRpc20Envelope) MarshalJSON() ([]byte, error) {
	if v.JsonrpcErrorResponse != nil {
		return json.Marshal(v.JsonrpcErrorResponse)
	}
	if v.JsonrpcRequest != nil {
		return json.Marshal(v.JsonrpcRequest)
	}
	if v.JsonrpcSuccessResponse != nil {
		return json.Marshal(v.JsonrpcSuccessResponse)
	}
	return nil, errors.New("JSONRpc20Envelope: no union member is set")
}

// Validate reports the first constraint violation, or nil.
func (v *JSONRpc20Envelope) Validate() error {
	if v.matched > 1 {
		return errors.New("JSONRpc20Envelope: input satisfies more than one alternative, and oneOf permits exactly one")
	}
	if v.JsonrpcErrorResponse != nil {
		return v.JsonrpcErrorResponse.Validate()
	}
	if v.JsonrpcRequest != nil {
		return v.JsonrpcRequest.Validate()
	}
	if v.JsonrpcSuccessResponse != nil {
		return v.JsonrpcSuccessResponse.Validate()
	}
	return nil
}

// JsonrpcError JSON-RPC transport-level error object. UCP business outcomes use result payloads with UCP messages instead of this object.
type JsonrpcError struct {
	// JSON-RPC error code. Standard codes are negative integers; UCP bindings reserve business errors for UCP messages.
	Code int64 `json:"code"`
	// Optional machine-readable transport error details.
	Data any `json:"data,omitzero"`
	// Short transport-level error description.
	Message string `json:"message"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *JsonrpcError) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("JsonrpcError: null is not a valid object")
	}
	type JsonrpcErrorAlias JsonrpcError
	var named JsonrpcErrorAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = JsonrpcError(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"code", "message"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "code")
	delete(all, "data")
	delete(all, "message")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v JsonrpcError) MarshalJSON() ([]byte, error) {
	type JsonrpcErrorAlias JsonrpcError
	named, err := json.Marshal(JsonrpcErrorAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *JsonrpcError) Validate() error {
	if v.present != nil {
		if !v.present["code"] {
			return errors.New("code: required property is missing")
		}
		if !v.present["message"] {
			return errors.New("message: required property is missing")
		}
	}
	return nil
}

// JsonrpcErrorResponse JSON-RPC transport error response envelope. This is for protocol-level failures, not UCP application-level messages.
type JsonrpcErrorResponse struct {
	Error JsonrpcError `json:"error"`
	ID    JsonrpcID    `json:"id"`
	// JSON-RPC protocol version.
	Jsonrpc string `json:"jsonrpc"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and records which of the
// required ones were present.
func (v *JsonrpcErrorResponse) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("JsonrpcErrorResponse: null is not a valid object")
	}
	type JsonrpcErrorResponseAlias JsonrpcErrorResponse
	var named JsonrpcErrorResponseAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = JsonrpcErrorResponse(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 3)
	for _, name := range []string{"error", "id", "jsonrpc"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	return nil
}

// Validate reports the first constraint violation, or nil.
func (v *JsonrpcErrorResponse) Validate() error {
	if v.present != nil {
		if !v.present["error"] {
			return errors.New("error: required property is missing")
		}
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["jsonrpc"] {
			return errors.New("jsonrpc: required property is missing")
		}
	}
	if v.Jsonrpc != "2.0" {
		return errors.New("jsonrpc: must be \"2.0\"")
	}
	if err := v.Error.Validate(); err != nil {
		return err
	}
	if err := v.ID.Validate(); err != nil {
		return err
	}
	return nil
}

// JsonrpcID JSON-RPC request identifier. Notifications omit id; responses echo the request id, or use null when the request id could not be determined.
//
// This schema is a oneOf of 3 alternatives with no shared
// properties, so it is carried as raw JSON. Typed alternatives are
// phase 4.
type JsonrpcID json.RawMessage

// Validate reports the first constraint violation, or nil.
func (v *JsonrpcID) Validate() error {
	return nil
}

// JsonrpcRequest JSON-RPC request or notification envelope. Presence of id makes the message a request; absence of id makes it a notification.
type JsonrpcRequest struct {
	ID *JsonrpcID `json:"id,omitzero"`
	// JSON-RPC protocol version.
	Jsonrpc string `json:"jsonrpc"`
	// Transport method name. Binding-specific schemas constrain the method namespace.
	Method string `json:"method"`
	// Method parameters. Binding-specific schemas define the object shape.
	Params json.RawMessage `json:"params,omitzero"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and records which of the
// required ones were present.
func (v *JsonrpcRequest) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("JsonrpcRequest: null is not a valid object")
	}
	type JsonrpcRequestAlias JsonrpcRequest
	var named JsonrpcRequestAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = JsonrpcRequest(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"jsonrpc", "method"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	return nil
}

// Validate reports the first constraint violation, or nil.
func (v *JsonrpcRequest) Validate() error {
	if v.present != nil {
		if !v.present["jsonrpc"] {
			return errors.New("jsonrpc: required property is missing")
		}
		if !v.present["method"] {
			return errors.New("method: required property is missing")
		}
	}
	if v.Jsonrpc != "2.0" {
		return errors.New("jsonrpc: must be \"2.0\"")
	}
	if utf8.RuneCountInString(string(v.Method)) < 1 {
		return errors.New("method: is shorter than minLength 1")
	}
	if v.ID != nil {
		if err := v.ID.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// JsonrpcSuccessResponse JSON-RPC success response envelope.
type JsonrpcSuccessResponse struct {
	ID JsonrpcID `json:"id"`
	// JSON-RPC protocol version.
	Jsonrpc string `json:"jsonrpc"`
	// Successful transport result. UCP bindings define the nested result payload.
	Result any `json:"result"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and records which of the
// required ones were present.
func (v *JsonrpcSuccessResponse) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("JsonrpcSuccessResponse: null is not a valid object")
	}
	type JsonrpcSuccessResponseAlias JsonrpcSuccessResponse
	var named JsonrpcSuccessResponseAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = JsonrpcSuccessResponse(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 3)
	for _, name := range []string{"id", "jsonrpc", "result"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	return nil
}

// Validate reports the first constraint violation, or nil.
func (v *JsonrpcSuccessResponse) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["jsonrpc"] {
			return errors.New("jsonrpc: required property is missing")
		}
		if !v.present["result"] {
			return errors.New("result: required property is missing")
		}
	}
	if v.Jsonrpc != "2.0" {
		return errors.New("jsonrpc: must be \"2.0\"")
	}
	if err := v.ID.Validate(); err != nil {
		return err
	}
	return nil
}
