// Code generated by ucpgen. DO NOT EDIT.
// Source: transports/mcp_tool_call.json (spec release/2026-08-25@cd78fb3)

package transports

import (
	"encoding/json"
	"errors"
	"unicode/utf8"
)

// MCPToolCallEnvelope UCP's MCP transport envelope for JSON-RPC tools/call messages. The schema validates the MCP mapping layer: operation name in params.name, UCP metadata and domain arguments in params.arguments, and UCP output in result.structuredContent.
//
// MCPToolCallEnvelope is a closed oneOf union: one field is set, holding the
// alternative the input matched. The schema requires that exactly one
// alternative match.
type MCPToolCallEnvelope struct {
	MCPToolCallRequest   *MCPToolCallRequest   `json:"-"`
	MCPToolCallResponse  *MCPToolCallResponse  `json:"-"`
	JsonrpcErrorResponse *JsonrpcErrorResponse `json:"-"`

	// matched counts the alternatives the decoded input satisfied.
	// oneOf permits exactly one, so more than one is a violation that
	// only decoding can observe. Zero means this value was never
	// decoded from JSON.
	matched int
}

// UnmarshalJSON decodes the union member that accepts the input,
// preferring one that also validates.
func (v *MCPToolCallEnvelope) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("MCPToolCallEnvelope: null is not a valid object")
	}
	var matched, fallback MCPToolCallEnvelope
	matches := 0
	parsed := false
	var asMCPToolCallRequest MCPToolCallRequest
	if err := json.Unmarshal(data, &asMCPToolCallRequest); err == nil {
		if asMCPToolCallRequest.Validate() == nil {
			if matches == 0 {
				matched = MCPToolCallEnvelope{MCPToolCallRequest: &asMCPToolCallRequest}
			}
			matches++
		}
		if !parsed {
			fallback, parsed = MCPToolCallEnvelope{MCPToolCallRequest: &asMCPToolCallRequest}, true
		}
	}
	var asMCPToolCallResponse MCPToolCallResponse
	if err := json.Unmarshal(data, &asMCPToolCallResponse); err == nil {
		if asMCPToolCallResponse.Validate() == nil {
			if matches == 0 {
				matched = MCPToolCallEnvelope{MCPToolCallResponse: &asMCPToolCallResponse}
			}
			matches++
		}
		if !parsed {
			fallback, parsed = MCPToolCallEnvelope{MCPToolCallResponse: &asMCPToolCallResponse}, true
		}
	}
	var asJsonrpcErrorResponse JsonrpcErrorResponse
	if err := json.Unmarshal(data, &asJsonrpcErrorResponse); err == nil {
		if asJsonrpcErrorResponse.Validate() == nil {
			if matches == 0 {
				matched = MCPToolCallEnvelope{JsonrpcErrorResponse: &asJsonrpcErrorResponse}
			}
			matches++
		}
		if !parsed {
			fallback, parsed = MCPToolCallEnvelope{JsonrpcErrorResponse: &asJsonrpcErrorResponse}, true
		}
	}
	if matches > 0 {
		*v = matched
		v.matched = matches
		return nil
	}
	if parsed {
		*v = fallback
		v.matched = 1
		return nil
	}
	return errors.New("MCPToolCallEnvelope: no union member accepted the input")
}

// MarshalJSON encodes whichever union member is set.
func (v MCPToolCallEnvelope) MarshalJSON() ([]byte, error) {
	if v.MCPToolCallRequest != nil {
		return json.Marshal(v.MCPToolCallRequest)
	}
	if v.MCPToolCallResponse != nil {
		return json.Marshal(v.MCPToolCallResponse)
	}
	if v.JsonrpcErrorResponse != nil {
		return json.Marshal(v.JsonrpcErrorResponse)
	}
	return nil, errors.New("MCPToolCallEnvelope: no union member is set")
}

// Validate reports the first constraint violation, or nil.
func (v *MCPToolCallEnvelope) Validate() error {
	if v.matched > 1 {
		return errors.New("MCPToolCallEnvelope: input satisfies more than one alternative, and oneOf permits exactly one")
	}
	if v.MCPToolCallRequest != nil {
		return v.MCPToolCallRequest.Validate()
	}
	if v.MCPToolCallResponse != nil {
		return v.MCPToolCallResponse.Validate()
	}
	if v.JsonrpcErrorResponse != nil {
		return v.JsonrpcErrorResponse.Validate()
	}
	return nil
}

// MCPToolCallArguments MCP tool arguments. UCP reserves meta for transport metadata; operation payload fields such as checkout, cart, order id, or catalog inputs are operation-specific.
type MCPToolCallArguments struct {
	Meta *MCPToolCallMeta `json:"meta,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *MCPToolCallArguments) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("MCPToolCallArguments: null is not a valid object")
	}
	type MCPToolCallArgumentsAlias MCPToolCallArguments
	var named MCPToolCallArgumentsAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = MCPToolCallArguments(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "meta")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v MCPToolCallArguments) MarshalJSON() ([]byte, error) {
	type MCPToolCallArgumentsAlias MCPToolCallArguments
	named, err := json.Marshal(MCPToolCallArgumentsAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *MCPToolCallArguments) Validate() error {
	if v.Meta != nil {
		if err := v.Meta.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// MCPToolCallContentPart MCP content part returned for clients that do not consume structuredContent.
type MCPToolCallContentPart struct {
	Text *string `json:"text,omitzero"`
	Type string  `json:"type"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *MCPToolCallContentPart) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("MCPToolCallContentPart: null is not a valid object")
	}
	type MCPToolCallContentPartAlias MCPToolCallContentPart
	var named MCPToolCallContentPartAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = MCPToolCallContentPart(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"type"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "text")
	delete(all, "type")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v MCPToolCallContentPart) MarshalJSON() ([]byte, error) {
	type MCPToolCallContentPartAlias MCPToolCallContentPart
	named, err := json.Marshal(MCPToolCallContentPartAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *MCPToolCallContentPart) Validate() error {
	if v.present != nil {
		if !v.present["type"] {
			return errors.New("type: required property is missing")
		}
	}
	return nil
}

// MCPToolCallMeta UCP request metadata passed through MCP params.arguments.meta.
type MCPToolCallMeta struct {
	// Optional idempotency key for retry-safe mutating operations.
	IdempotencyKey *string              `json:"idempotency-key,omitzero"`
	UCPAgent       *MCPToolCallUCPAgent `json:"ucp-agent,omitzero"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *MCPToolCallMeta) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("MCPToolCallMeta: null is not a valid object")
	}
	type MCPToolCallMetaAlias MCPToolCallMeta
	var named MCPToolCallMetaAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = MCPToolCallMeta(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	delete(all, "idempotency-key")
	delete(all, "ucp-agent")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v MCPToolCallMeta) MarshalJSON() ([]byte, error) {
	type MCPToolCallMetaAlias MCPToolCallMeta
	named, err := json.Marshal(MCPToolCallMetaAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *MCPToolCallMeta) Validate() error {
	if v.UCPAgent != nil {
		if err := v.UCPAgent.Validate(); err != nil {
			return err
		}
	}
	return nil
}

// MCPToolCallRequest MCP tools/call request envelope for invoking a UCP operation.
type MCPToolCallRequest struct {
	// JSON-RPC request identifier. Notifications omit id; responses echo the request id, or use null when the request id could not be determined.
	ID json.RawMessage `json:"id,omitzero"`
	// JSON-RPC protocol version.
	Jsonrpc string `json:"jsonrpc"`
	// Transport method name. Binding-specific schemas constrain the method namespace.
	Method string `json:"method"`
	// Method parameters. Binding-specific schemas define the object shape.
	Params MCPToolCallRequestParams `json:"params"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and records which of the
// required ones were present.
func (v *MCPToolCallRequest) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("MCPToolCallRequest: null is not a valid object")
	}
	type MCPToolCallRequestAlias MCPToolCallRequest
	var named MCPToolCallRequestAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = MCPToolCallRequest(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 3)
	for _, name := range []string{"jsonrpc", "method", "params"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	return nil
}

// Validate reports the first constraint violation, or nil.
func (v *MCPToolCallRequest) Validate() error {
	if v.present != nil {
		if !v.present["jsonrpc"] {
			return errors.New("jsonrpc: required property is missing")
		}
		if !v.present["method"] {
			return errors.New("method: required property is missing")
		}
		if !v.present["params"] {
			return errors.New("params: required property is missing")
		}
	}
	if v.Jsonrpc != "2.0" {
		return errors.New("jsonrpc: must be \"2.0\"")
	}
	if v.Method != "tools/call" {
		return errors.New("method: must be \"tools/call\"")
	}
	if utf8.RuneCountInString(string(v.Method)) < 1 {
		return errors.New("method: is shorter than minLength 1")
	}
	if err := v.Params.Validate(); err != nil {
		return err
	}
	return nil
}

// MCPToolCallResponse MCP tools/call response envelope. UCP payloads are carried in result.structuredContent; content is compatibility output.
type MCPToolCallResponse struct {
	// JSON-RPC request identifier. Notifications omit id; responses echo the request id, or use null when the request id could not be determined.
	ID json.RawMessage `json:"id"`
	// JSON-RPC protocol version.
	Jsonrpc string `json:"jsonrpc"`
	// Successful transport result. UCP bindings define the nested result payload.
	Result MCPToolCallResponseResult `json:"result"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and records which of the
// required ones were present.
func (v *MCPToolCallResponse) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("MCPToolCallResponse: null is not a valid object")
	}
	type MCPToolCallResponseAlias MCPToolCallResponse
	var named MCPToolCallResponseAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = MCPToolCallResponse(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 3)
	for _, name := range []string{"id", "jsonrpc", "result"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	return nil
}

// Validate reports the first constraint violation, or nil.
func (v *MCPToolCallResponse) Validate() error {
	if v.present != nil {
		if !v.present["id"] {
			return errors.New("id: required property is missing")
		}
		if !v.present["jsonrpc"] {
			return errors.New("jsonrpc: required property is missing")
		}
		if !v.present["result"] {
			return errors.New("result: required property is missing")
		}
	}
	if v.Jsonrpc != "2.0" {
		return errors.New("jsonrpc: must be \"2.0\"")
	}
	if err := v.Result.Validate(); err != nil {
		return err
	}
	return nil
}

// MCPToolCallUCPAgent UCP-Agent metadata carried inside MCP tool arguments.
type MCPToolCallUCPAgent struct {
	// Platform profile URI advertised to the business.
	//
	// Annotation only in draft 2020-12, so not asserted: format.
	Profile string `json:"profile"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *MCPToolCallUCPAgent) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("MCPToolCallUCPAgent: null is not a valid object")
	}
	type MCPToolCallUCPAgentAlias MCPToolCallUCPAgent
	var named MCPToolCallUCPAgentAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = MCPToolCallUCPAgent(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"profile"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "profile")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v MCPToolCallUCPAgent) MarshalJSON() ([]byte, error) {
	type MCPToolCallUCPAgentAlias MCPToolCallUCPAgent
	named, err := json.Marshal(MCPToolCallUCPAgentAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *MCPToolCallUCPAgent) Validate() error {
	if v.present != nil {
		if !v.present["profile"] {
			return errors.New("profile: required property is missing")
		}
	}
	return nil
}

// MCPToolCallRequestParams Method parameters. Binding-specific schemas define the object shape.
//
// The schema also declares 2 oneOf variants that narrow or replace
// these fields; they are not modeled as distinct types yet (phase 4),
// so this type reflects only the shared base.
type MCPToolCallRequestParams struct {
	Arguments MCPToolCallArguments `json:"arguments"`
	// MCP tool name matching the UCP operation binding, such as create_checkout or get_cart.
	Name string `json:"name"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *MCPToolCallRequestParams) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("MCPToolCallRequestParams: null is not a valid object")
	}
	type MCPToolCallRequestParamsAlias MCPToolCallRequestParams
	var named MCPToolCallRequestParamsAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = MCPToolCallRequestParams(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 2)
	for _, name := range []string{"arguments", "name"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "arguments")
	delete(all, "name")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v MCPToolCallRequestParams) MarshalJSON() ([]byte, error) {
	type MCPToolCallRequestParamsAlias MCPToolCallRequestParams
	named, err := json.Marshal(MCPToolCallRequestParamsAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *MCPToolCallRequestParams) Validate() error {
	if v.present != nil {
		if !v.present["arguments"] {
			return errors.New("arguments: required property is missing")
		}
		if !v.present["name"] {
			return errors.New("name: required property is missing")
		}
	}
	if utf8.RuneCountInString(string(v.Name)) < 1 {
		return errors.New("name: is shorter than minLength 1")
	}
	if err := v.Arguments.Validate(); err != nil {
		return err
	}
	return nil
}

// MCPToolCallResponseResult Successful transport result. UCP bindings define the nested result payload.
type MCPToolCallResponseResult struct {
	Content []MCPToolCallContentPart `json:"content,omitzero"`
	// The UCP response payload for the operation.
	Structuredcontent map[string]any `json:"structuredContent"`

	// Extra holds properties the schema does not name. The schema is
	// open (additionalProperties is not false), so extension keys are
	// preserved here and re-emitted on marshal rather than dropped.
	Extra map[string]json.RawMessage `json:"-"`

	// present records which properties the decoder saw, so a required
	// property that was absent can be told from one decoded to its zero
	// value. A nil map means this value was never decoded from JSON.
	present map[string]bool
}

// UnmarshalJSON decodes the named properties and keeps everything else
// in Extra.
func (v *MCPToolCallResponseResult) UnmarshalJSON(data []byte) error {
	if string(data) == "null" {
		return errors.New("MCPToolCallResponseResult: null is not a valid object")
	}
	type MCPToolCallResponseResultAlias MCPToolCallResponseResult
	var named MCPToolCallResponseResultAlias
	if err := json.Unmarshal(data, &named); err != nil {
		return err
	}
	*v = MCPToolCallResponseResult(named)

	var all map[string]json.RawMessage
	if err := json.Unmarshal(data, &all); err != nil {
		return err
	}
	v.present = make(map[string]bool, 1)
	for _, name := range []string{"structuredContent"} {
		if _, ok := all[name]; ok {
			v.present[name] = true
		}
	}
	delete(all, "content")
	delete(all, "structuredContent")
	if len(all) > 0 {
		v.Extra = all
	}
	return nil
}

// MarshalJSON emits the named properties alongside anything held in
// Extra.
func (v MCPToolCallResponseResult) MarshalJSON() ([]byte, error) {
	type MCPToolCallResponseResultAlias MCPToolCallResponseResult
	named, err := json.Marshal(MCPToolCallResponseResultAlias(v))
	if err != nil {
		return nil, err
	}
	if len(v.Extra) == 0 {
		return named, nil
	}
	var merged map[string]json.RawMessage
	if err := json.Unmarshal(named, &merged); err != nil {
		return nil, err
	}
	if merged == nil {
		merged = map[string]json.RawMessage{}
	}
	for k, val := range v.Extra {
		if _, named := merged[k]; !named {
			merged[k] = val
		}
	}
	return json.Marshal(merged)
}

// Validate reports the first constraint violation, or nil.
func (v *MCPToolCallResponseResult) Validate() error {
	if v.present != nil {
		if !v.present["structuredContent"] {
			return errors.New("structuredContent: required property is missing")
		}
	}
	for i := range v.Content {
		if err := v.Content[i].Validate(); err != nil {
			return err
		}
	}
	return nil
}
